{
  "courseId": "sql-interactive",
  "title": "Interactive SQL Mastery",
  "slug": "sql-interactive",
  "category": "Database Systems",
  "level": "Beginner to Advanced",
  "rating": 4.96,
  "reviewsCount": 4120,
  "isPopular": true,
  "badgeText": "Hands-On Interactive • SQLBolt & SQLZoo Edition",
  "description": "Master SQL from basics to advanced relational modeling with 18 authentic lessons from SQLBolt and milestone assessments from SQLZoo. Features a high-speed SQLite WebAssembly engine, live dataset inspection, auto-evaluated tasks, and realistic schema mutations.",
  "thumbnail": "/images/courses/sql_thumbnail.png",
  "skills": [
    "SELECT Queries & Filtering",
    "Relational INNER & OUTER JOINs",
    "Aggregation, GROUP BY & HAVING",
    "Query Execution Pipeline",
    "Database Schema Mutations (INSERT, UPDATE, DELETE, CREATE, ALTER, DROP)",
    "Subqueries & Self-Joins"
  ],
  "estimatedHours": 16,
  "modules": [
    {
      "moduleId": "sql-m1",
      "title": "Module 1: Querying Data with SELECT",
      "description": "Master the fundamental SQL SELECT query syntax, numerical and text filtering with WHERE/LIKE, sorting with ORDER BY, and pagination with LIMIT & OFFSET.",
      "estimatedMinutes": 90,
      "topics": [
        {
          "topicId": "sql-m1-t1",
          "title": "SQL Lesson 1: SELECT queries 101",
          "description": "Hands-on SQL exercise: SQL Lesson 1: SELECT queries 101. Learn syntax, inspect live SQLite tables, and validate queries in real-time.",
          "mode": "SQL_INTERACTIVE",
          "readingTimeMinutes": 12,
          "lessonMarkdown": "To retrieve data from a SQL database, we need to write `SELECT` statements, which are often colloquially \nrefered to as *queries*.  A query in itself is just a statement which declares what data we are \nlooking for, where to find it in the database, and optionally, how to transform it before it is returned. \nIt has a specific syntax though, which is what we are going to learn in the following exercises.\nAs we mentioned in the introduction, you can think of a table in SQL as a type of an entity (ie. Dogs), \nand each row in that table as a specific *instance* of that type (ie. A pug, a beagle, a different \ncolored pug, etc).  This means that the columns would then represent the common properties shared\nby all instances of that entity (ie. Color of fur, length of tail, etc).\nAnd given a table of data, the most basic query we could write would be one that selects for a couple \ncolumns (properties) of the table with all the rows (instances).\n\n> **Select query for a specific columns**\n```sql\nSELECT column, another_column, ...\nFROM mytable;\n```\n\nThe result of this query will be a two-dimensional set of rows and columns, effectively a copy of the \ntable, but only with the columns that we requested.\nIf we want to retrieve absolutely all the columns of data from a table, we can then use the asterisk (`*`) \nshorthand in place of listing all the column names individually.\n\n> **Select query for all columns**\n```sql\nSELECT * \nFROM mytable;\n```\n\nThis query, in particular, is really useful because it's a simple way to inspect a table by dumping \nall the data at once.\n\n### Exercise\n\nWe will be using a database with data about some of Pixar's classic movies for most of our exercises.\nThis first exercise will only involve the **Movies** table, and the default query below currently \nshows all the properties of each movie.  To continue onto the next lesson, alter the query to find\nthe exact information we need for each task.",
          "lessonContent": {
            "textAndVisuals": {
              "content": "To retrieve data from a SQL database, we need to write `SELECT` statements, which are often colloquially \nrefered to as *queries*.  A query in itself is just a statement which declares what data we are \nlooking for, where to find it in the database, and optionally, how to transform it before it is returned. \nIt has a specific syntax though, which is what we are going to learn in the following exercises.\nAs we mentioned in the introduction, you can think of a table in SQL as a type of an entity (ie. Dogs), \nand each row in that table as a specific *instance* of that type (ie. A pug, a beagle, a different \ncolored pug, etc).  This means that the columns would then represent the common properties shared\nby all instances of that entity (ie. Color of fur, length of tail, etc).\nAnd given a table of data, the most basic query we could write would be one that selects for a couple \ncolumns (properties) of the table with all the rows (instances).\n\n> **Select query for a specific columns**\n```sql\nSELECT column, another_column, ...\nFROM mytable;\n```\n\nThe result of this query will be a two-dimensional set of rows and columns, effectively a copy of the \ntable, but only with the columns that we requested.\nIf we want to retrieve absolutely all the columns of data from a table, we can then use the asterisk (`*`) \nshorthand in place of listing all the column names individually.\n\n> **Select query for all columns**\n```sql\nSELECT * \nFROM mytable;\n```\n\nThis query, in particular, is really useful because it's a simple way to inspect a table by dumping \nall the data at once.\n\n### Exercise\n\nWe will be using a database with data about some of Pixar's classic movies for most of our exercises.\nThis first exercise will only involve the **Movies** table, and the default query below currently \nshows all the properties of each movie.  To continue onto the next lesson, alter the query to find\nthe exact information we need for each task."
            }
          },
          "sqlExercise": {
            "schemaSql": "CREATE TABLE movies (\n    id integer primary key autoincrement,\n    title text,\n    director text,\n    year integer, \n    length_minutes integer\n);",
            "seedSql": "INSERT INTO movies (id, title, director, year, length_minutes) VALUES\n(1, 'Toy Story', 'John Lasseter', 1995, 81),\n(2, 'A Bug''s Life', 'John Lasseter', 1998, 95),\n(3, 'Toy Story 2', 'John Lasseter', 1999, 93),\n(4, 'Monsters, Inc.', 'Pete Docter', 2001, 92),\n(5, 'Finding Nemo', 'Andrew Stanton', 2003, 107),\n(6, 'The Incredibles', 'Brad Bird', 2004, 116),\n(7, 'Cars', 'John Lasseter', 2006, 117),\n(8, 'Ratatouille', 'Brad Bird', 2007, 115),\n(9, 'WALL-E', 'Andrew Stanton', 2008, 104),\n(10, 'Up', 'Pete Docter', 2009, 101),\n(11, 'Toy Story 3', 'Lee Unkrich', 2010, 103),\n(12, 'Cars 2', 'John Lasseter', 2011, 120),\n(13, 'Brave', 'Brenda Chapman', 2012, 102),\n(14, 'Monsters University', 'Dan Scanlon', 2013, 110);\n",
            "defaultTable": "movies",
            "defaultQuery": "-- Write your SQL statement below to solve the exercise tasks:\nSELECT * FROM movies LIMIT 10;",
            "tasks": [
              {
                "instruction": "Find the title of each film",
                "expectedQuery": "SELECT title FROM movies;",
                "solution": "SELECT title FROM movies;",
                "orderSensitive": false
              },
              {
                "instruction": "Find the director of each film",
                "expectedQuery": "SELECT director FROM movies;",
                "solution": "SELECT director FROM movies;",
                "orderSensitive": false
              },
              {
                "instruction": "Find the title and director of each film",
                "expectedQuery": "SELECT title, director FROM movies;",
                "solution": "SELECT title, director FROM movies;",
                "orderSensitive": false
              },
              {
                "instruction": "Find the title and year of each film",
                "expectedQuery": "SELECT title, year FROM movies;",
                "solution": "SELECT title, year FROM movies;",
                "orderSensitive": false
              },
              {
                "instruction": "Find all the information about each film",
                "expectedQuery": "SELECT * FROM movies;",
                "solution": "SELECT * FROM movies;",
                "orderSensitive": false
              }
            ]
          }
        },
        {
          "topicId": "sql-m1-t2",
          "title": "SQL Lesson 2: Queries with constraints (Pt. 1)",
          "description": "Hands-on SQL exercise: SQL Lesson 2: Queries with constraints (Pt. 1). Learn syntax, inspect live SQLite tables, and validate queries in real-time.",
          "mode": "SQL_INTERACTIVE",
          "readingTimeMinutes": 15,
          "lessonMarkdown": "Now we know how to select for specific columns of data from a table, but if you had a table with a \nhundred million rows of data, reading through all the rows would be inefficient and perhaps even\nimpossible.\nIn order to filter certain results from being returned, we need to use a `WHERE` clause in the query.\nThe clause is applied to each row of data by checking specific column values to determine whether it\nshould be included in the results or not.\n\n    Select query with constraints\n    SELECT column, another_column, ...\nFROM mytable\nWHERE *condition*\n    AND/OR *another_condition*\n    AND/OR ...;\n\nMore complex clauses can be constructed by joining numerous `AND` or `OR` logical keywords (ie. \nnum_wheels >= 4 AND doors <= 2).  And below are some useful operators that you can use for numerical \ndata (ie. integer or floating point):\n\n    \n        \n            Operator\n            Condition\n            SQL Example\n        \n        \n            =, !=, <, <=, >, &gt=\n            Standard numerical operators\n            col_name != 4\n        \n        \n            BETWEEN ... AND ...\n            Number is within range of two values (inclusive)\n            col_name BETWEEN 1.5 AND 10.5\n        \n        \n            NOT BETWEEN ... AND ...\n            Number is not within range of two values (inclusive)\n            col_name NOT BETWEEN 1 AND 10\n        \n        \n            IN (...)\n            Number exists in a list\n            col_name IN (2, 4, 6)\n        \n        \n            NOT IN (...)\n            Number does not exist in a list\n            col_name NOT IN (1, 3, 5)\n        \n    \n\nIn addition to making the results more manageable to understand, writing clauses to constrain the\nset of rows returned also allows the query to run faster due to the reduction in unnecessary data\nbeing returned.\n\n    Did you know?\n    \n    As you might have noticed by now, SQL doesn't *require* you to write the keywords all capitalized,\n    but as a convention, it helps people distinguish SQL keywords from column and tables names, and makes\n    the query easier to read.\n    \n\n### Exercise\n\nUsing the right constraints, find the information we need from the **Movies** table for each task below.",
          "lessonContent": {
            "textAndVisuals": {
              "content": "Now we know how to select for specific columns of data from a table, but if you had a table with a \nhundred million rows of data, reading through all the rows would be inefficient and perhaps even\nimpossible.\nIn order to filter certain results from being returned, we need to use a `WHERE` clause in the query.\nThe clause is applied to each row of data by checking specific column values to determine whether it\nshould be included in the results or not.\n\n    Select query with constraints\n    SELECT column, another_column, ...\nFROM mytable\nWHERE *condition*\n    AND/OR *another_condition*\n    AND/OR ...;\n\nMore complex clauses can be constructed by joining numerous `AND` or `OR` logical keywords (ie. \nnum_wheels >= 4 AND doors <= 2).  And below are some useful operators that you can use for numerical \ndata (ie. integer or floating point):\n\n    \n        \n            Operator\n            Condition\n            SQL Example\n        \n        \n            =, !=, <, <=, >, &gt=\n            Standard numerical operators\n            col_name != 4\n        \n        \n            BETWEEN ... AND ...\n            Number is within range of two values (inclusive)\n            col_name BETWEEN 1.5 AND 10.5\n        \n        \n            NOT BETWEEN ... AND ...\n            Number is not within range of two values (inclusive)\n            col_name NOT BETWEEN 1 AND 10\n        \n        \n            IN (...)\n            Number exists in a list\n            col_name IN (2, 4, 6)\n        \n        \n            NOT IN (...)\n            Number does not exist in a list\n            col_name NOT IN (1, 3, 5)\n        \n    \n\nIn addition to making the results more manageable to understand, writing clauses to constrain the\nset of rows returned also allows the query to run faster due to the reduction in unnecessary data\nbeing returned.\n\n    Did you know?\n    \n    As you might have noticed by now, SQL doesn't *require* you to write the keywords all capitalized,\n    but as a convention, it helps people distinguish SQL keywords from column and tables names, and makes\n    the query easier to read.\n    \n\n### Exercise\n\nUsing the right constraints, find the information we need from the **Movies** table for each task below."
            }
          },
          "sqlExercise": {
            "schemaSql": "CREATE TABLE movies (\n    id integer primary key autoincrement,\n    title text,\n    director text,\n    year integer, \n    length_minutes integer\n);",
            "seedSql": "INSERT INTO movies (id, title, director, year, length_minutes) VALUES\n(1, 'Toy Story', 'John Lasseter', 1995, 81),\n(2, 'A Bug''s Life', 'John Lasseter', 1998, 95),\n(3, 'Toy Story 2', 'John Lasseter', 1999, 93),\n(4, 'Monsters, Inc.', 'Pete Docter', 2001, 92),\n(5, 'Finding Nemo', 'Andrew Stanton', 2003, 107),\n(6, 'The Incredibles', 'Brad Bird', 2004, 116),\n(7, 'Cars', 'John Lasseter', 2006, 117),\n(8, 'Ratatouille', 'Brad Bird', 2007, 115),\n(9, 'WALL-E', 'Andrew Stanton', 2008, 104),\n(10, 'Up', 'Pete Docter', 2009, 101),\n(11, 'Toy Story 3', 'Lee Unkrich', 2010, 103),\n(12, 'Cars 2', 'John Lasseter', 2011, 120),\n(13, 'Brave', 'Brenda Chapman', 2012, 102),\n(14, 'Monsters University', 'Dan Scanlon', 2013, 110);\n",
            "defaultTable": "movies",
            "defaultQuery": "-- Write your SQL statement below to solve the exercise tasks:\nSELECT * FROM movies LIMIT 10;",
            "tasks": [
              {
                "instruction": "Find the movie with a row id of 6",
                "expectedQuery": "SELECT title FROM movies \nWHERE id = 6;",
                "solution": "SELECT title FROM movies \nWHERE id = 6;",
                "orderSensitive": false
              },
              {
                "instruction": "Find the movies released in the year s between 2000 and 2010",
                "expectedQuery": "SELECT title FROM movies\nWHERE year BETWEEN 2000 AND 2010;",
                "solution": "SELECT title FROM movies\nWHERE year BETWEEN 2000 AND 2010;",
                "orderSensitive": false
              },
              {
                "instruction": "Find the movies not released in the year s between 2000 and 2010",
                "expectedQuery": "SELECT title FROM movies\nWHERE year < 2000 OR year > 2010;",
                "solution": "SELECT title FROM movies\nWHERE year < 2000 OR year > 2010;",
                "orderSensitive": false
              },
              {
                "instruction": "Find the first 5 Pixar movies and their release year",
                "expectedQuery": "SELECT title FROM movies\nWHERE year <= 2003;",
                "solution": "SELECT title FROM movies\nWHERE year <= 2003;",
                "orderSensitive": false
              }
            ]
          }
        },
        {
          "topicId": "sql-m1-t3",
          "title": "SQL Lesson 3: Queries with constraints (Pt. 2)",
          "description": "Hands-on SQL exercise: SQL Lesson 3: Queries with constraints (Pt. 2). Learn syntax, inspect live SQLite tables, and validate queries in real-time.",
          "mode": "SQL_INTERACTIVE",
          "readingTimeMinutes": 15,
          "lessonMarkdown": "When writing `WHERE` clauses with columns containing text data, SQL supports a number of useful operators\nto do things like case-insensitive string comparison and wildcard pattern matching.  We show a few common\ntext-data specific operators below:\n\n    \n        \n            Operator\n            Condition\n            Example\n        \n        \n            =\n            Case sensitive exact string comparison (*notice the single equals*)\n            col_name = \"abc\"\n        \n        \n            != or <>\n            Case sensitive exact string inequality comparison\n            col_name != \"abcd\"\n        \n        \n            LIKE\n            Case insensitive exact string comparison\n            col_name LIKE \"ABC\"\n        \n        \n            NOT LIKE\n            Case insensitive exact string inequality comparison\n            col_name NOT LIKE \"ABCD\"\n        \n        \n            %\n            Used anywhere in a string to match \n                a sequence of zero or more characters (only with LIKE or NOT LIKE)\n            col_name LIKE \"%AT%\"\n                (matches \"AT\", \"ATTIC\", \"CAT\" \n                    or even \"BATS\")\n        \n        \n            _\n            Used anywhere in a string to match \n                a single character (only with LIKE or NOT LIKE)\n            col_name LIKE \"AN_\"\n                (matches \"AND\", but not \"AN\")\n        \n        \n            IN (...)\n            String exists in a list\n            col_name IN (\"A\", \"B\", \"C\")\n        \n        \n            NOT IN (...)\n            String does not exist in a list\n            col_name NOT IN (\"D\", \"E\", \"F\")\n        \n    \n\n    Did you know?\n    \n    All strings must be quoted so that the query parser can distinguish words in the string from SQL keywords.\n    \n\nWe should note that while most database implementations are quite efficient when using these operators, \nfull-text search is best left to dedicated libraries like Apache Lucene \nor Sphinx. These libraries are designed specifically to \ndo full text search, and as a result are more efficient and can support a wider variety of search \nfeatures including internationalization and advanced queries.\n\n### Exercise\n\nHere's the definition of a query with a `WHERE` clause again, go ahead and try and write some queries\nwith the operators above to limit the results to the information we need in the tasks below.\n\n    Select query with constraints\n    SELECT column, another_column, ...\nFROM mytable\nWHERE *condition*\n    AND/OR *another_condition*\n    AND/OR ...;",
          "lessonContent": {
            "textAndVisuals": {
              "content": "When writing `WHERE` clauses with columns containing text data, SQL supports a number of useful operators\nto do things like case-insensitive string comparison and wildcard pattern matching.  We show a few common\ntext-data specific operators below:\n\n    \n        \n            Operator\n            Condition\n            Example\n        \n        \n            =\n            Case sensitive exact string comparison (*notice the single equals*)\n            col_name = \"abc\"\n        \n        \n            != or <>\n            Case sensitive exact string inequality comparison\n            col_name != \"abcd\"\n        \n        \n            LIKE\n            Case insensitive exact string comparison\n            col_name LIKE \"ABC\"\n        \n        \n            NOT LIKE\n            Case insensitive exact string inequality comparison\n            col_name NOT LIKE \"ABCD\"\n        \n        \n            %\n            Used anywhere in a string to match \n                a sequence of zero or more characters (only with LIKE or NOT LIKE)\n            col_name LIKE \"%AT%\"\n                (matches \"AT\", \"ATTIC\", \"CAT\" \n                    or even \"BATS\")\n        \n        \n            _\n            Used anywhere in a string to match \n                a single character (only with LIKE or NOT LIKE)\n            col_name LIKE \"AN_\"\n                (matches \"AND\", but not \"AN\")\n        \n        \n            IN (...)\n            String exists in a list\n            col_name IN (\"A\", \"B\", \"C\")\n        \n        \n            NOT IN (...)\n            String does not exist in a list\n            col_name NOT IN (\"D\", \"E\", \"F\")\n        \n    \n\n    Did you know?\n    \n    All strings must be quoted so that the query parser can distinguish words in the string from SQL keywords.\n    \n\nWe should note that while most database implementations are quite efficient when using these operators, \nfull-text search is best left to dedicated libraries like Apache Lucene \nor Sphinx. These libraries are designed specifically to \ndo full text search, and as a result are more efficient and can support a wider variety of search \nfeatures including internationalization and advanced queries.\n\n### Exercise\n\nHere's the definition of a query with a `WHERE` clause again, go ahead and try and write some queries\nwith the operators above to limit the results to the information we need in the tasks below.\n\n    Select query with constraints\n    SELECT column, another_column, ...\nFROM mytable\nWHERE *condition*\n    AND/OR *another_condition*\n    AND/OR ...;"
            }
          },
          "sqlExercise": {
            "schemaSql": "CREATE TABLE movies (\n    id integer primary key autoincrement,\n    title text,\n    director text,\n    year integer, \n    length_minutes integer\n);",
            "seedSql": "INSERT INTO movies (id, title, director, year, length_minutes) VALUES\n(1, 'Toy Story', 'John Lasseter', 1995, 81),\n(2, 'A Bug''s Life', 'John Lasseter', 1998, 95),\n(3, 'Toy Story 2', 'John Lasseter', 1999, 93),\n(4, 'Monsters, Inc.', 'Pete Docter', 2001, 92),\n(5, 'Finding Nemo', 'Andrew Stanton', 2003, 107),\n(6, 'The Incredibles', 'Brad Bird', 2004, 116),\n(7, 'Cars', 'John Lasseter', 2006, 117),\n(8, 'Ratatouille', 'Brad Bird', 2007, 115),\n(9, 'WALL-E', 'Andrew Stanton', 2008, 104),\n(10, 'Up', 'Pete Docter', 2009, 101),\n(11, 'Toy Story 3', 'Lee Unkrich', 2010, 103),\n(12, 'Cars 2', 'John Lasseter', 2011, 120),\n(13, 'Brave', 'Brenda Chapman', 2012, 102),\n(14, 'Monsters University', 'Dan Scanlon', 2013, 110);\n",
            "defaultTable": "movies",
            "defaultQuery": "-- Write your SQL statement below to solve the exercise tasks:\nSELECT * FROM movies LIMIT 10;",
            "tasks": [
              {
                "instruction": "Find all the Toy Story movies",
                "expectedQuery": "SELECT title FROM movies \nWHERE title LIKE \"Toy Story%\";",
                "solution": "SELECT title FROM movies \nWHERE title LIKE \"Toy Story%\";",
                "orderSensitive": false
              },
              {
                "instruction": "Find all the movies directed by John Lasseter",
                "expectedQuery": "SELECT title FROM movies \nWHERE director = \"John Lasseter\";",
                "solution": "SELECT title FROM movies \nWHERE director = \"John Lasseter\";",
                "orderSensitive": false
              },
              {
                "instruction": "Find all the movies (and director) not directed by John Lasseter",
                "expectedQuery": "SELECT title FROM movies \nWHERE director != \"John Lasseter\";",
                "solution": "SELECT title FROM movies \nWHERE director != \"John Lasseter\";",
                "orderSensitive": false
              },
              {
                "instruction": "Find all the WALL-* movies",
                "expectedQuery": "SELECT title FROM movies \nWHERE title LIKE \"WALL-_\";",
                "solution": "SELECT title FROM movies \nWHERE title LIKE \"WALL-_\";",
                "orderSensitive": false
              }
            ]
          }
        },
        {
          "topicId": "sql-m1-t4",
          "title": "SQL Lesson 4: Filtering and sorting Query results",
          "description": "Hands-on SQL exercise: SQL Lesson 4: Filtering and sorting Query results. Learn syntax, inspect live SQLite tables, and validate queries in real-time.",
          "mode": "SQL_INTERACTIVE",
          "readingTimeMinutes": 15,
          "lessonMarkdown": "Even though the data in a database may be unique, the results of any particular query may not be – \ntake our Movies table for example, many different movies can be released the same year.  In such cases, \nSQL provides a convenient way to discard rows that have a duplicate column value by using the `DISTINCT`\nkeyword.\n\n> **Select query with unique results**\n```sql\nSELECT **DISTINCT** column, another_column, ...\nFROM mytable\nWHERE *condition(s)*;\n```\n\nSince the `DISTINCT` keyword will blindly remove duplicate rows, we will learn in a future lesson\nhow to discard duplicates based on specific columns using grouping and the `GROUP BY` clause.\n\n### Ordering results\n\nUnlike our neatly ordered table in the last few lessons, most data in real databases are added in no\nparticular column order.  As a result, it can be difficult to read through and understand the results \nof a query as the size of a table increases to thousands or even millions rows.\nTo help with this, SQL provides a way to sort your results by a given column in ascending or descending \norder using the `ORDER BY` clause.\n\n> **Select query with ordered results**\n```sql\nSELECT column, another_column, ...\nFROM mytable\nWHERE *condition(s)*\n**ORDER BY column ASC/DESC**;\n```\n\nWhen an `ORDER BY` clause is specified, each row is sorted alpha-numerically based on the specified\ncolumn's value.  In some databases, you can also specify a collation to better sort data containing\ninternational text.\n\n### Limiting results to a subset\n\nAnother clause which is commonly used with the `ORDER BY` clause are the `LIMIT` and `OFFSET` clauses, \nwhich are a useful optimization to indicate to the database the subset of the results you care about.\nThe `LIMIT` will reduce the number of rows to return, and the optional `OFFSET` will specify where to \nbegin counting the number rows from.\n\n> **Select query with limited rows**\n```sql\nSELECT column, another_column, ...\nFROM mytable\nWHERE *condition(s)*\nORDER BY column ASC/DESC\n**LIMIT num_limit OFFSET num_offset**;\n```\n\nIf you think about websites like Reddit or Pinterest, the front page is a list of links sorted by popularity \nand time, and each subsequent page can be represented by sets of links at different offsets in the \ndatabase.  Using these clauses, the database can then execute queries faster and more efficiently\nby processing and returning only the requested content.\n\n    Did you know?\n    \n    If you are curious about when the `LIMIT` and `OFFSET` are applied relative \n    to the other parts of a query, they are generally done last after the other clauses have been applied.  \n    We'll touch more on this in Lesson 12: Order of execution\n    after introducting a few more parts of the query.\n    \n\n### Exercise\n\nThere are a few concepts in this lesson, but all are pretty straight-forward to apply.  To spice things \nup, we've gone and scrambled the **Movies** table for you in the exercise to better mimic what kind of\ndata you might see in real life.  Try and use the necessary keywords and clauses introduced above in \nyour queries.",
          "lessonContent": {
            "textAndVisuals": {
              "content": "Even though the data in a database may be unique, the results of any particular query may not be – \ntake our Movies table for example, many different movies can be released the same year.  In such cases, \nSQL provides a convenient way to discard rows that have a duplicate column value by using the `DISTINCT`\nkeyword.\n\n> **Select query with unique results**\n```sql\nSELECT **DISTINCT** column, another_column, ...\nFROM mytable\nWHERE *condition(s)*;\n```\n\nSince the `DISTINCT` keyword will blindly remove duplicate rows, we will learn in a future lesson\nhow to discard duplicates based on specific columns using grouping and the `GROUP BY` clause.\n\n### Ordering results\n\nUnlike our neatly ordered table in the last few lessons, most data in real databases are added in no\nparticular column order.  As a result, it can be difficult to read through and understand the results \nof a query as the size of a table increases to thousands or even millions rows.\nTo help with this, SQL provides a way to sort your results by a given column in ascending or descending \norder using the `ORDER BY` clause.\n\n> **Select query with ordered results**\n```sql\nSELECT column, another_column, ...\nFROM mytable\nWHERE *condition(s)*\n**ORDER BY column ASC/DESC**;\n```\n\nWhen an `ORDER BY` clause is specified, each row is sorted alpha-numerically based on the specified\ncolumn's value.  In some databases, you can also specify a collation to better sort data containing\ninternational text.\n\n### Limiting results to a subset\n\nAnother clause which is commonly used with the `ORDER BY` clause are the `LIMIT` and `OFFSET` clauses, \nwhich are a useful optimization to indicate to the database the subset of the results you care about.\nThe `LIMIT` will reduce the number of rows to return, and the optional `OFFSET` will specify where to \nbegin counting the number rows from.\n\n> **Select query with limited rows**\n```sql\nSELECT column, another_column, ...\nFROM mytable\nWHERE *condition(s)*\nORDER BY column ASC/DESC\n**LIMIT num_limit OFFSET num_offset**;\n```\n\nIf you think about websites like Reddit or Pinterest, the front page is a list of links sorted by popularity \nand time, and each subsequent page can be represented by sets of links at different offsets in the \ndatabase.  Using these clauses, the database can then execute queries faster and more efficiently\nby processing and returning only the requested content.\n\n    Did you know?\n    \n    If you are curious about when the `LIMIT` and `OFFSET` are applied relative \n    to the other parts of a query, they are generally done last after the other clauses have been applied.  \n    We'll touch more on this in Lesson 12: Order of execution\n    after introducting a few more parts of the query.\n    \n\n### Exercise\n\nThere are a few concepts in this lesson, but all are pretty straight-forward to apply.  To spice things \nup, we've gone and scrambled the **Movies** table for you in the exercise to better mimic what kind of\ndata you might see in real life.  Try and use the necessary keywords and clauses introduced above in \nyour queries."
            }
          },
          "sqlExercise": {
            "schemaSql": "CREATE TABLE movies (\n    id integer primary key autoincrement,\n    title text,\n    director text,\n    year integer, \n    length_minutes integer\n);",
            "seedSql": "INSERT INTO movies (id, title, director, year, length_minutes) VALUES\n(1, 'Toy Story', 'John Lasseter', 1995, 81),\n(2, 'A Bug''s Life', 'John Lasseter', 1998, 95),\n(3, 'Toy Story 2', 'John Lasseter', 1999, 93),\n(4, 'Monsters, Inc.', 'Pete Docter', 2001, 92),\n(5, 'Finding Nemo', 'Andrew Stanton', 2003, 107),\n(6, 'The Incredibles', 'Brad Bird', 2004, 116),\n(7, 'Cars', 'John Lasseter', 2006, 117),\n(8, 'Ratatouille', 'Brad Bird', 2007, 115),\n(9, 'WALL-E', 'Andrew Stanton', 2008, 104),\n(10, 'Up', 'Pete Docter', 2009, 101),\n(11, 'Toy Story 3', 'Lee Unkrich', 2010, 103),\n(12, 'Cars 2', 'John Lasseter', 2011, 120),\n(13, 'Brave', 'Brenda Chapman', 2012, 102),\n(14, 'Monsters University', 'Dan Scanlon', 2013, 110);\n",
            "defaultTable": "movies",
            "defaultQuery": "-- Write your SQL statement below to solve the exercise tasks:\nSELECT * FROM movies LIMIT 10;",
            "tasks": [
              {
                "instruction": "List all directors of Pixar movies (alphabetically), without duplicates",
                "expectedQuery": "SELECT DISTINCT director FROM movies\nORDER BY director ASC;",
                "solution": "SELECT DISTINCT director FROM movies\nORDER BY director ASC;",
                "orderSensitive": false
              },
              {
                "instruction": "List the last four Pixar movies released (ordered from most recent to least)",
                "expectedQuery": "SELECT title FROM movies\nORDER BY year DESC\nLIMIT 4;",
                "solution": "SELECT title FROM movies\nORDER BY year DESC\nLIMIT 4;",
                "orderSensitive": false
              },
              {
                "instruction": "List the first five Pixar movies sorted alphabetically",
                "expectedQuery": "SELECT title FROM movies\nORDER BY title ASC\nLIMIT 5;",
                "solution": "SELECT title FROM movies\nORDER BY title ASC\nLIMIT 5;",
                "orderSensitive": false
              },
              {
                "instruction": "List the next five Pixar movies sorted alphabetically",
                "expectedQuery": "SELECT title FROM movies\nORDER BY title ASC\nLIMIT 5 OFFSET 5;",
                "solution": "SELECT title FROM movies\nORDER BY title ASC\nLIMIT 5 OFFSET 5;",
                "orderSensitive": false
              }
            ]
          }
        },
        {
          "topicId": "sql-m1-t5",
          "title": "SQL Review: Simple SELECT Queries",
          "description": "Hands-on SQL exercise: SQL Review: Simple SELECT Queries. Learn syntax, inspect live SQLite tables, and validate queries in real-time.",
          "mode": "SQL_INTERACTIVE",
          "readingTimeMinutes": 20,
          "lessonMarkdown": "You've done a good job getting to this point!  Now that you've gotten a taste of how to write a basic \nquery, you need to practice writing queries that solve actual problems.\n\n> **SELECT query**\n```sql\nSELECT column, another_column, ...\nFROM mytable\nWHERE *condition(s)*\nORDER BY column ASC/DESC\nLIMIT num_limit OFFSET num_offset;\n```\n\n### Exercise\n\nIn the exercise below, you will be working with a different table.  This table instead contains information \nabout a few of the most populous cities of North America[1] including their population and geo-spatial location in the world.  \n\n    Did you know?\n    \n    Positive latitudes correspond to the northern hemisphere, and positive longitudes \n    correspond to the eastern hemisphere.  Since North America is north of the equator and west of \n    the prime meridian, all of the cities in the list have positive latitudes and negative longitudes.\n    \n\nTry and write some queries to find the information requested in the tasks below.  You may have to \nuse a different combination of clauses in your query for each task.  Once you're done, continue onto \nthe next lesson to learn about queries that span multiple tables.",
          "lessonContent": {
            "textAndVisuals": {
              "content": "You've done a good job getting to this point!  Now that you've gotten a taste of how to write a basic \nquery, you need to practice writing queries that solve actual problems.\n\n> **SELECT query**\n```sql\nSELECT column, another_column, ...\nFROM mytable\nWHERE *condition(s)*\nORDER BY column ASC/DESC\nLIMIT num_limit OFFSET num_offset;\n```\n\n### Exercise\n\nIn the exercise below, you will be working with a different table.  This table instead contains information \nabout a few of the most populous cities of North America[1] including their population and geo-spatial location in the world.  \n\n    Did you know?\n    \n    Positive latitudes correspond to the northern hemisphere, and positive longitudes \n    correspond to the eastern hemisphere.  Since North America is north of the equator and west of \n    the prime meridian, all of the cities in the list have positive latitudes and negative longitudes.\n    \n\nTry and write some queries to find the information requested in the tasks below.  You may have to \nuse a different combination of clauses in your query for each task.  Once you're done, continue onto \nthe next lesson to learn about queries that span multiple tables."
            }
          },
          "sqlExercise": {
            "schemaSql": "CREATE TABLE north_american_cities (\n    -- id integer primary key autoincrement,\n    city text,\n    country text,\n    population integer,\n    latitude real,\n    longitude real\n);",
            "seedSql": "INSERT INTO north_american_cities (city, country, population, latitude, longitude) VALUES\n('Guadalajara', 'Mexico', 1500800, 20.659699, -103.349609),\n('Toronto', 'Canada', 2795060, 43.653226, -79.383184),\n('Houston', 'United States', 2195914, 29.760427, -95.369803),\n('New York', 'United States', 8405837, 40.712784, -74.005941),\n('Philadelphia', 'United States', 1553165, 39.952584, -75.165222),\n('Havana', 'Cuba', 2106146, 23.05407, -82.345189),\n('Mexico City', 'Mexico', 8555500, 19.432608, -99.133208),\n('Phoenix', 'United States', 1513367, 33.448377, -112.074037),\n('Los Angeles', 'United States', 3884307, 34.052234, -118.243685),\n('Ecatepec de Morelos', 'Mexico', 1742000, 19.601841, -99.050674),\n('Montreal', 'Canada', 1717767, 45.501689, -73.567256),\n('Chicago', 'United States', 2718782, 41.878114, -87.629798);\n",
            "defaultTable": "north_american_cities",
            "defaultQuery": "-- Write your SQL statement below to solve the exercise tasks:\nSELECT * FROM north_american_cities LIMIT 10;",
            "tasks": [
              {
                "instruction": "List all the Canadian cities and their populations",
                "expectedQuery": "SELECT city, population FROM north_american_cities\nWHERE country = \"Canada\";",
                "solution": "SELECT city, population FROM north_american_cities\nWHERE country = \"Canada\";",
                "orderSensitive": false
              },
              {
                "instruction": "Order all the cities in the United States by their latitude from north to south",
                "expectedQuery": "SELECT city FROM north_american_cities\nWHERE country = \"United States\"\nORDER BY latitude DESC;",
                "solution": "SELECT city FROM north_american_cities\nWHERE country = \"United States\"\nORDER BY latitude DESC;",
                "orderSensitive": false
              },
              {
                "instruction": "List all the cities west of Chicago, ordered from west to east",
                "expectedQuery": "SELECT city FROM north_american_cities\nWHERE longitude < -87.629798\nORDER BY longitude ASC;",
                "solution": "SELECT city FROM north_american_cities\nWHERE longitude < -87.629798\nORDER BY longitude ASC;",
                "orderSensitive": false
              },
              {
                "instruction": "List the two largest cities in Mexico (by population)",
                "expectedQuery": "SELECT city FROM north_american_cities\nWHERE country LIKE \"Mexico\"\nORDER BY population DESC\nLIMIT 2;",
                "solution": "SELECT city FROM north_american_cities\nWHERE country LIKE \"Mexico\"\nORDER BY population DESC\nLIMIT 2;",
                "orderSensitive": false
              },
              {
                "instruction": "List the third and fourth largest cities (by population) in the United States and their population",
                "expectedQuery": "SELECT city FROM north_american_cities\nWHERE country LIKE \"United States\"\nORDER BY population DESC\nLIMIT 2 OFFSET 2;",
                "solution": "SELECT city FROM north_american_cities\nWHERE country LIKE \"United States\"\nORDER BY population DESC\nLIMIT 2 OFFSET 2;",
                "orderSensitive": false
              }
            ]
          }
        }
      ],
      "msa": {
        "title": "Module 1 Assessment: SELECT Queries & Filtering Mastery (SQLZoo)",
        "passPercent": 85,
        "durationMinutes": 20,
        "mcqSection": {
          "sectionTitle": "Section 1: SQLZoo SELECT Basics & Filtering MCQs",
          "durationMinutes": 20,
          "passCutoffPercent": 85,
          "questions": [
            {
              "questionId": "select_quiz-q1",
              "question": "Select the code which produces this table name population Bahrain 1234571 Swaziland 1220000 Timor-Leste 1066409",
              "prompt": "Select the code which produces this table name population Bahrain 1234571 Swaziland 1220000 Timor-Leste 1066409",
              "options": [
                "FROM world SELECT name , population BETWEEN 1000000 AND 1250000",
                "FROM name , population WHERE population BETWEEN 1000000 AND 1250000 SELECT world",
                "SELECT name , population FROM world WHERE population BETWEEN 1000000 AND 1250000",
                "SELECT population BETWEEN 1000000 AND 1250000 FROM world"
              ],
              "correctAnswer": 2,
              "correctIndex": 2,
              "correctOptionIndex": 2,
              "explanation": "Correct SQL Syntax/Output: SELECT name , population FROM world WHERE population BETWEEN 1000000 AND 1250000"
            },
            {
              "questionId": "select_quiz-q3",
              "question": "Select the code which shows the countries that end in A or L",
              "prompt": "Select the code which shows the countries that end in A or L",
              "options": [
                "SELECT name FROM world WHERE name LIKE 'a%' AND name LIKE 'l%'",
                "SELECT name FROM world WHERE name LIKE 'a%' OR name LIKE 'l%'",
                "SELECT name FROM world WHERE name LIKE '%a' AND name LIKE '%l'",
                "SELECT name FROM world WHERE name LIKE '%a' OR 'l%'"
              ],
              "correctAnswer": 0,
              "correctIndex": 0,
              "correctOptionIndex": 0,
              "explanation": "Correct SQL Syntax/Output: SELECT name FROM world WHERE name LIKE 'a%' AND name LIKE 'l%'"
            },
            {
              "questionId": "select_quiz-q6",
              "question": "Select the code that would show the countries with an area larger than 50000 and a population smaller than 10000000",
              "prompt": "Select the code that would show the countries with an area larger than 50000 and a population smaller than 10000000",
              "options": [
                "SELECT name , area , population FROM world WHERE area < 50000 AND population < 10000000",
                "SELECT name , area , population FROM world WHERE area < 50000 AND population > 10000000",
                "SELECT name , area , population FROM world WHERE area > 50000 AND population < 10000000",
                "SELECT name , area , population FROM world WHERE area > 50000 AND population > 10000000"
              ],
              "correctAnswer": 2,
              "correctIndex": 2,
              "correctOptionIndex": 2,
              "explanation": "Correct SQL Syntax/Output: SELECT name , area , population FROM world WHERE area > 50000 AND population < 10000000"
            },
            {
              "questionId": "select_quiz-q7",
              "question": "Select the code that shows the population density of China, Australia, Nigeria and France",
              "prompt": "Select the code that shows the population density of China, Australia, Nigeria and France",
              "options": [
                "SELECT name , area / population FROM world WHERE name IN ( 'China' , 'Nigeria' , 'France' , 'Australia' )",
                "SELECT name , area / population FROM world WHERE name LIKE ( 'China' , 'Nigeria' , 'France' , 'Australia' )",
                "SELECT name , population / area FROM world WHERE name IN ( 'China' , 'Nigeria' , 'France' , 'Australia' )",
                "SELECT name , population / area FROM world WHERE name LIKE ( 'China' , 'Nigeria' , 'France' , 'Australia' )"
              ],
              "correctAnswer": 2,
              "correctIndex": 2,
              "correctOptionIndex": 2,
              "explanation": "Correct SQL Syntax/Output: SELECT name , population / area FROM world WHERE name IN ( 'China' , 'Nigeria' , 'France' , 'Australia' )"
            },
            {
              "questionId": "m1-msa-q5",
              "question": "Which clause is used to eliminate duplicate values from the output of a SELECT query?",
              "prompt": "Which clause is used to eliminate duplicate values from the output of a SELECT query?",
              "options": [
                "UNIQUE",
                "DISTINCT",
                "FILTER",
                "GROUP"
              ],
              "correctAnswer": 1,
              "correctIndex": 1,
              "correctOptionIndex": 1,
              "explanation": "The DISTINCT keyword ensures that only unique rows are returned."
            },
            {
              "questionId": "m1-msa-q6",
              "question": "In SQL, what is the effect of writing `LIMIT 5 OFFSET 10`?",
              "prompt": "In SQL, what is the effect of writing `LIMIT 5 OFFSET 10`?",
              "options": [
                "Returns 10 rows starting from row 5",
                "Returns 5 rows after skipping the first 10 rows",
                "Returns rows between 5 and 10",
                "Limits the query duration to 5 seconds with 10 retries"
              ],
              "correctAnswer": 1,
              "correctIndex": 1,
              "correctOptionIndex": 1,
              "explanation": "OFFSET skips the specified number of rows, and LIMIT restricts the count returned."
            },
            {
              "questionId": "m1-msa-q7",
              "question": "What does the query `SELECT DISTINCT department, role FROM employees;` return?",
              "prompt": "What does the query `SELECT DISTINCT department, role FROM employees;` return?",
              "options": [
                "Unique departments only, with any arbitrary role for each",
                "Unique combinations of department and role",
                "A list of distinct departments followed by a separate list of distinct roles",
                "Only rows where the department and role values are identical"
              ],
              "correctAnswer": 1,
              "correctIndex": 1,
              "correctOptionIndex": 1,
              "explanation": "DISTINCT applies to the entire combination of selected columns in the row, discarding only rows where all specified columns match another row."
            },
            {
              "questionId": "m1-msa-q8",
              "question": "Which operator matches any sequence of zero or more characters in standard SQL `LIKE` pattern matching?",
              "prompt": "Which operator matches any sequence of zero or more characters in standard SQL `LIKE` pattern matching?",
              "options": [
                "? (Question mark)",
                "* (Asterisk)",
                "% (Percent sign)",
                "_ (Underscore)"
              ],
              "correctAnswer": 2,
              "correctIndex": 2,
              "correctOptionIndex": 2,
              "explanation": "In SQL LIKE clauses, '%' matches zero or more characters, while '_' matches exactly one character."
            },
            {
              "questionId": "m1-msa-q9",
              "question": "How does SQL evaluate the condition `WHERE bonus IS NULL` compared to `WHERE bonus = NULL`?",
              "prompt": "How does SQL evaluate the condition `WHERE bonus IS NULL` compared to `WHERE bonus = NULL`?",
              "options": [
                "`WHERE bonus IS NULL` is valid; `WHERE bonus = NULL` evaluates to UNKNOWN and returns 0 rows.",
                "Both expressions are completely identical and interchangeable in standard SQL.",
                "`WHERE bonus = NULL` returns all rows where bonus is 0 or empty string.",
                "`WHERE bonus IS NULL` throws a compilation error in SQLite and PostgreSQL."
              ],
              "correctAnswer": 0,
              "correctIndex": 0,
              "correctOptionIndex": 0,
              "explanation": "NULL represents an unknown value. Comparisons using '=' against NULL always yield UNKNOWN (falsy in WHERE filters). The 'IS NULL' operator is required."
            },
            {
              "questionId": "m1-msa-q10",
              "question": "Given `SELECT title, release_year FROM movies ORDER BY release_year DESC, title ASC;`, how are rows with the same release_year sorted?",
              "prompt": "Given `SELECT title, release_year FROM movies ORDER BY release_year DESC, title ASC;`, how are rows with the same release_year sorted?",
              "options": [
                "In descending order by title",
                "In ascending alphabetical order by title",
                "In arbitrary, unpredictable physical storage order",
                "An error is thrown because two conflicting sort directions cannot be mixed"
              ],
              "correctAnswer": 1,
              "correctIndex": 1,
              "correctOptionIndex": 1,
              "explanation": "Secondary sort criteria apply when preceding sort columns have identical values. Here, ties in release_year are broken by title in ascending alphabetical order."
            },
            {
              "questionId": "m1-msa-q11",
              "question": "Which SQL clause evaluates multiple possible discrete values as a shorthand for multiple `OR` conditions?",
              "prompt": "Which SQL clause evaluates multiple possible discrete values as a shorthand for multiple `OR` conditions?",
              "options": [
                "BETWEEN",
                "IN",
                "LIKE",
                "EXISTS"
              ],
              "correctAnswer": 1,
              "correctIndex": 1,
              "correctOptionIndex": 1,
              "explanation": "The `IN (val1, val2, ...)` operator checks if a column matches any value in a designated list, equivalent to `col = val1 OR col = val2`."
            },
            {
              "questionId": "m1-msa-q12",
              "question": "In the query `SELECT name, salary FROM employees WHERE salary BETWEEN 40000 AND 60000;`, are the boundary values 40000 and 60000 included in the output?",
              "prompt": "In the query `SELECT name, salary FROM employees WHERE salary BETWEEN 40000 AND 60000;`, are the boundary values 40000 and 60000 included in the output?",
              "options": [
                "Yes, `BETWEEN ... AND ...` is inclusive of both endpoints.",
                "No, only values strictly greater than 40000 and strictly less than 60000 are returned.",
                "Only the lower boundary (40000) is included.",
                "Only the upper boundary (60000) is included."
              ],
              "correctAnswer": 0,
              "correctIndex": 0,
              "correctOptionIndex": 0,
              "explanation": "In SQL, `BETWEEN low AND high` is equivalent to `>= low AND <= high` and is inclusive of both boundary values."
            }
          ]
        }
      }
    },
    {
      "moduleId": "sql-m2",
      "title": "Module 2: Multi-Table Queries & Relational JOINs",
      "description": "Connect relational tables with INNER JOIN, LEFT/RIGHT OUTER JOINs, and handle missing values with IS NULL / IS NOT NULL.",
      "estimatedMinutes": 80,
      "topics": [
        {
          "topicId": "sql-m2-t1",
          "title": "SQL Lesson 6: Multi-table queries with JOINs",
          "description": "Hands-on SQL exercise: SQL Lesson 6: Multi-table queries with JOINs. Learn syntax, inspect live SQLite tables, and validate queries in real-time.",
          "mode": "SQL_INTERACTIVE",
          "readingTimeMinutes": 18,
          "lessonMarkdown": "Up to now, we've been working with a single table, but entity data in the real world is often broken\ndown into pieces and stored across multiple orthogonal tables using a process known as *normalization*[1]. \n\n### Database normalization\n\nDatabase normalization is useful because it minimizes duplicate data in any single table, and allows \nfor data in the database to grow independently of each other (ie. Types of car engines can grow \nindependent of each type of car).  As a trade-off, queries get slightly more complex since they have\nto be able to find data from different parts of the database, and performance issues can arise when \nworking with many large tables.\nIn order to answer questions about an entity that has data spanning multiple tables in a normalized \ndatabase, we need to learn how to write a query that can combine all that data and pull out exactly \nthe information we need.\n\n### Multi-table queries with JOINs\n\nTables that share information about a single entity need to have a *primary key* that identifies \nthat entity *uniquely* across the database.  One common primary key type is an auto-incrementing \ninteger (because they are space efficient), but it can also be a string, hashed value, so long as it\nis unique.\nUsing the `JOIN` clause in a query, we can combine row data across two separate tables using this \nunique key.  The first of the joins that we will introduce is the `INNER JOIN`.\n\n> **Select query with INNER JOIN on multiple tables**\n```sql\nSELECT column, another_table_column, ...\nFROM mytable\nINNER JOIN another_table \n    ON mytable.id = another_table.id\nWHERE *condition(s)*\nORDER BY column, ... ASC/DESC\nLIMIT num_limit OFFSET num_offset;\n```\n\nThe `INNER JOIN` is a process that matches rows from the first table and the second table which have \nthe same key (as defined by the `ON` constraint) to create a result row with the combined columns\nfrom both tables.  After the tables are joined, the other clauses we learned previously are then \napplied.\n\n    Did you know?\n    \n    You might see queries where the `INNER JOIN` is written simply as a `JOIN`.  \n    These two are equivalent, but we will continue to refer to these joins as inner-joins because they \n    make the query easier to read once you start using other types of joins, which will be introduced \n    in the following lesson.\n    \n\n### Exercise\n\nWe've added a new table to the Pixar database so that you can try practicing some joins. The **BoxOffice** \ntable stores information about the ratings and sales of each particular Pixar movie, and the *Movie_id* \ncolumn in that table corresponds with the *Id* column in the **Movies** table 1-to-1.  Try and solve\nthe tasks below using the `INNER JOIN` introduced above.",
          "lessonContent": {
            "textAndVisuals": {
              "content": "Up to now, we've been working with a single table, but entity data in the real world is often broken\ndown into pieces and stored across multiple orthogonal tables using a process known as *normalization*[1]. \n\n### Database normalization\n\nDatabase normalization is useful because it minimizes duplicate data in any single table, and allows \nfor data in the database to grow independently of each other (ie. Types of car engines can grow \nindependent of each type of car).  As a trade-off, queries get slightly more complex since they have\nto be able to find data from different parts of the database, and performance issues can arise when \nworking with many large tables.\nIn order to answer questions about an entity that has data spanning multiple tables in a normalized \ndatabase, we need to learn how to write a query that can combine all that data and pull out exactly \nthe information we need.\n\n### Multi-table queries with JOINs\n\nTables that share information about a single entity need to have a *primary key* that identifies \nthat entity *uniquely* across the database.  One common primary key type is an auto-incrementing \ninteger (because they are space efficient), but it can also be a string, hashed value, so long as it\nis unique.\nUsing the `JOIN` clause in a query, we can combine row data across two separate tables using this \nunique key.  The first of the joins that we will introduce is the `INNER JOIN`.\n\n> **Select query with INNER JOIN on multiple tables**\n```sql\nSELECT column, another_table_column, ...\nFROM mytable\nINNER JOIN another_table \n    ON mytable.id = another_table.id\nWHERE *condition(s)*\nORDER BY column, ... ASC/DESC\nLIMIT num_limit OFFSET num_offset;\n```\n\nThe `INNER JOIN` is a process that matches rows from the first table and the second table which have \nthe same key (as defined by the `ON` constraint) to create a result row with the combined columns\nfrom both tables.  After the tables are joined, the other clauses we learned previously are then \napplied.\n\n    Did you know?\n    \n    You might see queries where the `INNER JOIN` is written simply as a `JOIN`.  \n    These two are equivalent, but we will continue to refer to these joins as inner-joins because they \n    make the query easier to read once you start using other types of joins, which will be introduced \n    in the following lesson.\n    \n\n### Exercise\n\nWe've added a new table to the Pixar database so that you can try practicing some joins. The **BoxOffice** \ntable stores information about the ratings and sales of each particular Pixar movie, and the *Movie_id* \ncolumn in that table corresponds with the *Id* column in the **Movies** table 1-to-1.  Try and solve\nthe tasks below using the `INNER JOIN` introduced above."
            }
          },
          "sqlExercise": {
            "schemaSql": "CREATE TABLE movies (\n    id integer primary key autoincrement,\n    title text,\n    director text,\n    year integer, \n    length_minutes integer\n);\nCREATE TABLE boxoffice (\n    -- id integer primary key autoincrement,\n    movie_id integer,\n    rating real,\n    domestic_sales real,\n    international_sales real\n);",
            "seedSql": "INSERT INTO movies (id, title, director, year, length_minutes) VALUES\n(1, 'Toy Story', 'John Lasseter', 1995, 81),\n(2, 'A Bug''s Life', 'John Lasseter', 1998, 95),\n(3, 'Toy Story 2', 'John Lasseter', 1999, 93),\n(4, 'Monsters, Inc.', 'Pete Docter', 2001, 92),\n(5, 'Finding Nemo', 'Andrew Stanton', 2003, 107),\n(6, 'The Incredibles', 'Brad Bird', 2004, 116),\n(7, 'Cars', 'John Lasseter', 2006, 117),\n(8, 'Ratatouille', 'Brad Bird', 2007, 115),\n(9, 'WALL-E', 'Andrew Stanton', 2008, 104),\n(10, 'Up', 'Pete Docter', 2009, 101),\n(11, 'Toy Story 3', 'Lee Unkrich', 2010, 103),\n(12, 'Cars 2', 'John Lasseter', 2011, 120),\n(13, 'Brave', 'Brenda Chapman', 2012, 102),\n(14, 'Monsters University', 'Dan Scanlon', 2013, 110);\n\nINSERT INTO boxoffice (movie_id, rating, domestic_sales, international_sales) VALUES\n(5, 8.2, 380843261, 555900000),\n(14, 7.4, 268492764, 475066843),\n(8, 8, 206445654, 417277164),\n(12, 6.4, 191452396, 368400000),\n(3, 7.9, 245852179, 239163000),\n(6, 8, 261441092, 370001000),\n(9, 8.5, 223808164, 297503696),\n(11, 8.4, 415004880, 648167031),\n(1, 8.3, 191796233, 170162503),\n(7, 7.2, 244082982, 217900167),\n(10, 8.3, 293004164, 438338580),\n(4, 8.1, 289916256, 272900000),\n(2, 7.2, 162798565, 200600000),\n(13, 7.2, 237283207, 301700000);\n",
            "defaultTable": "movies",
            "defaultQuery": "-- Write your SQL statement below to solve the exercise tasks:\nSELECT * FROM movies LIMIT 10;",
            "tasks": [
              {
                "instruction": "Find the domestic and international sales for each movie",
                "expectedQuery": "SELECT title, domestic_sales, international_sales \nFROM movies\n  JOIN boxoffice\n    ON movies.id = boxoffice.movie_id;",
                "solution": "SELECT title, domestic_sales, international_sales \nFROM movies\n  JOIN boxoffice\n    ON movies.id = boxoffice.movie_id;",
                "orderSensitive": false
              },
              {
                "instruction": "Show the sales numbers for each movie that did better internationally rather than domestically",
                "expectedQuery": "SELECT title, domestic_sales, international_sales\nFROM movies\n  JOIN boxoffice\n    ON movies.id = boxoffice.movie_id\nWHERE international_sales > domestic_sales;",
                "solution": "SELECT title, domestic_sales, international_sales\nFROM movies\n  JOIN boxoffice\n    ON movies.id = boxoffice.movie_id\nWHERE international_sales > domestic_sales;",
                "orderSensitive": false
              },
              {
                "instruction": "List all the movies by their ratings in descending order",
                "expectedQuery": "SELECT title\nFROM movies\n  JOIN boxoffice\n    ON movies.id = boxoffice.movie_id\nORDER BY rating DESC;",
                "solution": "SELECT title\nFROM movies\n  JOIN boxoffice\n    ON movies.id = boxoffice.movie_id\nORDER BY rating DESC;",
                "orderSensitive": false
              }
            ]
          }
        },
        {
          "topicId": "sql-m2-t2",
          "title": "SQL Lesson 7: OUTER JOINs",
          "description": "Hands-on SQL exercise: SQL Lesson 7: OUTER JOINs. Learn syntax, inspect live SQLite tables, and validate queries in real-time.",
          "mode": "SQL_INTERACTIVE",
          "readingTimeMinutes": 18,
          "lessonMarkdown": "Depending on how you want to analyze the data, the `INNER JOIN` we used last lesson might not be \nsufficient because the resulting table only contains data that belongs in both of the tables.\nIf the two tables have asymmetric data, which can easily happen when data is entered in different\nstages, then we would have to use a `LEFT JOIN`, `RIGHT JOIN` or `FULL JOIN` instead to ensure that \nthe data you need is not left out of the results.\n\n> **Select query with LEFT/RIGHT/FULL JOINs on multiple tables**\n```sql\nSELECT column, another_column, ...\nFROM mytable\nINNER/LEFT/RIGHT/FULL JOIN another_table \n    ON mytable.id = another_table.matching_id\nWHERE *condition(s)*\nORDER BY column, ... ASC/DESC\nLIMIT num_limit OFFSET num_offset;\n```\n\nLike the `INNER JOIN` these three new joins have to specify which column to join the data on.\nWhen joining table A to table B, a `LEFT JOIN` simply includes rows from A regardless of whether \na matching row is found in B.  The `RIGHT JOIN` is the same, but reversed, keeping rows in B \nregardless of whether a match is found in A.  Finally, a `FULL JOIN` simply means that rows from \nboth tables are kept, regardless of whether a matching row exists in the other table.\nWhen using any of these new joins, you will likely have to write additional logic to deal with `NULL`s\nin the result and constraints (more on this in the next lesson).\n\n    Did you know?\n    \n    You might see queries with these joins written as `LEFT OUTER JOIN`, \n    `RIGHT OUTER JOIN`, or `FULL OUTER JOIN`, but the `OUTER` \n    keyword is really kept for SQL-92 compatibility and these queries are simply equivalent to \n    `LEFT JOIN`, `RIGHT JOIN`, and `FULL JOIN` respectively.\n    \n\n### Exercise\n\nIn this exercise, you are going to be working with a new table which stores fictional data about **Employees** \nin the film studio and their assigned office **Buildings**. Some of the buildings are new, so they \ndon't have any employees in them yet, but we need to find some information about them regardless.\nSince our browser SQL database is somewhat limited, only the `LEFT JOIN` is supported in the \nexercise below.",
          "lessonContent": {
            "textAndVisuals": {
              "content": "Depending on how you want to analyze the data, the `INNER JOIN` we used last lesson might not be \nsufficient because the resulting table only contains data that belongs in both of the tables.\nIf the two tables have asymmetric data, which can easily happen when data is entered in different\nstages, then we would have to use a `LEFT JOIN`, `RIGHT JOIN` or `FULL JOIN` instead to ensure that \nthe data you need is not left out of the results.\n\n> **Select query with LEFT/RIGHT/FULL JOINs on multiple tables**\n```sql\nSELECT column, another_column, ...\nFROM mytable\nINNER/LEFT/RIGHT/FULL JOIN another_table \n    ON mytable.id = another_table.matching_id\nWHERE *condition(s)*\nORDER BY column, ... ASC/DESC\nLIMIT num_limit OFFSET num_offset;\n```\n\nLike the `INNER JOIN` these three new joins have to specify which column to join the data on.\nWhen joining table A to table B, a `LEFT JOIN` simply includes rows from A regardless of whether \na matching row is found in B.  The `RIGHT JOIN` is the same, but reversed, keeping rows in B \nregardless of whether a match is found in A.  Finally, a `FULL JOIN` simply means that rows from \nboth tables are kept, regardless of whether a matching row exists in the other table.\nWhen using any of these new joins, you will likely have to write additional logic to deal with `NULL`s\nin the result and constraints (more on this in the next lesson).\n\n    Did you know?\n    \n    You might see queries with these joins written as `LEFT OUTER JOIN`, \n    `RIGHT OUTER JOIN`, or `FULL OUTER JOIN`, but the `OUTER` \n    keyword is really kept for SQL-92 compatibility and these queries are simply equivalent to \n    `LEFT JOIN`, `RIGHT JOIN`, and `FULL JOIN` respectively.\n    \n\n### Exercise\n\nIn this exercise, you are going to be working with a new table which stores fictional data about **Employees** \nin the film studio and their assigned office **Buildings**. Some of the buildings are new, so they \ndon't have any employees in them yet, but we need to find some information about them regardless.\nSince our browser SQL database is somewhat limited, only the `LEFT JOIN` is supported in the \nexercise below."
            }
          },
          "sqlExercise": {
            "schemaSql": "CREATE TABLE employees (\n    -- id integer primary key autoincrement,\n    role text,\n    name text,\n    building text,\n    years_employed integer\n);\nCREATE TABLE buildings (\n    building_name text,\n    capacity int\n);",
            "seedSql": "INSERT INTO employees (role, name, building, years_employed) VALUES\n('Engineer', 'Becky A.', '1e', 4),\n('Engineer', 'Dan B.', '1e', 2),\n('Engineer', 'Sharon F.', '1e', 6),\n('Engineer', 'Dan M.', '1e', 4),\n('Engineer', 'Malcom S.', '1e', 1),\n('Artist', 'Tylar S.', '2w', 2),\n('Artist', 'Sherman D.', '2w', 8),\n('Artist', 'Jakob J.', '2w', 6),\n('Artist', 'Lillia A.', '2w', 7),\n('Artist', 'Brandon J.', '2w', 7),\n('Manager', 'Scott K.', '1e', 9),\n('Manager', 'Shirlee M.', '1e', 3),\n('Manager', 'Daria O.', '2w', 6);\n\nINSERT INTO buildings (building_name, capacity) VALUES\n('1e', 24),\n('1w', 32),\n('2e', 16),\n('2w', 20);\n",
            "defaultTable": "employees",
            "defaultQuery": "-- Write your SQL statement below to solve the exercise tasks:\nSELECT * FROM employees LIMIT 10;",
            "tasks": [
              {
                "instruction": "Find the list of all buildings that have employees",
                "expectedQuery": "SELECT DISTINCT building FROM employees;",
                "solution": "SELECT DISTINCT building FROM employees;",
                "orderSensitive": false
              },
              {
                "instruction": "Find the list of all buildings and their capacity",
                "expectedQuery": "SELECT * FROM buildings;",
                "solution": "SELECT * FROM buildings;",
                "orderSensitive": false
              },
              {
                "instruction": "List all buildings and the distinct employee roles in each building (including empty buildings)",
                "expectedQuery": "SELECT DISTINCT building_name, role \nFROM buildings \n  LEFT JOIN employees\n    ON building_name = building;",
                "solution": "SELECT DISTINCT building_name, role \nFROM buildings \n  LEFT JOIN employees\n    ON building_name = building;",
                "orderSensitive": false
              }
            ]
          }
        },
        {
          "topicId": "sql-m2-t3",
          "title": "SQL Lesson 8: A short note on NULLs",
          "description": "Hands-on SQL exercise: SQL Lesson 8: A short note on NULLs. Learn syntax, inspect live SQLite tables, and validate queries in real-time.",
          "mode": "SQL_INTERACTIVE",
          "readingTimeMinutes": 15,
          "lessonMarkdown": "As promised in the last lesson, we are going to quickly talk about `NULL` values in an SQL database.\nIt's always good to reduce the possibility of `NULL` values in databases because they require special\nattention when constructing queries, constraints (certain functions behave differently with null values)\nand when processing the results.  \nAn alternative to `NULL` values in your database is to have *data-type appropriate default values*, like \n0 for numerical data, empty strings for text data, etc.  But if your database needs to store incomplete \ndata, then `NULL` values can be appropriate if the default values will skew later analysis (for example, \nwhen taking averages of numerical data).\nSometimes, it's also not possible to avoid `NULL` values, as we saw in the last lesson when outer-joining\ntwo tables with asymmetric data.  In these cases, you can test a column for `NULL` values in a `WHERE`\nclause by using either the `IS NULL` or `IS NOT NULL` constraint.\n\n> **Select query with constraints on NULL values**\n```sql\nSELECT column, another_column, ...\nFROM mytable\n**WHERE column IS/IS NOT NULL**\nAND/OR *another_condition*\nAND/OR ...;\n```\n\n### Exercise\n\nThis exercise will be a sort of review of the last few lessons.  We're using the same **Employees** and\n**Buildings** table from the last lesson, but we've hired a few more people, who haven't yet been assigned\na building.",
          "lessonContent": {
            "textAndVisuals": {
              "content": "As promised in the last lesson, we are going to quickly talk about `NULL` values in an SQL database.\nIt's always good to reduce the possibility of `NULL` values in databases because they require special\nattention when constructing queries, constraints (certain functions behave differently with null values)\nand when processing the results.  \nAn alternative to `NULL` values in your database is to have *data-type appropriate default values*, like \n0 for numerical data, empty strings for text data, etc.  But if your database needs to store incomplete \ndata, then `NULL` values can be appropriate if the default values will skew later analysis (for example, \nwhen taking averages of numerical data).\nSometimes, it's also not possible to avoid `NULL` values, as we saw in the last lesson when outer-joining\ntwo tables with asymmetric data.  In these cases, you can test a column for `NULL` values in a `WHERE`\nclause by using either the `IS NULL` or `IS NOT NULL` constraint.\n\n> **Select query with constraints on NULL values**\n```sql\nSELECT column, another_column, ...\nFROM mytable\n**WHERE column IS/IS NOT NULL**\nAND/OR *another_condition*\nAND/OR ...;\n```\n\n### Exercise\n\nThis exercise will be a sort of review of the last few lessons.  We're using the same **Employees** and\n**Buildings** table from the last lesson, but we've hired a few more people, who haven't yet been assigned\na building."
            }
          },
          "sqlExercise": {
            "schemaSql": "CREATE TABLE employees (\n    -- id integer primary key autoincrement,\n    role text,\n    name text,\n    building text,\n    years_employed integer\n);\nCREATE TABLE buildings (\n    building_name text,\n    capacity int\n);",
            "seedSql": "INSERT INTO employees (role, name, building, years_employed) VALUES\n('Engineer', 'Becky A.', '1e', 4),\n('Engineer', 'Dan B.', '1e', 2),\n('Engineer', 'Sharon F.', '1e', 6),\n('Engineer', 'Dan M.', '1e', 4),\n('Engineer', 'Malcom S.', '1e', 1),\n('Artist', 'Tylar S.', '2w', 2),\n('Artist', 'Sherman D.', '2w', 8),\n('Artist', 'Jakob J.', '2w', 6),\n('Artist', 'Lillia A.', '2w', 7),\n('Artist', 'Brandon J.', '2w', 7),\n('Manager', 'Scott K.', '1e', 9),\n('Manager', 'Shirlee M.', '1e', 3),\n('Manager', 'Daria O.', '2w', 6);\n\nINSERT INTO buildings (building_name, capacity) VALUES\n('1e', 24),\n('1w', 32),\n('2e', 16),\n('2w', 20);\n",
            "defaultTable": "employees",
            "defaultQuery": "-- Write your SQL statement below to solve the exercise tasks:\nSELECT * FROM employees LIMIT 10;",
            "tasks": [
              {
                "instruction": "Find the name and role of all employees who have not been assigned to a building",
                "expectedQuery": "SELECT name, role FROM employees\nWHERE building IS NULL;",
                "solution": "SELECT name, role FROM employees\nWHERE building IS NULL;",
                "orderSensitive": false
              },
              {
                "instruction": "Find the names of the buildings that hold no employees",
                "expectedQuery": "SELECT DISTINCT building_name\nFROM buildings \n  LEFT JOIN employees\n    ON building_name = building\nWHERE role IS NULL;",
                "solution": "SELECT DISTINCT building_name\nFROM buildings \n  LEFT JOIN employees\n    ON building_name = building\nWHERE role IS NULL;",
                "orderSensitive": false
              }
            ]
          }
        }
      ],
      "msa": {
        "title": "Module 2 Assessment: Relational JOINs & NULL Handling (SQLZoo)",
        "passPercent": 85,
        "durationMinutes": 25,
        "mcqSection": {
          "sectionTitle": "Section 1: SQLZoo JOINs & NULL Handling MCQs",
          "durationMinutes": 25,
          "passCutoffPercent": 85,
          "questions": [
            {
              "questionId": "join_quiz-q1",
              "question": "You want to find the stadium where player 'Dimitris Salpingidis' scored. Select the JOIN condition to use:",
              "prompt": "You want to find the stadium where player 'Dimitris Salpingidis' scored. Select the JOIN condition to use:",
              "options": [
                "eteam JOIN game ON ( id = team1 )",
                "eteam JOIN game ON ( id = team2 )",
                "eteam JOIN goal ON ( teamid = id )",
                "game JOIN goal ON ( id = matchid )"
              ],
              "correctAnswer": 3,
              "correctIndex": 3,
              "correctOptionIndex": 3,
              "explanation": "Correct SQL Syntax/Output: game JOIN goal ON ( id = matchid )"
            },
            {
              "questionId": "join_quiz-q2",
              "question": "You JOIN the tables goal and eteam in an SQL statement. Indicate the list of column names that may be used in the SELECT line:",
              "prompt": "You JOIN the tables goal and eteam in an SQL statement. Indicate the list of column names that may be used in the SELECT line:",
              "options": [
                "gtime , mdate , stadium , matchid",
                "mdate , stadium , id",
                "matchid , teamid , player , gtime , id , teamname , coach",
                "matchid , teamid , player , gtime , mdate , stadium , team1"
              ],
              "correctAnswer": 2,
              "correctIndex": 2,
              "correctOptionIndex": 2,
              "explanation": "Correct SQL Syntax/Output: matchid , teamid , player , gtime , id , teamname , coach"
            },
            {
              "questionId": "join_quiz-q3",
              "question": "Select the code which shows players, their team and the amount of goals they scored against Greece(GRE).",
              "prompt": "Select the code which shows players, their team and the amount of goals they scored against Greece(GRE).",
              "options": [
                "SELECT player , teamid , COUNT ( * ) FROM game JOIN goal ON matchid = id WHERE ( team1 = \"GRE\" OR team2 = \"GRE\" ) AND teamid != 'GRE' GROUP BY player , teamid",
                "SELECT player , teamid , COUNT ( * ) FROM game JOIN goal ON matchid = id WHERE ( team1 = \"GRE\" ) AND teamid != 'GRE' GROUP BY player , teamid",
                "SELECT player , teamid , COUNT ( * ) FROM game JOIN goal ON matchid = id WHERE ( team1 = \"POL\" OR team2 = \"POL\" ) AND teamid != 'POL' GROUP BY player , teamid",
                "SELECT player , teamid , COUNT ( * ) FROM game JOIN goal WITH matchid = id WHERE ( team1 = \"GRE\" OR team2 = \"GRE\" ) AND teamid != 'GRE' GROUP BY player , teamid"
              ],
              "correctAnswer": 0,
              "correctIndex": 0,
              "correctOptionIndex": 0,
              "explanation": "Correct SQL Syntax/Output: SELECT player , teamid , COUNT ( * ) FROM game JOIN goal ON matchid = id WHERE ( team1 = \"GRE\" OR team2 = \"GRE\" ) AND teamid != 'GRE' GROUP BY player , teamid"
            },
            {
              "questionId": "join_quiz-q5",
              "question": "Select the code which would show the player and their team for those who have scored against Poland(POL) in National Stadium, Warsaw.",
              "prompt": "Select the code which would show the player and their team for those who have scored against Poland(POL) in National Stadium, Warsaw.",
              "options": [
                "SELECT DISTINCT player , teamid FROM game JOIN goal ON matchid = id WHERE stadium = 'National Stadium, Warsaw' AND ( team1 = 'GER' OR team2 = 'GER' ) AND teamid != 'GER'",
                "SELECT DISTINCT player , teamid FROM game JOIN goal ON matchid = id WHERE stadium = 'National Stadium, Warsaw' AND ( team1 = 'POL' OR team2 = 'POL' ) AND teamid != 'POL'",
                "SELECT DISTINCT player , teamid FROM game JOIN goal ON matchid = id WHERE stadium = 'National Stadium, Warsaw' AND teamid != 'POL'",
                "SELECT DISTINCT player , teamid FROM game JOIN goal ON matchid = id WHERE stadium = 'Stadion Miejski (Wroclaw)' AND ( team1 = 'POL' OR team2 = 'POL' ) AND teamid != 'POL'"
              ],
              "correctAnswer": 1,
              "correctIndex": 1,
              "correctOptionIndex": 1,
              "explanation": "Correct SQL Syntax/Output: SELECT DISTINCT player , teamid FROM game JOIN goal ON matchid = id WHERE stadium = 'National Stadium, Warsaw' AND ( team1 = 'POL' OR team2 = 'POL' ) AND teamid != 'POL'"
            },
            {
              "questionId": "join_quiz-q6",
              "question": "Select the code which shows the player, their team and the time they scored, for players who have played in Stadion Miejski (Wroclaw) but not against Italy(ITA).",
              "prompt": "Select the code which shows the player, their team and the time they scored, for players who have played in Stadion Miejski (Wroclaw) but not against Italy(ITA).",
              "options": [
                "SELECT DISTINCT player , teamid , gtime FROM game JOIN goal ON matchid = id WHERE stadium = 'National Stadium, Warsaw' AND (( teamid = team2 AND team1 != 'ITA' ) OR ( teamid = team1 AND team2 != 'ITA' ))",
                "SELECT DISTINCT player , teamid , gtime FROM game JOIN goal ON matchid = id WHERE stadium = 'Stadion Miejski (Wroclaw)' AND (( teamid = team2 AND team1 != 'ESP' ) OR ( teamid = team1 AND team2 != 'ESP' ))",
                "SELECT DISTINCT player , teamid , gtime FROM game JOIN goal ON matchid = id WHERE stadium = 'Stadion Miejski (Wroclaw)' AND (( teamid = team2 AND team1 != 'ITA' ) OR ( teamid = team1 AND team2 != 'ITA' ))",
                "SELECT DISTINCT teamid , gtime FROM game JOIN goal ON matchid = id WHERE stadium = 'Stadion Miejski (Wroclaw)' AND (( teamid = team2 AND team1 != 'ITA' ) OR ( teamid = team1 AND team2 != 'ITA' ))"
              ],
              "correctAnswer": 2,
              "correctIndex": 2,
              "correctOptionIndex": 2,
              "explanation": "Correct SQL Syntax/Output: SELECT DISTINCT player , teamid , gtime FROM game JOIN goal ON matchid = id WHERE stadium = 'Stadion Miejski (Wroclaw)' AND (( teamid = team2 AND team1 != 'ITA' ) OR ( teamid = team1 AND team2 != 'ITA' ))"
            },
            {
              "questionId": "using_null_quiz-q1",
              "question": "Select the code which uses an outer join correctly.",
              "prompt": "Select the code which uses an outer join correctly.",
              "options": [
                "SELECT teacher . name , dept . name FROM teacher JOIN dept ON ( dept = id )",
                "SELECT teacher . name , dept . name FROM teacher , dept INNER JOIN ON ( teacher . dept = dept . id )",
                "SELECT teacher . name , dept . name FROM teacher , dept JOIN WHERE ( teacher . dept = dept . id )",
                "SELECT teacher . name , dept . name FROM teacher OUTER JOIN dept ON dept . id"
              ],
              "correctAnswer": 0,
              "correctIndex": 0,
              "correctOptionIndex": 0,
              "explanation": "Correct SQL Syntax/Output: SELECT teacher . name , dept . name FROM teacher JOIN dept ON ( dept = id )"
            },
            {
              "questionId": "using_null_quiz-q2",
              "question": "Select the correct statement that shows the name of department which employs Cutflower -",
              "prompt": "Select the correct statement that shows the name of department which employs Cutflower -",
              "options": [
                "SELECT dept . name FROM teacher JOIN dept ON ( dept . id = ( SELECT dept FROM teacher WHERE name = 'Cutflower' ))",
                "SELECT dept . name FROM teacher JOIN dept ON ( dept . id = teacher . dept ) WHERE dept . id = ( SELECT dept FROM teacher HAVING name = 'Cutflower' )",
                "SELECT dept . name FROM teacher JOIN dept ON ( dept . id = teacher . dept ) WHERE teacher . name = 'Cutflower'",
                "SELECT dept . name FROM teacher JOIN dept WHERE dept . id = ( SELECT dept FROM teacher WHERE name = 'Cutflower' )"
              ],
              "correctAnswer": 2,
              "correctIndex": 2,
              "correctOptionIndex": 2,
              "explanation": "Correct SQL Syntax/Output: SELECT dept . name FROM teacher JOIN dept ON ( dept . id = teacher . dept ) WHERE teacher . name = 'Cutflower'"
            },
            {
              "questionId": "using_null_quiz-q3",
              "question": "Select out of following the code which uses a JOIN to show a list of all the departments and number of employed teachers",
              "prompt": "Select out of following the code which uses a JOIN to show a list of all the departments and number of employed teachers",
              "options": [
                "SELECT dept . name , COUNT ( * ) FROM teacher LEFT JOIN dept ON dept . id = teacher . dept",
                "SELECT dept . name , COUNT ( teacher . name ) FROM teacher , dept JOIN ON dept . id = teacher . dept GROUP BY dept . name",
                "SELECT dept . name , COUNT ( teacher . name ) FROM teacher JOIN dept ON dept . id = teacher . dept GROUP BY dept . name",
                "SELECT dept . name , COUNT ( teacher . name ) FROM teacher LEFT OUTER JOIN dept ON dept . id = teacher . dept GROUP BY dept . name"
              ],
              "correctAnswer": 0,
              "correctIndex": 0,
              "correctOptionIndex": 0,
              "explanation": "Correct SQL Syntax/Output: SELECT dept . name , COUNT ( * ) FROM teacher LEFT JOIN dept ON dept . id = teacher . dept"
            },
            {
              "questionId": "using_null_quiz-q4",
              "question": "Using SELECT name, dept, COALESCE(dept, 0) AS result FROM teacher on teacher table will:",
              "prompt": "Using SELECT name, dept, COALESCE(dept, 0) AS result FROM teacher on teacher table will:",
              "options": [
                "display 0 in result column for all teachers",
                "display 0 in result column for all teachers without department",
                "do nothing - the statement is incorrect",
                "set dept value of all teachers to 0"
              ],
              "correctAnswer": 1,
              "correctIndex": 1,
              "correctOptionIndex": 1,
              "explanation": "Correct SQL Syntax/Output: display 0 in result column for all teachers without department"
            },
            {
              "questionId": "using_null_quiz-q5",
              "question": "Query: SELECT name , CASE WHEN phone = 2752 THEN 'two' WHEN phone = 2753 THEN 'three' WHEN phone = 2754 THEN 'four' END AS digit FROM teacher shows following 'digit':",
              "prompt": "Query: SELECT name , CASE WHEN phone = 2752 THEN 'two' WHEN phone = 2753 THEN 'three' WHEN phone = 2754 THEN 'four' END AS digit FROM teacher shows following 'digit':",
              "options": [
                "'four' for Throd",
                "NULL for all teachers",
                "NULL for Shrivell",
                "'two' for Cutflower"
              ],
              "correctAnswer": 0,
              "correctIndex": 0,
              "correctOptionIndex": 0,
              "explanation": "Correct SQL Syntax/Output: 'four' for Throd"
            },
            {
              "questionId": "m2-msa-q11",
              "question": "What is the primary difference between an `INNER JOIN` and a `LEFT OUTER JOIN`?",
              "prompt": "What is the primary difference between an `INNER JOIN` and a `LEFT OUTER JOIN`?",
              "options": [
                "INNER JOIN preserves rows from the left table with no match, while LEFT JOIN discards them.",
                "LEFT JOIN preserves all rows from the left table even if there is no matching row in the right table (filling right columns with NULL).",
                "INNER JOIN operates only on primary keys, while LEFT JOIN works on any column.",
                "LEFT JOIN requires both tables to have the exact same number of columns."
              ],
              "correctAnswer": 1,
              "correctIndex": 1,
              "correctOptionIndex": 1,
              "explanation": "LEFT OUTER JOIN returns all rows from the left table; where no match exists in the right table, columns from the right table are populated with NULLs. INNER JOIN drops non-matching rows."
            },
            {
              "questionId": "m2-msa-q12",
              "question": "What function can replace NULL with a fallback default value in standard SQL?",
              "prompt": "What function can replace NULL with a fallback default value in standard SQL?",
              "options": [
                "NVL_REPLACE()",
                "IFEMPTY()",
                "COALESCE()",
                "NULL_DEFAULT()"
              ],
              "correctAnswer": 2,
              "correctIndex": 2,
              "correctOptionIndex": 2,
              "explanation": "COALESCE(val1, val2, ...) evaluates its arguments in order and returns the first non-NULL value."
            },
            {
              "questionId": "m2-msa-q13",
              "question": "If table A has 5 rows and table B has 4 rows, how many rows are produced by a `CROSS JOIN` (cartesian product) between A and B?",
              "prompt": "If table A has 5 rows and table B has 4 rows, how many rows are produced by a `CROSS JOIN` (cartesian product) between A and B?",
              "options": [
                "9 rows (5 + 4)",
                "20 rows (5 * 4)",
                "5 rows (maximum of 5 and 4)",
                "1 row (intersection)"
              ],
              "correctAnswer": 1,
              "correctIndex": 1,
              "correctOptionIndex": 1,
              "explanation": "A CROSS JOIN pairs each row of the first table with every row of the second table, resulting in 5 * 4 = 20 total rows."
            },
            {
              "questionId": "m2-msa-q14",
              "question": "What happens when a filtering condition for the right table is placed in the `WHERE` clause instead of the `ON` clause in a `LEFT JOIN`?",
              "prompt": "What happens when a filtering condition for the right table is placed in the `WHERE` clause instead of the `ON` clause in a `LEFT JOIN`?",
              "options": [
                "It makes no difference; SQL query optimizers always treat ON and WHERE identically.",
                "It effectively converts the `LEFT JOIN` into an `INNER JOIN` for rows where the right column is tested (since NULLs fail the WHERE check).",
                "The query fails with a syntax error.",
                "The left table rows are duplicated for each condition."
              ],
              "correctAnswer": 1,
              "correctIndex": 1,
              "correctOptionIndex": 1,
              "explanation": "Placing conditions on the right table in the WHERE clause filters rows AFTER the join. Since unmatched rows have NULL for right columns, conditions like `WHERE right_table.status = 'active'` filter out the non-matching left rows, turning it into an inner join."
            },
            {
              "questionId": "m2-msa-q15",
              "question": "What is a 'Self-Join' in SQL?",
              "prompt": "What is a 'Self-Join' in SQL?",
              "options": [
                "A query that joins a table to itself using table aliases to establish hierarchical or comparative relationships",
                "A join that creates an identical copy of a table in memory",
                "A join that automatically links tables sharing the same name in different schemas",
                "A join performed automatically by the database index engine"
              ],
              "correctAnswer": 0,
              "correctIndex": 0,
              "correctOptionIndex": 0,
              "explanation": "A self-join joins a table to itself (e.g. `employees e JOIN employees m ON e.manager_id = m.id`) by giving the table two distinct aliases."
            },
            {
              "questionId": "m2-msa-q16",
              "question": "How can you identify customers who have never placed an order using a `LEFT JOIN`?",
              "prompt": "How can you identify customers who have never placed an order using a `LEFT JOIN`?",
              "options": [
                "JOIN customers ON orders WHERE count = 0",
                "SELECT c.* FROM customers c LEFT JOIN orders o ON c.id = o.customer_id WHERE o.id IS NULL;",
                "SELECT c.* FROM customers c INNER JOIN orders o ON c.id = o.customer_id WHERE o.total = 0;",
                "SELECT * FROM customers WHERE orders = NULL;"
              ],
              "correctAnswer": 1,
              "correctIndex": 1,
              "correctOptionIndex": 1,
              "explanation": "A LEFT JOIN with `WHERE right_table.id IS NULL` is an anti-join pattern that identifies records from the left table that have no corresponding record in the right table."
            }
          ]
        }
      }
    },
    {
      "moduleId": "sql-m3",
      "title": "Module 3: Aggregates, Expressions & Execution Order",
      "description": "Compute statistical aggregates (COUNT, SUM, AVG, MIN, MAX), group records with GROUP BY and HAVING, and master the SQL query execution lifecycle.",
      "estimatedMinutes": 90,
      "topics": [
        {
          "topicId": "sql-m3-t1",
          "title": "SQL Lesson 9: Queries with expressions",
          "description": "Hands-on SQL exercise: SQL Lesson 9: Queries with expressions. Learn syntax, inspect live SQLite tables, and validate queries in real-time.",
          "mode": "SQL_INTERACTIVE",
          "readingTimeMinutes": 15,
          "lessonMarkdown": "In addition to querying and referencing raw column data with SQL, you can also use *expressions* \nto write more complex logic on column values in a query.  These expressions can use mathematical \nand string functions along with basic arithmetic to transform values when the query is executed, \nas shown in this physics example.\n\n> **Example query with expressions**\n```sql\nSELECT **particle_speed / 2.0** AS half_particle_speed\nFROM physics_data\nWHERE **ABS(particle_position) * 10.0 > 500**;\n```\n\nEach database has its own supported set of mathematical, string, and date functions that can be used\nin a query, which you can find in their own respective docs.\nThe use of expressions can save time and extra post-processing of the result data, but can also make \nthe query harder to read, so we recommend that when expressions are used in the `SELECT` part of the \nquery, that they are also given a descriptive *alias* using the `AS` keyword.\n\n> **Select query with expression aliases**\n```sql\nSELECT ***col_expression* AS *expr_description***, ...\nFROM mytable;\n```\n\nIn addition to expressions, regular columns and even tables can also have aliases to make them easier\nto reference in the output and as a part of simplifying more complex queries.\n\n> **Example query with both column and table name aliases**\n```sql\nSELECT column **AS better_column_name**, ...\nFROM a_long_widgets_table_name **AS mywidgets**\nINNER JOIN widget_sales\n  ON mywidgets.id = widget_sales.widget_id;\n```\n\n### Exercise\n\nYou are going to have to use expressions to transform the **BoxOffice** data into something easier to \nunderstand for the tasks below.",
          "lessonContent": {
            "textAndVisuals": {
              "content": "In addition to querying and referencing raw column data with SQL, you can also use *expressions* \nto write more complex logic on column values in a query.  These expressions can use mathematical \nand string functions along with basic arithmetic to transform values when the query is executed, \nas shown in this physics example.\n\n> **Example query with expressions**\n```sql\nSELECT **particle_speed / 2.0** AS half_particle_speed\nFROM physics_data\nWHERE **ABS(particle_position) * 10.0 > 500**;\n```\n\nEach database has its own supported set of mathematical, string, and date functions that can be used\nin a query, which you can find in their own respective docs.\nThe use of expressions can save time and extra post-processing of the result data, but can also make \nthe query harder to read, so we recommend that when expressions are used in the `SELECT` part of the \nquery, that they are also given a descriptive *alias* using the `AS` keyword.\n\n> **Select query with expression aliases**\n```sql\nSELECT ***col_expression* AS *expr_description***, ...\nFROM mytable;\n```\n\nIn addition to expressions, regular columns and even tables can also have aliases to make them easier\nto reference in the output and as a part of simplifying more complex queries.\n\n> **Example query with both column and table name aliases**\n```sql\nSELECT column **AS better_column_name**, ...\nFROM a_long_widgets_table_name **AS mywidgets**\nINNER JOIN widget_sales\n  ON mywidgets.id = widget_sales.widget_id;\n```\n\n### Exercise\n\nYou are going to have to use expressions to transform the **BoxOffice** data into something easier to \nunderstand for the tasks below."
            }
          },
          "sqlExercise": {
            "schemaSql": "CREATE TABLE movies (\n    id integer primary key autoincrement,\n    title text,\n    director text,\n    year integer, \n    length_minutes integer\n);\nCREATE TABLE boxoffice (\n    -- id integer primary key autoincrement,\n    movie_id integer,\n    rating real,\n    domestic_sales real,\n    international_sales real\n);",
            "seedSql": "INSERT INTO movies (id, title, director, year, length_minutes) VALUES\n(1, 'Toy Story', 'John Lasseter', 1995, 81),\n(2, 'A Bug''s Life', 'John Lasseter', 1998, 95),\n(3, 'Toy Story 2', 'John Lasseter', 1999, 93),\n(4, 'Monsters, Inc.', 'Pete Docter', 2001, 92),\n(5, 'Finding Nemo', 'Andrew Stanton', 2003, 107),\n(6, 'The Incredibles', 'Brad Bird', 2004, 116),\n(7, 'Cars', 'John Lasseter', 2006, 117),\n(8, 'Ratatouille', 'Brad Bird', 2007, 115),\n(9, 'WALL-E', 'Andrew Stanton', 2008, 104),\n(10, 'Up', 'Pete Docter', 2009, 101),\n(11, 'Toy Story 3', 'Lee Unkrich', 2010, 103),\n(12, 'Cars 2', 'John Lasseter', 2011, 120),\n(13, 'Brave', 'Brenda Chapman', 2012, 102),\n(14, 'Monsters University', 'Dan Scanlon', 2013, 110);\n\nINSERT INTO boxoffice (movie_id, rating, domestic_sales, international_sales) VALUES\n(5, 8.2, 380843261, 555900000),\n(14, 7.4, 268492764, 475066843),\n(8, 8, 206445654, 417277164),\n(12, 6.4, 191452396, 368400000),\n(3, 7.9, 245852179, 239163000),\n(6, 8, 261441092, 370001000),\n(9, 8.5, 223808164, 297503696),\n(11, 8.4, 415004880, 648167031),\n(1, 8.3, 191796233, 170162503),\n(7, 7.2, 244082982, 217900167),\n(10, 8.3, 293004164, 438338580),\n(4, 8.1, 289916256, 272900000),\n(2, 7.2, 162798565, 200600000),\n(13, 7.2, 237283207, 301700000);\n",
            "defaultTable": "movies",
            "defaultQuery": "-- Write your SQL statement below to solve the exercise tasks:\nSELECT * FROM movies LIMIT 10;",
            "tasks": [
              {
                "instruction": "List all movies and their combined sales in millions of dollars",
                "expectedQuery": "SELECT title, (domestic_sales + international_sales) / 1000000 AS gross_sales_millions\nFROM movies\n  JOIN boxoffice\n    ON movies.id = boxoffice.movie_id;",
                "solution": "SELECT title, (domestic_sales + international_sales) / 1000000 AS gross_sales_millions\nFROM movies\n  JOIN boxoffice\n    ON movies.id = boxoffice.movie_id;",
                "orderSensitive": false
              },
              {
                "instruction": "List all movies and their ratings in percent",
                "expectedQuery": "SELECT title, rating * 10 AS rating_percent\nFROM movies\n  JOIN boxoffice\n    ON movies.id = boxoffice.movie_id;",
                "solution": "SELECT title, rating * 10 AS rating_percent\nFROM movies\n  JOIN boxoffice\n    ON movies.id = boxoffice.movie_id;",
                "orderSensitive": false
              },
              {
                "instruction": "List all movies that were released on even number years",
                "expectedQuery": "SELECT title\nFROM movies\nWHERE year % 2 = 0;",
                "solution": "SELECT title\nFROM movies\nWHERE year % 2 = 0;",
                "orderSensitive": false
              }
            ]
          }
        },
        {
          "topicId": "sql-m3-t2",
          "title": "SQL Lesson 10: Queries with aggregates (Pt. 1)",
          "description": "Hands-on SQL exercise: SQL Lesson 10: Queries with aggregates (Pt. 1). Learn syntax, inspect live SQLite tables, and validate queries in real-time.",
          "mode": "SQL_INTERACTIVE",
          "readingTimeMinutes": 18,
          "lessonMarkdown": "In addition to the simple expressions that we introduced last lesson, SQL also supports the use of\naggregate expressions (or functions) that allow you to summarize information about a group of rows\nof data.  With the Pixar database that you've been using, aggregate functions can be used to answer \nquestions like, \"How many movies has Pixar produced?\", or \"What is the highest grossing Pixar film \neach year?\". \n\n> **Select query with aggregate functions over all rows**\n```sql\n**SELECT AGG_FUNC(*column_or_expression*) AS aggregate_description**, ...\nFROM mytable\nWHERE *constraint_expression*;\n```\n\nWithout a specified grouping, each aggregate function is going to run on the whole set of result \nrows and return a single value.  And like normal expressions, giving your aggregate functions an alias\nensures that the results will be easier to read and process.\n\n### Common aggregate functions\n\nHere are some common aggregate functions that we are going to use in our examples:\n\n    \n        \n            Function\n            Description\n        \n        \n            **COUNT(*****)**, **COUNT(**column**)**\n            A common function used to counts the number of rows in the group if no column name is specified. \n                Otherwise, count the number of rows in the group with non-NULL values in the specified column.\n        \n        \n            **MIN(**column**)**\n            Finds the smallest numerical value in the specified column for all rows in the group.\n        \n        \n            **MAX(**column**)**\n            Finds the largest numerical value in the specified column for all rows in the group.\n        \n        \n            **AVG(**column)\n            Finds the average numerical value in the specified column for all rows in the group.\n        \n        \n            **SUM(**column**)**\n            Finds the sum of all numerical values in the specified column for the rows in the group.\n        \n        \n            Docs:\n                MySQL,\n                Postgres,\n                SQLite,\n                Microsoft SQL Server\n            \n        \n    \n\n### Grouped aggregate functions\n\nIn addition to aggregating across all the rows, you can instead apply the aggregate functions to \nindividual groups of data within that group (ie. box office sales for Comedies vs Action movies).\nThis would then create as many results as there are unique groups defined as by the `GROUP BY` clause.\n\n> **Select query with aggregate functions over groups**\n```sql\nSELECT AGG_FUNC(*column_or_expression*) AS aggregate_description, ...\nFROM mytable\nWHERE *constraint_expression*\n**GROUP BY column**;\n```\n\nThe `GROUP BY` clause works by grouping rows that have the same value in the column specified.  \n\n    Did you know?\n    \n    If you aren't using aggregate functions with your `GROUP BY` clause, then the resulting\n    behavior will be similar to using the `DISTINCT` keyword.\n    \n\n-->\n\n### Exercise\n\nFor this exercise, we are going to work with our **Employees** table.  Notice how the rows in this \ntable have shared data, which will give us an opportunity to use aggregate functions to summarize \nsome high-level metrics about the teams.  Go ahead and give it a shot.",
          "lessonContent": {
            "textAndVisuals": {
              "content": "In addition to the simple expressions that we introduced last lesson, SQL also supports the use of\naggregate expressions (or functions) that allow you to summarize information about a group of rows\nof data.  With the Pixar database that you've been using, aggregate functions can be used to answer \nquestions like, \"How many movies has Pixar produced?\", or \"What is the highest grossing Pixar film \neach year?\". \n\n> **Select query with aggregate functions over all rows**\n```sql\n**SELECT AGG_FUNC(*column_or_expression*) AS aggregate_description**, ...\nFROM mytable\nWHERE *constraint_expression*;\n```\n\nWithout a specified grouping, each aggregate function is going to run on the whole set of result \nrows and return a single value.  And like normal expressions, giving your aggregate functions an alias\nensures that the results will be easier to read and process.\n\n### Common aggregate functions\n\nHere are some common aggregate functions that we are going to use in our examples:\n\n    \n        \n            Function\n            Description\n        \n        \n            **COUNT(*****)**, **COUNT(**column**)**\n            A common function used to counts the number of rows in the group if no column name is specified. \n                Otherwise, count the number of rows in the group with non-NULL values in the specified column.\n        \n        \n            **MIN(**column**)**\n            Finds the smallest numerical value in the specified column for all rows in the group.\n        \n        \n            **MAX(**column**)**\n            Finds the largest numerical value in the specified column for all rows in the group.\n        \n        \n            **AVG(**column)\n            Finds the average numerical value in the specified column for all rows in the group.\n        \n        \n            **SUM(**column**)**\n            Finds the sum of all numerical values in the specified column for the rows in the group.\n        \n        \n            Docs:\n                MySQL,\n                Postgres,\n                SQLite,\n                Microsoft SQL Server\n            \n        \n    \n\n### Grouped aggregate functions\n\nIn addition to aggregating across all the rows, you can instead apply the aggregate functions to \nindividual groups of data within that group (ie. box office sales for Comedies vs Action movies).\nThis would then create as many results as there are unique groups defined as by the `GROUP BY` clause.\n\n> **Select query with aggregate functions over groups**\n```sql\nSELECT AGG_FUNC(*column_or_expression*) AS aggregate_description, ...\nFROM mytable\nWHERE *constraint_expression*\n**GROUP BY column**;\n```\n\nThe `GROUP BY` clause works by grouping rows that have the same value in the column specified.  \n\n    Did you know?\n    \n    If you aren't using aggregate functions with your `GROUP BY` clause, then the resulting\n    behavior will be similar to using the `DISTINCT` keyword.\n    \n\n-->\n\n### Exercise\n\nFor this exercise, we are going to work with our **Employees** table.  Notice how the rows in this \ntable have shared data, which will give us an opportunity to use aggregate functions to summarize \nsome high-level metrics about the teams.  Go ahead and give it a shot."
            }
          },
          "sqlExercise": {
            "schemaSql": "CREATE TABLE employees (\n    -- id integer primary key autoincrement,\n    role text,\n    name text,\n    building text,\n    years_employed integer\n);",
            "seedSql": "INSERT INTO employees (role, name, building, years_employed) VALUES\n('Engineer', 'Becky A.', '1e', 4),\n('Engineer', 'Dan B.', '1e', 2),\n('Engineer', 'Sharon F.', '1e', 6),\n('Engineer', 'Dan M.', '1e', 4),\n('Engineer', 'Malcom S.', '1e', 1),\n('Artist', 'Tylar S.', '2w', 2),\n('Artist', 'Sherman D.', '2w', 8),\n('Artist', 'Jakob J.', '2w', 6),\n('Artist', 'Lillia A.', '2w', 7),\n('Artist', 'Brandon J.', '2w', 7),\n('Manager', 'Scott K.', '1e', 9),\n('Manager', 'Shirlee M.', '1e', 3),\n('Manager', 'Daria O.', '2w', 6);\n",
            "defaultTable": "employees",
            "defaultQuery": "-- Write your SQL statement below to solve the exercise tasks:\nSELECT * FROM employees LIMIT 10;",
            "tasks": [
              {
                "instruction": "Find the longest time that an employee has been at the studio",
                "expectedQuery": "SELECT MAX(years_employed) as Max_years_employed\nFROM employees;",
                "solution": "SELECT MAX(years_employed) as Max_years_employed\nFROM employees;",
                "orderSensitive": false
              },
              {
                "instruction": "For each role, find the average number of years employed by employees in that role",
                "expectedQuery": "SELECT role, AVG(years_employed) as Average_years_employed\nFROM employees\nGROUP BY role;",
                "solution": "SELECT role, AVG(years_employed) as Average_years_employed\nFROM employees\nGROUP BY role;",
                "orderSensitive": false
              },
              {
                "instruction": "Find the total number of employee years worked in each building",
                "expectedQuery": "SELECT building, SUM(years_employed) as Total_years_employed\nFROM employees\nGROUP BY building;",
                "solution": "SELECT building, SUM(years_employed) as Total_years_employed\nFROM employees\nGROUP BY building;",
                "orderSensitive": false
              }
            ]
          }
        },
        {
          "topicId": "sql-m3-t3",
          "title": "SQL Lesson 11: Queries with aggregates (Pt. 2)",
          "description": "Hands-on SQL exercise: SQL Lesson 11: Queries with aggregates (Pt. 2). Learn syntax, inspect live SQLite tables, and validate queries in real-time.",
          "mode": "SQL_INTERACTIVE",
          "readingTimeMinutes": 18,
          "lessonMarkdown": "Our queries are getting fairly complex, but we have nearly introduced all the important parts of a \n`SELECT` query.  One thing that you might have noticed is that if the `GROUP BY` clause is executed\nafter the `WHERE` clause (which filters the rows which are to be grouped), then how exactly do we \nfilter the grouped rows?  \nLuckily, SQL allows us to do this by adding an additional `HAVING` clause which is used specifically \nwith the `GROUP BY` clause to allow us to filter grouped rows from the result set.\n\n> **Select query with HAVING constraint**\n```sql\nSELECT group_by_column, AGG_FUNC(*column_expression*) AS aggregate_result_alias, ...\nFROM mytable\nWHERE *condition*\nGROUP BY column\n**HAVING *group_condition***;\n```\n\nThe `HAVING` clause constraints are written the same way as the `WHERE` clause constraints, and are \napplied to the grouped rows.  With our examples, this might not seem like a particularly useful\nconstruct, but if you imagine data with millions of rows with different properties, being able to apply\nadditional constraints is often necessary to quickly make sense of the data.\n\n    Did you know?\n    \n    If you aren't using the `GROUP BY` clause, a simple `WHERE` clause will suffice.\n    \n\n### Exercise\n\nFor this exercise, you are going to dive deeper into **Employee** data at the film studio.  Think about\nthe different clauses you want to apply for each task.",
          "lessonContent": {
            "textAndVisuals": {
              "content": "Our queries are getting fairly complex, but we have nearly introduced all the important parts of a \n`SELECT` query.  One thing that you might have noticed is that if the `GROUP BY` clause is executed\nafter the `WHERE` clause (which filters the rows which are to be grouped), then how exactly do we \nfilter the grouped rows?  \nLuckily, SQL allows us to do this by adding an additional `HAVING` clause which is used specifically \nwith the `GROUP BY` clause to allow us to filter grouped rows from the result set.\n\n> **Select query with HAVING constraint**\n```sql\nSELECT group_by_column, AGG_FUNC(*column_expression*) AS aggregate_result_alias, ...\nFROM mytable\nWHERE *condition*\nGROUP BY column\n**HAVING *group_condition***;\n```\n\nThe `HAVING` clause constraints are written the same way as the `WHERE` clause constraints, and are \napplied to the grouped rows.  With our examples, this might not seem like a particularly useful\nconstruct, but if you imagine data with millions of rows with different properties, being able to apply\nadditional constraints is often necessary to quickly make sense of the data.\n\n    Did you know?\n    \n    If you aren't using the `GROUP BY` clause, a simple `WHERE` clause will suffice.\n    \n\n### Exercise\n\nFor this exercise, you are going to dive deeper into **Employee** data at the film studio.  Think about\nthe different clauses you want to apply for each task."
            }
          },
          "sqlExercise": {
            "schemaSql": "CREATE TABLE employees (\n    -- id integer primary key autoincrement,\n    role text,\n    name text,\n    building text,\n    years_employed integer\n);",
            "seedSql": "INSERT INTO employees (role, name, building, years_employed) VALUES\n('Engineer', 'Becky A.', '1e', 4),\n('Engineer', 'Dan B.', '1e', 2),\n('Engineer', 'Sharon F.', '1e', 6),\n('Engineer', 'Dan M.', '1e', 4),\n('Engineer', 'Malcom S.', '1e', 1),\n('Artist', 'Tylar S.', '2w', 2),\n('Artist', 'Sherman D.', '2w', 8),\n('Artist', 'Jakob J.', '2w', 6),\n('Artist', 'Lillia A.', '2w', 7),\n('Artist', 'Brandon J.', '2w', 7),\n('Manager', 'Scott K.', '1e', 9),\n('Manager', 'Shirlee M.', '1e', 3),\n('Manager', 'Daria O.', '2w', 6);\n",
            "defaultTable": "employees",
            "defaultQuery": "-- Write your SQL statement below to solve the exercise tasks:\nSELECT * FROM employees LIMIT 10;",
            "tasks": [
              {
                "instruction": "Find the number of Artists in the studio (without a HAVING clause)",
                "expectedQuery": "SELECT COUNT(*) as Number_of_artists\nFROM employees\nWHERE role = \"Artist\";",
                "solution": "SELECT COUNT(*) as Number_of_artists\nFROM employees\nWHERE role = \"Artist\";",
                "orderSensitive": false
              },
              {
                "instruction": "Find the number of Employees of each role in the studio",
                "expectedQuery": "SELECT role, COUNT(*)\nFROM employees\nGROUP BY role;",
                "solution": "SELECT role, COUNT(*)\nFROM employees\nGROUP BY role;",
                "orderSensitive": false
              },
              {
                "instruction": "Find the total number of years employed by all Engineers",
                "expectedQuery": "SELECT SUM(years_employed)\nFROM employees\nGROUP BY role\nHAVING role = \"Engineer\";",
                "solution": "SELECT SUM(years_employed)\nFROM employees\nGROUP BY role\nHAVING role = \"Engineer\";",
                "orderSensitive": false
              }
            ]
          }
        },
        {
          "topicId": "sql-m3-t4",
          "title": "SQL Lesson 12: Order of execution of a Query",
          "description": "Hands-on SQL exercise: SQL Lesson 12: Order of execution of a Query. Learn syntax, inspect live SQLite tables, and validate queries in real-time.",
          "mode": "SQL_INTERACTIVE",
          "readingTimeMinutes": 15,
          "lessonMarkdown": "Now that we have an idea of all the parts of a query, we can now talk about how they all fit together \nin the context of a complete query.\n\n> **Complete SELECT query**\n```sql\nSELECT DISTINCT column, AGG_FUNC(*column_or_expression*), ...\nFROM mytable\n    JOIN another_table\n      ON mytable.column = another_table.column\n    WHERE *constraint_expression*\n    GROUP BY column\n    HAVING *constraint_expression*\n    ORDER BY *column* ASC/DESC\n    LIMIT *count* OFFSET *COUNT*;\n```\n\nEach query begins with finding the data that we need in a database, and then filtering that data down\ninto something that can be processed and understood as quickly as possible.  Because each part of the\nquery is executed sequentially, it's important to understand the order of execution so that you know\nwhat results are accessible where.\n\n### Query order of execution\n\n#### 1. `FROM` and `JOIN`s\n\nThe `FROM` clause, and subsequent `JOIN`s are first executed to determine the total working set of\ndata that is being queried.  This includes subqueries in this clause, and can cause temporary tables\nto be created under the hood containing all the columns and rows of the tables being joined.\n\n#### 2. `WHERE`\n\nOnce we have the total working set of data, the first-pass `WHERE` constraints are applied to the \nindividual rows, and rows that do not satisfy the constraint are discarded.  Each of the constraints\ncan only access columns directly from the tables requested in the `FROM` clause.  Aliases in the\n`SELECT` part of the query are not accessible in most databases since they may include \nexpressions dependent on parts of the query that have not yet executed.\n\n#### 3. `GROUP BY`\n\nThe remaining rows after the `WHERE` constraints are applied are then grouped based on common values\nin the column specified in the `GROUP BY` clause.  As a result of the grouping, there will only be as \nmany rows as there are unique values in that column.  Implicitly, this means that you should only need \nto use this when you have aggregate functions in your query.\n\n#### 4. `HAVING`\n\nIf the query has a `GROUP BY` clause, then the constraints in the `HAVING` clause are then applied\nto the grouped rows, discard the grouped rows that don't satisfy the constraint.  Like the `WHERE` \nclause, aliases are also not accessible from this step in most databases.\n\n#### 5. `SELECT`\n\nAny expressions in the `SELECT` part of the query are finally computed.\n\n#### 6. `DISTINCT`\n\nOf the remaining rows, rows with duplicate values in the column marked as `DISTINCT` will be discarded.\n\n#### 7. `ORDER BY`\n\nIf an order is specified by the `ORDER BY` clause, the rows are then sorted by the specified data in \neither ascending or descending order.  Since all the expressions in the `SELECT` part of the query \nhave been computed, you can reference aliases in this clause.\n\n#### 8. `LIMIT` / `OFFSET`\n\nFinally, the rows that fall outside the range specified by the `LIMIT` and `OFFSET` are discarded, \nleaving the final set of rows to be returned from the query.\n\n#### Conclusion\n\nNot every query needs to have all the parts we listed above, but a part of why SQL is so flexible is\nthat it allows developers and data analysts to quickly manipulate data without having to write additional\ncode, all just by using the above clauses.\n\n### Exercise\n\nHere ends our lessons on `SELECT` queries, congrats of making it this far!  This exercise will try\nand test your understanding of queries, so don't be discouraged if you find them challenging.  Just\ntry your best.",
          "lessonContent": {
            "textAndVisuals": {
              "content": "Now that we have an idea of all the parts of a query, we can now talk about how they all fit together \nin the context of a complete query.\n\n> **Complete SELECT query**\n```sql\nSELECT DISTINCT column, AGG_FUNC(*column_or_expression*), ...\nFROM mytable\n    JOIN another_table\n      ON mytable.column = another_table.column\n    WHERE *constraint_expression*\n    GROUP BY column\n    HAVING *constraint_expression*\n    ORDER BY *column* ASC/DESC\n    LIMIT *count* OFFSET *COUNT*;\n```\n\nEach query begins with finding the data that we need in a database, and then filtering that data down\ninto something that can be processed and understood as quickly as possible.  Because each part of the\nquery is executed sequentially, it's important to understand the order of execution so that you know\nwhat results are accessible where.\n\n### Query order of execution\n\n#### 1. `FROM` and `JOIN`s\n\nThe `FROM` clause, and subsequent `JOIN`s are first executed to determine the total working set of\ndata that is being queried.  This includes subqueries in this clause, and can cause temporary tables\nto be created under the hood containing all the columns and rows of the tables being joined.\n\n#### 2. `WHERE`\n\nOnce we have the total working set of data, the first-pass `WHERE` constraints are applied to the \nindividual rows, and rows that do not satisfy the constraint are discarded.  Each of the constraints\ncan only access columns directly from the tables requested in the `FROM` clause.  Aliases in the\n`SELECT` part of the query are not accessible in most databases since they may include \nexpressions dependent on parts of the query that have not yet executed.\n\n#### 3. `GROUP BY`\n\nThe remaining rows after the `WHERE` constraints are applied are then grouped based on common values\nin the column specified in the `GROUP BY` clause.  As a result of the grouping, there will only be as \nmany rows as there are unique values in that column.  Implicitly, this means that you should only need \nto use this when you have aggregate functions in your query.\n\n#### 4. `HAVING`\n\nIf the query has a `GROUP BY` clause, then the constraints in the `HAVING` clause are then applied\nto the grouped rows, discard the grouped rows that don't satisfy the constraint.  Like the `WHERE` \nclause, aliases are also not accessible from this step in most databases.\n\n#### 5. `SELECT`\n\nAny expressions in the `SELECT` part of the query are finally computed.\n\n#### 6. `DISTINCT`\n\nOf the remaining rows, rows with duplicate values in the column marked as `DISTINCT` will be discarded.\n\n#### 7. `ORDER BY`\n\nIf an order is specified by the `ORDER BY` clause, the rows are then sorted by the specified data in \neither ascending or descending order.  Since all the expressions in the `SELECT` part of the query \nhave been computed, you can reference aliases in this clause.\n\n#### 8. `LIMIT` / `OFFSET`\n\nFinally, the rows that fall outside the range specified by the `LIMIT` and `OFFSET` are discarded, \nleaving the final set of rows to be returned from the query.\n\n#### Conclusion\n\nNot every query needs to have all the parts we listed above, but a part of why SQL is so flexible is\nthat it allows developers and data analysts to quickly manipulate data without having to write additional\ncode, all just by using the above clauses.\n\n### Exercise\n\nHere ends our lessons on `SELECT` queries, congrats of making it this far!  This exercise will try\nand test your understanding of queries, so don't be discouraged if you find them challenging.  Just\ntry your best."
            }
          },
          "sqlExercise": {
            "schemaSql": "CREATE TABLE movies (\n    id integer primary key autoincrement,\n    title text,\n    director text,\n    year integer, \n    length_minutes integer\n);\nCREATE TABLE boxoffice (\n    -- id integer primary key autoincrement,\n    movie_id integer,\n    rating real,\n    domestic_sales real,\n    international_sales real\n);",
            "seedSql": "INSERT INTO movies (id, title, director, year, length_minutes) VALUES\n(1, 'Toy Story', 'John Lasseter', 1995, 81),\n(2, 'A Bug''s Life', 'John Lasseter', 1998, 95),\n(3, 'Toy Story 2', 'John Lasseter', 1999, 93),\n(4, 'Monsters, Inc.', 'Pete Docter', 2001, 92),\n(5, 'Finding Nemo', 'Andrew Stanton', 2003, 107),\n(6, 'The Incredibles', 'Brad Bird', 2004, 116),\n(7, 'Cars', 'John Lasseter', 2006, 117),\n(8, 'Ratatouille', 'Brad Bird', 2007, 115),\n(9, 'WALL-E', 'Andrew Stanton', 2008, 104),\n(10, 'Up', 'Pete Docter', 2009, 101),\n(11, 'Toy Story 3', 'Lee Unkrich', 2010, 103),\n(12, 'Cars 2', 'John Lasseter', 2011, 120),\n(13, 'Brave', 'Brenda Chapman', 2012, 102),\n(14, 'Monsters University', 'Dan Scanlon', 2013, 110);\n\nINSERT INTO boxoffice (movie_id, rating, domestic_sales, international_sales) VALUES\n(5, 8.2, 380843261, 555900000),\n(14, 7.4, 268492764, 475066843),\n(8, 8, 206445654, 417277164),\n(12, 6.4, 191452396, 368400000),\n(3, 7.9, 245852179, 239163000),\n(6, 8, 261441092, 370001000),\n(9, 8.5, 223808164, 297503696),\n(11, 8.4, 415004880, 648167031),\n(1, 8.3, 191796233, 170162503),\n(7, 7.2, 244082982, 217900167),\n(10, 8.3, 293004164, 438338580),\n(4, 8.1, 289916256, 272900000),\n(2, 7.2, 162798565, 200600000),\n(13, 7.2, 237283207, 301700000);\n",
            "defaultTable": "movies",
            "defaultQuery": "-- Write your SQL statement below to solve the exercise tasks:\nSELECT * FROM movies LIMIT 10;",
            "tasks": [
              {
                "instruction": "Find the number of movies each director has directed",
                "expectedQuery": "SELECT director, COUNT(id) as Num_movies_directed\nFROM movies\nGROUP BY director;",
                "solution": "SELECT director, COUNT(id) as Num_movies_directed\nFROM movies\nGROUP BY director;",
                "orderSensitive": false
              },
              {
                "instruction": "Find the total domestic and international sales that can be attributed to each director",
                "expectedQuery": "SELECT director, SUM(domestic_sales + international_sales) as Cumulative_sales_from_all_movies\nFROM movies\n    INNER JOIN boxoffice\n        ON movies.id = boxoffice.movie_id\nGROUP BY director;",
                "solution": "SELECT director, SUM(domestic_sales + international_sales) as Cumulative_sales_from_all_movies\nFROM movies\n    INNER JOIN boxoffice\n        ON movies.id = boxoffice.movie_id\nGROUP BY director;",
                "orderSensitive": false
              }
            ]
          }
        }
      ],
      "msa": {
        "title": "Module 3 Assessment: Aggregation, GROUP BY & Execution Order (SQLZoo)",
        "passPercent": 85,
        "durationMinutes": 20,
        "mcqSection": {
          "sectionTitle": "Section 1: SQLZoo SUM, COUNT & Grouping MCQs",
          "durationMinutes": 20,
          "passCutoffPercent": 85,
          "questions": [
            {
              "questionId": "sum_and_count_quiz-q1",
              "question": "Select the statement that shows the sum of population of all countries in 'Europe'",
              "prompt": "Select the statement that shows the sum of population of all countries in 'Europe'",
              "options": [
                "SELECT name , population FROM bbc WHERE region = 'Europe'",
                "SELECT population FROM bbc WHERE region = 'Europe' SUM BY region",
                "SELECT SUM ( population ) FROM bbc WHERE region = 'Europe'",
                "SELECT SUM ( population FROM bbc WHERE region = 'Europe' )"
              ],
              "correctAnswer": 2,
              "correctIndex": 2,
              "correctOptionIndex": 2,
              "explanation": "Correct SQL Syntax/Output: SELECT SUM ( population ) FROM bbc WHERE region = 'Europe'"
            },
            {
              "questionId": "sum_and_count_quiz-q2",
              "question": "Select the statement that shows the number of countries with population smaller than 150000",
              "prompt": "Select the statement that shows the number of countries with population smaller than 150000",
              "options": [
                "SELECT COUNT ( name ) FROM bbc WHERE population < 150000",
                "SELECT COUNT ( population < 150000 ) FROM bbc",
                "SELECT name FROM bbc WHERE population < 150000",
                "SELECT population AS COUNT FROM bbc WHERE population < 150000"
              ],
              "correctAnswer": 0,
              "correctIndex": 0,
              "correctOptionIndex": 0,
              "explanation": "Correct SQL Syntax/Output: SELECT COUNT ( name ) FROM bbc WHERE population < 150000"
            },
            {
              "questionId": "sum_and_count_quiz-q3",
              "question": "Select the list of core SQL aggregate functions",
              "prompt": "Select the list of core SQL aggregate functions",
              "options": [
                "AVG(), COUNT(), FIRST(), LAST(), SUM()",
                "AVG(), COUNT(), MAX(), MEDIAN(), MIN(), ROUND(), SUM()",
                "AVG(), COUNT(), CONCAT(), FIRST(), LAST(), MAX(), MIN(), SUM()",
                "AVG(), COUNT(), MAX(), MIN(), SUM()"
              ],
              "correctAnswer": 3,
              "correctIndex": 3,
              "correctOptionIndex": 3,
              "explanation": "Correct SQL Syntax/Output: AVG(), COUNT(), MAX(), MIN(), SUM()"
            },
            {
              "questionId": "sum_and_count_quiz-q5",
              "question": "Select the statement that shows the average population of 'Poland', 'Germany' and 'Denmark'",
              "prompt": "Select the statement that shows the average population of 'Poland', 'Germany' and 'Denmark'",
              "options": [
                "SELECT AVG ( population ) FROM bbc WHERE name = ( 'Poland' , 'Germany' , 'Denmark' )",
                "SELECT AVG ( population ) FROM bbc WHERE name IN ( 'Poland' , 'Germany' , 'Denmark' )",
                "SELECT AVG ( population ) FROM bbc WHERE name LIKE ( 'Poland' , 'Germany' , 'Denmark' )",
                "SELECT AVG ( population ) FROM bbc WHERE name LIKE ( Poland , Germany , Denmark )"
              ],
              "correctAnswer": 1,
              "correctIndex": 1,
              "correctOptionIndex": 1,
              "explanation": "Correct SQL Syntax/Output: SELECT AVG ( population ) FROM bbc WHERE name IN ( 'Poland' , 'Germany' , 'Denmark' )"
            },
            {
              "questionId": "sum_and_count_quiz-q6",
              "question": "Select the statement that shows the medium population density of each region",
              "prompt": "Select the statement that shows the medium population density of each region",
              "options": [
                "SELECT region , AVG ( population / area ) AS density FROM bbc",
                "SELECT region , COUNT ( population ) / COUNT ( area ) AS density FROM bbc GROUP BY region",
                "SELECT region , SUM ( population ) / COUNT ( area ) AS density FROM bbc GROUP BY region",
                "SELECT region , SUM ( population ) / SUM ( area ) AS density FROM bbc HAVING region"
              ],
              "correctAnswer": 0,
              "correctIndex": 0,
              "correctOptionIndex": 0,
              "explanation": "Correct SQL Syntax/Output: SELECT region , AVG ( population / area ) AS density FROM bbc"
            },
            {
              "questionId": "sum_and_count_quiz-q7",
              "question": "Select the statement that shows the name and population density of the country with the largest population",
              "prompt": "Select the statement that shows the name and population density of the country with the largest population",
              "options": [
                "SELECT name , density AS population / area FROM bbc WHERE population = MAX ( population )",
                "SELECT name , density AS population / area FROM bbc WHERE population = ( SELECT MAX ( population ) FROM bbc )",
                "SELECT name, MAX (population) FROM bbc WHERE population / (SELECT area FROM bbc)",
                "SELECT name, population/area AS density FROM bbc WHERE population = (SELECT MAX(population) FROM bbc)"
              ],
              "correctAnswer": 3,
              "correctIndex": 3,
              "correctOptionIndex": 3,
              "explanation": "Correct SQL Syntax/Output: SELECT name, population/area AS density FROM bbc WHERE population = (SELECT MAX(population) FROM bbc)"
            },
            {
              "questionId": "m3-msa-q7",
              "question": "What is the fundamental difference between the WHERE clause and the HAVING clause in SQL?",
              "prompt": "What is the fundamental difference between the WHERE clause and the HAVING clause in SQL?",
              "options": [
                "WHERE filters individual rows before grouping; HAVING filters aggregated groups after grouping.",
                "WHERE works only with numbers, while HAVING works with strings.",
                "WHERE is optional, but HAVING is mandatory in all queries.",
                "There is no difference; they are aliases for each other."
              ],
              "correctAnswer": 0,
              "correctIndex": 0,
              "correctOptionIndex": 0,
              "explanation": "WHERE filters records prior to GROUP BY aggregation; HAVING filters the aggregated summary groups."
            },
            {
              "questionId": "m3-msa-q8",
              "question": "In what logical order does a relational SQL query execute?",
              "prompt": "In what logical order does a relational SQL query execute?",
              "options": [
                "SELECT -> FROM -> WHERE -> GROUP BY -> ORDER BY",
                "FROM -> WHERE -> GROUP BY -> HAVING -> SELECT -> DISTINCT -> ORDER BY -> LIMIT",
                "WHERE -> SELECT -> FROM -> ORDER BY -> LIMIT",
                "LIMIT -> ORDER BY -> SELECT -> FROM -> WHERE"
              ],
              "correctAnswer": 1,
              "correctIndex": 1,
              "correctOptionIndex": 1,
              "explanation": "The database first determines the tables (FROM), applies row filters (WHERE), aggregates (GROUP BY/HAVING), computes selected columns (SELECT/DISTINCT), and finally sorts and paginates (ORDER BY/LIMIT)."
            },
            {
              "questionId": "m3-msa-q9",
              "question": "What is the critical difference between `COUNT(*)` and `COUNT(column_name)`?",
              "prompt": "What is the critical difference between `COUNT(*)` and `COUNT(column_name)`?",
              "options": [
                "`COUNT(*)` counts all rows including NULLs; `COUNT(column_name)` only counts rows where `column_name` is NOT NULL.",
                "`COUNT(*)` is slower and always performs a full disk scan.",
                "`COUNT(column_name)` includes NULL values if the column is indexed.",
                "There is no difference in output in standard SQL."
              ],
              "correctAnswer": 0,
              "correctIndex": 0,
              "correctOptionIndex": 0,
              "explanation": "COUNT(*) returns the count of all rows in the group regardless of contents, while COUNT(col) ignores rows where that specific column contains NULL."
            },
            {
              "questionId": "m3-msa-q10",
              "question": "What is the difference between the `WHERE` clause and the `HAVING` clause?",
              "prompt": "What is the difference between the `WHERE` clause and the `HAVING` clause?",
              "options": [
                "`WHERE` filters individual rows before grouping; `HAVING` filters aggregated groups after `GROUP BY`.",
                "`HAVING` filters individual rows before grouping; `WHERE` filters after `GROUP BY`.",
                "`WHERE` can use aggregate functions like `SUM()`, but `HAVING` cannot.",
                "`HAVING` is deprecated in modern SQL and replaced by nested queries."
              ],
              "correctAnswer": 0,
              "correctIndex": 0,
              "correctOptionIndex": 0,
              "explanation": "WHERE filters rows before any grouping or aggregation occurs. HAVING filters groups after aggregation and can contain aggregate expressions like `HAVING COUNT(*) > 5`."
            },
            {
              "questionId": "m3-msa-q11",
              "question": "What is the correct logical order of query execution in SQL?",
              "prompt": "What is the correct logical order of query execution in SQL?",
              "options": [
                "SELECT -> FROM -> WHERE -> GROUP BY -> HAVING -> ORDER BY -> LIMIT",
                "FROM -> WHERE -> GROUP BY -> HAVING -> SELECT -> ORDER BY -> LIMIT",
                "FROM -> SELECT -> WHERE -> ORDER BY -> GROUP BY -> LIMIT",
                "WHERE -> FROM -> GROUP BY -> HAVING -> SELECT -> ORDER BY -> LIMIT"
              ],
              "correctAnswer": 1,
              "correctIndex": 1,
              "correctOptionIndex": 1,
              "explanation": "SQL logical query processing starts at FROM (identifying tables), applies WHERE (row filtering), groups via GROUP BY, filters groups with HAVING, evaluates SELECT expressions, sorts via ORDER BY, and finally applies LIMIT/OFFSET."
            },
            {
              "questionId": "m3-msa-q12",
              "question": "Why does the query `SELECT department, AVG(salary) FROM employees WHERE AVG(salary) > 50000 GROUP BY department;` fail?",
              "prompt": "Why does the query `SELECT department, AVG(salary) FROM employees WHERE AVG(salary) > 50000 GROUP BY department;` fail?",
              "options": [
                "`AVG()` cannot be computed for salaries.",
                "Aggregate functions like `AVG()` cannot appear in the `WHERE` clause; they must be in `HAVING`.",
                "`department` must be listed in an ORDER BY clause.",
                "The query requires a subquery to calculate averages."
              ],
              "correctAnswer": 1,
              "correctIndex": 1,
              "correctOptionIndex": 1,
              "explanation": "The WHERE clause filters individual rows before any groups or aggregate calculations exist. Filtering based on aggregate values must be written as `HAVING AVG(salary) > 50000`."
            },
            {
              "questionId": "m3-msa-q13",
              "question": "What does `ROUND(AVG(rating), 2)` do in a SELECT list?",
              "prompt": "What does `ROUND(AVG(rating), 2)` do in a SELECT list?",
              "options": [
                "Rounds the average rating to 2 significant digits",
                "Rounds the average rating to 2 decimal places",
                "Divides the average rating by 2",
                "Limits the rating count to 2"
              ],
              "correctAnswer": 1,
              "correctIndex": 1,
              "correctOptionIndex": 1,
              "explanation": "ROUND(numeric_expression, n) rounds the numerical output to n places after the decimal point."
            },
            {
              "questionId": "m3-msa-q14",
              "question": "Can non-aggregated columns be included in the `SELECT` list if they are not part of the `GROUP BY` clause in standard SQL?",
              "prompt": "Can non-aggregated columns be included in the `SELECT` list if they are not part of the `GROUP BY` clause in standard SQL?",
              "options": [
                "Yes, the database randomly picks an arbitrary value from each group.",
                "No, in standard SQL, every non-aggregated column in the SELECT list must appear in the GROUP BY clause.",
                "Yes, as long as the table has a primary key.",
                "Yes, but only in SQLite and MySQL 8+."
              ],
              "correctAnswer": 1,
              "correctIndex": 1,
              "correctOptionIndex": 1,
              "explanation": "In standard SQL, each row in the output represents a group. Therefore, any selected column must either be grouped (in GROUP BY) or aggregated (SUM, AVG, etc.) so that each group produces a single definitive value."
            }
          ]
        }
      }
    },
    {
      "moduleId": "sql-m4",
      "title": "Module 4: Database Mutations & Table Administration",
      "description": "Master DDL and DML commands: inserting new records, safely modifying data with UPDATE/DELETE, and structuring tables with CREATE, ALTER, and DROP.",
      "estimatedMinutes": 90,
      "topics": [
        {
          "topicId": "sql-m4-t1",
          "title": "SQL Lesson 13: Inserting rows",
          "description": "Hands-on SQL exercise: SQL Lesson 13: Inserting rows. Learn syntax, inspect live SQLite tables, and validate queries in real-time.",
          "mode": "SQL_INTERACTIVE",
          "readingTimeMinutes": 15,
          "lessonMarkdown": "We've spent quite a few lessons on how to query for data in a database, so it's time to start learning\na bit about SQL schemas and how to add new data.\n\n### What is a Schema?\n\nWe previously described a table in a database as a two-dimensional set of rows and columns, with the\ncolumns being the properties and the rows being instances of the entity in the table.  In SQL, the \n*database schema* is what describes the structure of each table, and the datatypes that each column\nof the table can contain.  \n\n    Example: Correlated subquery\n    \n        For example, in our **Movies** table, the values in the *Year* column must \n        be an Integer, and the values in the *Title* column must be a String.\n    \n\nThis fixed structure is what allows a database to be efficient, and consistent despite storing millions\nor even billions of rows.\n\n### Inserting new data\n\nWhen inserting data into a database, we need to use an `INSERT` statement, which declares which table\nto write into, the columns of data that we are filling, and one or more rows of data to insert.  In \ngeneral, each row of data you insert should contain values for every corresponding column in the table.\nYou can insert multiple rows at a time by just listing them sequentially.\n\n> **Insert statement with values for all columns**\n```sql\nINSERT INTO mytable\nVALUES (value_or_expr, another_value_or_expr, ...),\n       (value_or_expr_2, another_value_or_expr_2, ...),\n       ...;\n```\n\nIn some cases, if you have incomplete data and the table contains columns that support default values,\nyou can insert rows with only the columns of data you have by specifying them explicitly.\n\n> **Insert statement with specific columns**\n```sql\nINSERT INTO mytable\n**(column, another_column, ...)**\nVALUES (value_or_expr, another_value_or_expr, ...),\n      (value_or_expr_2, another_value_or_expr_2, ...),\n      ...;\n```\n\nIn these cases, the number of values need to match the number of columns specified.  Despite this being \na more verbose statement to write, inserting values this way has the benefit of being forward compatible.\nFor example, if you add a new column to the table with a default value, no hardcoded `INSERT` statements\nwill have to change as a result to accommodate that change.\nIn addition, you can use mathematical and string expressions with the values that you are inserting.\nThis can be useful to ensure that all data inserted is formatted a certain way.\n\n> **Example Insert statement with expressions**\n```sql\nINSERT INTO boxoffice\n**(movie_id, rating, sales_in_millions)**\nVALUES (1, 9.9, 283742034 / 1000000);\n```\n\n### Exercise\n\nIn this exercise, we are going to play studio executive and add a few movies to the **Movies** to our\nportfolio.  In this table, the **Id** is an auto-incrementing integer, so you can try \ninserting a row with only the other columns defined.  \nSince the following lessons will modify the database, you'll have to manually run each query once they \nare ready to go.",
          "lessonContent": {
            "textAndVisuals": {
              "content": "We've spent quite a few lessons on how to query for data in a database, so it's time to start learning\na bit about SQL schemas and how to add new data.\n\n### What is a Schema?\n\nWe previously described a table in a database as a two-dimensional set of rows and columns, with the\ncolumns being the properties and the rows being instances of the entity in the table.  In SQL, the \n*database schema* is what describes the structure of each table, and the datatypes that each column\nof the table can contain.  \n\n    Example: Correlated subquery\n    \n        For example, in our **Movies** table, the values in the *Year* column must \n        be an Integer, and the values in the *Title* column must be a String.\n    \n\nThis fixed structure is what allows a database to be efficient, and consistent despite storing millions\nor even billions of rows.\n\n### Inserting new data\n\nWhen inserting data into a database, we need to use an `INSERT` statement, which declares which table\nto write into, the columns of data that we are filling, and one or more rows of data to insert.  In \ngeneral, each row of data you insert should contain values for every corresponding column in the table.\nYou can insert multiple rows at a time by just listing them sequentially.\n\n> **Insert statement with values for all columns**\n```sql\nINSERT INTO mytable\nVALUES (value_or_expr, another_value_or_expr, ...),\n       (value_or_expr_2, another_value_or_expr_2, ...),\n       ...;\n```\n\nIn some cases, if you have incomplete data and the table contains columns that support default values,\nyou can insert rows with only the columns of data you have by specifying them explicitly.\n\n> **Insert statement with specific columns**\n```sql\nINSERT INTO mytable\n**(column, another_column, ...)**\nVALUES (value_or_expr, another_value_or_expr, ...),\n      (value_or_expr_2, another_value_or_expr_2, ...),\n      ...;\n```\n\nIn these cases, the number of values need to match the number of columns specified.  Despite this being \na more verbose statement to write, inserting values this way has the benefit of being forward compatible.\nFor example, if you add a new column to the table with a default value, no hardcoded `INSERT` statements\nwill have to change as a result to accommodate that change.\nIn addition, you can use mathematical and string expressions with the values that you are inserting.\nThis can be useful to ensure that all data inserted is formatted a certain way.\n\n> **Example Insert statement with expressions**\n```sql\nINSERT INTO boxoffice\n**(movie_id, rating, sales_in_millions)**\nVALUES (1, 9.9, 283742034 / 1000000);\n```\n\n### Exercise\n\nIn this exercise, we are going to play studio executive and add a few movies to the **Movies** to our\nportfolio.  In this table, the **Id** is an auto-incrementing integer, so you can try \ninserting a row with only the other columns defined.  \nSince the following lessons will modify the database, you'll have to manually run each query once they \nare ready to go."
            }
          },
          "sqlExercise": {
            "schemaSql": "CREATE TABLE movies (\n    id integer primary key autoincrement,\n    title text,\n    director text,\n    year integer, \n    length_minutes integer\n);",
            "seedSql": "INSERT INTO movies (id, title, director, year, length_minutes) VALUES\n(1, 'Toy Story', 'John Lasseter', 1995, 81),\n(2, 'A Bug''s Life', 'John Lasseter', 1998, 95),\n(3, 'Toy Story 2', 'John Lasseter', 1999, 93),\n(4, 'Monsters, Inc.', 'Pete Docter', 2001, 92),\n(5, 'Finding Nemo', 'Andrew Stanton', 2003, 107),\n(6, 'The Incredibles', 'Brad Bird', 2004, 116),\n(7, 'Cars', 'John Lasseter', 2006, 117),\n(8, 'Ratatouille', 'Brad Bird', 2007, 115),\n(9, 'WALL-E', 'Andrew Stanton', 2008, 104),\n(10, 'Up', 'Pete Docter', 2009, 101),\n(11, 'Toy Story 3', 'Lee Unkrich', 2010, 103),\n(12, 'Cars 2', 'John Lasseter', 2011, 120),\n(13, 'Brave', 'Brenda Chapman', 2012, 102),\n(14, 'Monsters University', 'Dan Scanlon', 2013, 110);\n",
            "defaultTable": "movies",
            "defaultQuery": "-- Write your INSERT INTO statement here\n",
            "tasks": [
              {
                "instruction": "Add the studio's new production, Toy Story 4 to the list of movies (you can use any director)",
                "expectedQuery": "INSERT INTO movies VALUES (4, \"Toy Story 4\", \"El Directore\", 2015, 90);",
                "solution": "INSERT INTO movies VALUES (4, \"Toy Story 4\", \"El Directore\", 2015, 90);",
                "orderSensitive": false
              },
              {
                "instruction": "Toy Story 4 has been released to critical acclaim! It had a rating of 8.7 , and made 340 million domestically and 270 million internationally . Add the record to the BoxOffice table.",
                "expectedQuery": "INSERT INTO boxoffice VALUES (4, 8.7, 340000000, 270000000);",
                "solution": "INSERT INTO boxoffice VALUES (4, 8.7, 340000000, 270000000);",
                "orderSensitive": false
              }
            ]
          }
        },
        {
          "topicId": "sql-m4-t2",
          "title": "SQL Lesson 14: Updating rows",
          "description": "Hands-on SQL exercise: SQL Lesson 14: Updating rows. Learn syntax, inspect live SQLite tables, and validate queries in real-time.",
          "mode": "SQL_INTERACTIVE",
          "readingTimeMinutes": 15,
          "lessonMarkdown": "In addition to adding new data, a common task is to update existing data, which can be done using an \n`UPDATE` statement.  Similar to the `INSERT` statement, you have to specify exactly which table,\ncolumns, and rows to update.  In addition, the data you are updating has to match the data type of \nthe columns in the table schema.\n\n> **Update statement with values**\n```sql\nUPDATE mytable\nSET column = value_or_expr, \n    other_column = another_value_or_expr, \n    ...\nWHERE condition;\n```\n\nThe statement works by taking multiple column/value pairs, and applying those changes to each and \nevery row that satisfies the constraint in the `WHERE` clause.\n\n### Taking care\n\nMost people working with SQL **will** make mistakes updating data at one point or another.  Whether it's \nupdating the wrong set of rows in a production database, or accidentally leaving out the `WHERE` \nclause (which causes the update to apply to *all* rows), you need to be extra careful when \nconstructing `UPDATE` statements.\nOne helpful tip is to always write the constraint first and test it in a `SELECT` query to make sure \nyou are updating the right rows, and only then writing the column/value pairs to update.\n\n### Exercise\n\nIt looks like some of the information in our **Movies** database might be incorrect, so go ahead and\nfix them through the exercises below.",
          "lessonContent": {
            "textAndVisuals": {
              "content": "In addition to adding new data, a common task is to update existing data, which can be done using an \n`UPDATE` statement.  Similar to the `INSERT` statement, you have to specify exactly which table,\ncolumns, and rows to update.  In addition, the data you are updating has to match the data type of \nthe columns in the table schema.\n\n> **Update statement with values**\n```sql\nUPDATE mytable\nSET column = value_or_expr, \n    other_column = another_value_or_expr, \n    ...\nWHERE condition;\n```\n\nThe statement works by taking multiple column/value pairs, and applying those changes to each and \nevery row that satisfies the constraint in the `WHERE` clause.\n\n### Taking care\n\nMost people working with SQL **will** make mistakes updating data at one point or another.  Whether it's \nupdating the wrong set of rows in a production database, or accidentally leaving out the `WHERE` \nclause (which causes the update to apply to *all* rows), you need to be extra careful when \nconstructing `UPDATE` statements.\nOne helpful tip is to always write the constraint first and test it in a `SELECT` query to make sure \nyou are updating the right rows, and only then writing the column/value pairs to update.\n\n### Exercise\n\nIt looks like some of the information in our **Movies** database might be incorrect, so go ahead and\nfix them through the exercises below."
            }
          },
          "sqlExercise": {
            "schemaSql": "CREATE TABLE movies (\n    id integer primary key autoincrement,\n    title text,\n    director text,\n    year integer, \n    length_minutes integer\n);",
            "seedSql": "INSERT INTO movies (id, title, director, year, length_minutes) VALUES\n(1, 'Toy Story', 'John Lasseter', 1995, 81),\n(2, 'A Bug''s Life', 'John Lasseter', 1998, 95),\n(3, 'Toy Story 2', 'John Lasseter', 1999, 93),\n(4, 'Monsters, Inc.', 'Pete Docter', 2001, 92),\n(5, 'Finding Nemo', 'Andrew Stanton', 2003, 107),\n(6, 'The Incredibles', 'Brad Bird', 2004, 116),\n(7, 'Cars', 'John Lasseter', 2006, 117),\n(8, 'Ratatouille', 'Brad Bird', 2007, 115),\n(9, 'WALL-E', 'Andrew Stanton', 2008, 104),\n(10, 'Up', 'Pete Docter', 2009, 101),\n(11, 'Toy Story 3', 'Lee Unkrich', 2010, 103),\n(12, 'Cars 2', 'John Lasseter', 2011, 120),\n(13, 'Brave', 'Brenda Chapman', 2012, 102),\n(14, 'Monsters University', 'Dan Scanlon', 2013, 110);\n",
            "defaultTable": "movies",
            "defaultQuery": "-- Write your UPDATE statement here\n",
            "tasks": [
              {
                "instruction": "The director for A Bug's Life is incorrect, it was actually directed by John Lasseter",
                "expectedQuery": "UPDATE movies\nSET director = \"John Lasseter\"\nWHERE id = 2;",
                "solution": "UPDATE movies\nSET director = \"John Lasseter\"\nWHERE id = 2;",
                "orderSensitive": false
              },
              {
                "instruction": "The year that Toy Story 2 was released is incorrect, it was actually released in 1999",
                "expectedQuery": "UPDATE movies\nSET year = 1999\nWHERE id = 3;",
                "solution": "UPDATE movies\nSET year = 1999\nWHERE id = 3;",
                "orderSensitive": false
              },
              {
                "instruction": "Both the title and director for Toy Story 8 is incorrect! The title should be \"Toy Story 3\" and it was directed by Lee Unkrich",
                "expectedQuery": "UPDATE movies\nSET title = \"Toy Story 3\", director = \"Lee Unkrich\"\nWHERE id = 11;",
                "solution": "UPDATE movies\nSET title = \"Toy Story 3\", director = \"Lee Unkrich\"\nWHERE id = 11;",
                "orderSensitive": false
              }
            ]
          }
        },
        {
          "topicId": "sql-m4-t3",
          "title": "SQL Lesson 15: Deleting rows",
          "description": "Hands-on SQL exercise: SQL Lesson 15: Deleting rows. Learn syntax, inspect live SQLite tables, and validate queries in real-time.",
          "mode": "SQL_INTERACTIVE",
          "readingTimeMinutes": 15,
          "lessonMarkdown": "When you need to delete data from a table in the database, you can use a `DELETE` statement, which \ndescribes the table to act on, and the rows of the table to delete through the `WHERE` clause.\n\n> **Delete statement with condition**\n```sql\nDELETE FROM mytable\nWHERE condition;\n```\n\nIf you decide to leave out the `WHERE` constraint, then *all* rows are removed, which is a quick\nand easy way to clear out a table completely (if intentional).\n\n### Taking extra care\n\nLike the `UPDATE` statement from last lesson, it's recommended that you run the constraint in a \n`SELECT` query first to ensure that you are removing the right rows.  Without a proper backup or test\ndatabase, it is downright easy to irrevocably remove data, so always read your `DELETE` statements \ntwice and execute once.\n\n### Exercise\n\nThe database needs to be cleaned up a little bit, so try and delete a few rows in the tasks below.",
          "lessonContent": {
            "textAndVisuals": {
              "content": "When you need to delete data from a table in the database, you can use a `DELETE` statement, which \ndescribes the table to act on, and the rows of the table to delete through the `WHERE` clause.\n\n> **Delete statement with condition**\n```sql\nDELETE FROM mytable\nWHERE condition;\n```\n\nIf you decide to leave out the `WHERE` constraint, then *all* rows are removed, which is a quick\nand easy way to clear out a table completely (if intentional).\n\n### Taking extra care\n\nLike the `UPDATE` statement from last lesson, it's recommended that you run the constraint in a \n`SELECT` query first to ensure that you are removing the right rows.  Without a proper backup or test\ndatabase, it is downright easy to irrevocably remove data, so always read your `DELETE` statements \ntwice and execute once.\n\n### Exercise\n\nThe database needs to be cleaned up a little bit, so try and delete a few rows in the tasks below."
            }
          },
          "sqlExercise": {
            "schemaSql": "CREATE TABLE movies (\n    id integer primary key autoincrement,\n    title text,\n    director text,\n    year integer, \n    length_minutes integer\n);",
            "seedSql": "INSERT INTO movies (id, title, director, year, length_minutes) VALUES\n(1, 'Toy Story', 'John Lasseter', 1995, 81),\n(2, 'A Bug''s Life', 'John Lasseter', 1998, 95),\n(3, 'Toy Story 2', 'John Lasseter', 1999, 93),\n(4, 'Monsters, Inc.', 'Pete Docter', 2001, 92),\n(5, 'Finding Nemo', 'Andrew Stanton', 2003, 107),\n(6, 'The Incredibles', 'Brad Bird', 2004, 116),\n(7, 'Cars', 'John Lasseter', 2006, 117),\n(8, 'Ratatouille', 'Brad Bird', 2007, 115),\n(9, 'WALL-E', 'Andrew Stanton', 2008, 104),\n(10, 'Up', 'Pete Docter', 2009, 101),\n(11, 'Toy Story 3', 'Lee Unkrich', 2010, 103),\n(12, 'Cars 2', 'John Lasseter', 2011, 120),\n(13, 'Brave', 'Brenda Chapman', 2012, 102),\n(14, 'Monsters University', 'Dan Scanlon', 2013, 110);\n",
            "defaultTable": "movies",
            "defaultQuery": "-- Write your DELETE statement here\n",
            "tasks": [
              {
                "instruction": "This database is getting too big, lets remove all movies that were released before 2005.",
                "expectedQuery": "DELETE FROM movies\nwhere year < 2005;",
                "solution": "DELETE FROM movies\nwhere year < 2005;",
                "orderSensitive": false
              },
              {
                "instruction": "Andrew Stanton has also left the studio, so please remove all movies directed by him.",
                "expectedQuery": "DELETE FROM movies\nwhere director = \"Andrew Stanton\";",
                "solution": "DELETE FROM movies\nwhere director = \"Andrew Stanton\";",
                "orderSensitive": false
              }
            ]
          }
        },
        {
          "topicId": "sql-m4-t4",
          "title": "SQL Lesson 16: Creating tables",
          "description": "Hands-on SQL exercise: SQL Lesson 16: Creating tables. Learn syntax, inspect live SQLite tables, and validate queries in real-time.",
          "mode": "SQL_INTERACTIVE",
          "readingTimeMinutes": 20,
          "lessonMarkdown": "When you have new entities and relationships to store in your database, you can create a new database \ntable using the `CREATE TABLE` statement.  \n\n> **Create table statement w/ optional table constraint and default value**\n```sql\nCREATE TABLE IF NOT EXISTS mytable (\n    column *DataType* *TableConstraint* DEFAULT *default_value*,\n    another_column *DataType* *TableConstraint* DEFAULT *default_value*,\n    ...\n);\n```\n\nThe structure of the new table is defined by its *table schema*, which defines a series of columns. \nEach column has a name, the type of data allowed in that column, an *optional* table constraint \non values being inserted, and an optional default value.\nIf there already exists a table with the same name, the SQL implementation will usually throw an error,\nso to suppress the error and skip creating a table if one exists, you can use the `IF NOT EXISTS` clause.\n\n### Table data types\n\nDifferent databases support different data types, but the common types support numeric, string, and \nother miscellaneous things like dates, booleans, or even binary data.  Here are some examples that you\nmight use in real code.\n\n    \n        \n            Data type\n            Description\n        \n        \n            `INTEGER`, `BOOLEAN`\n            The integer datatypes can store whole integer values like the count of a number or an \n                age.  In some implementations, the boolean value is just represented as an integer value\n                of just 0 or 1.\n        \n        \n            `FLOAT`, `DOUBLE`, `REAL`\n            The floating point datatypes can store more precise numerical data like measurements \n                or fractional values.  Different types can be used depending on the floating point \n                precision required for that value.\n        \n        \n            `CHARACTER(num_chars)`, `VARCHAR(num_chars)`, `TEXT`\n            The text based datatypes can store strings and text in all sorts of locales.  The distinction\n                between the various types generally amount to underlaying efficiency of the database when working\n                with these columns.\n                Both the CHARACTER and VARCHAR (variable character) types are specified with\n                the max number of characters that they can store (longer values may be truncated), so can be more\n                efficient to store and query with big tables.\n        \n        \n            `DATE`, `DATETIME`\n            SQL can also store date and time stamps to keep track of time series and event data.  They can be\n                tricky to work with especially when manipulating data across timezones.\n        \n        \n            `BLOB`\n            Finally, SQL can store binary data in blobs right in the database.  These values are often opaque\n                to the database, so you usually have to store them with the right metadata to requery them.\n        \n        \n            Docs:\n                MySQL,\n                Postgres,\n                SQLite,\n                Microsoft SQL Server\n            \n        \n    \n\n### Table constraints\n\nWe aren't going to dive too deep into table constraints in this lesson, but each column can have \nadditional table constraints on it which limit what values can be inserted into that column.  This\nis not a comprehensive list, but will show a few common constraints that you might find useful.\n\n    \n        \n            Constraint\n            Description\n        \n        \n            `PRIMARY KEY`\n            This means that the values in this column are unique, and each value can be used to \n                identify a single row in this table.\n        \n        \n            `AUTOINCREMENT`\n            For integer values, this means that the value is automatically filled in and incremented \n                with each row insertion.  Not supported in all databases.\n        \n        \n            `UNIQUE`\n            This means that the values in this column have to be unique, so you can't insert another \n                row with the same value in this column as another row in the table.  Differs from the\n                `PRIMARY KEY` in that it doesn't have to be a key for a row in the table.\n        \n        \n            `NOT NULL`\n            This means that the inserted value can not be `NULL`.\n        \n        \n            `CHECK (expression)`\n            This allows you to run a more complex expression to test whether the values inserted \n                are valid. For example, you can check that values are positive, or greater than a specific \n                size, or start with a certain prefix, etc.\n        \n        \n            `FOREIGN KEY`\n            This is a consistency check which ensures that each value in this column corresponds to \n                another value in a column in another table.\n                For example, if there are two tables, one listing all Employees by ID,\n                and another listing their payroll information, the `FOREIGN KEY` can ensure that every \n                row in the payroll table corresponds to a valid employee in the master Employee list.\n        \n    \n\n### An example\n\nHere's an example schema for the *Movies* table that we've been using in the lessons up to now.\n\n> **Movies table schema**\n```sql\nCREATE TABLE movies (\n    id INTEGER PRIMARY KEY,\n    title TEXT,\n    director TEXT,\n    year INTEGER, \n    length_minutes INTEGER\n);\n```\n\n### Exercise\n\nIn this exercise, you'll need to create a new table for us to insert some new rows into.",
          "lessonContent": {
            "textAndVisuals": {
              "content": "When you have new entities and relationships to store in your database, you can create a new database \ntable using the `CREATE TABLE` statement.  \n\n> **Create table statement w/ optional table constraint and default value**\n```sql\nCREATE TABLE IF NOT EXISTS mytable (\n    column *DataType* *TableConstraint* DEFAULT *default_value*,\n    another_column *DataType* *TableConstraint* DEFAULT *default_value*,\n    ...\n);\n```\n\nThe structure of the new table is defined by its *table schema*, which defines a series of columns. \nEach column has a name, the type of data allowed in that column, an *optional* table constraint \non values being inserted, and an optional default value.\nIf there already exists a table with the same name, the SQL implementation will usually throw an error,\nso to suppress the error and skip creating a table if one exists, you can use the `IF NOT EXISTS` clause.\n\n### Table data types\n\nDifferent databases support different data types, but the common types support numeric, string, and \nother miscellaneous things like dates, booleans, or even binary data.  Here are some examples that you\nmight use in real code.\n\n    \n        \n            Data type\n            Description\n        \n        \n            `INTEGER`, `BOOLEAN`\n            The integer datatypes can store whole integer values like the count of a number or an \n                age.  In some implementations, the boolean value is just represented as an integer value\n                of just 0 or 1.\n        \n        \n            `FLOAT`, `DOUBLE`, `REAL`\n            The floating point datatypes can store more precise numerical data like measurements \n                or fractional values.  Different types can be used depending on the floating point \n                precision required for that value.\n        \n        \n            `CHARACTER(num_chars)`, `VARCHAR(num_chars)`, `TEXT`\n            The text based datatypes can store strings and text in all sorts of locales.  The distinction\n                between the various types generally amount to underlaying efficiency of the database when working\n                with these columns.\n                Both the CHARACTER and VARCHAR (variable character) types are specified with\n                the max number of characters that they can store (longer values may be truncated), so can be more\n                efficient to store and query with big tables.\n        \n        \n            `DATE`, `DATETIME`\n            SQL can also store date and time stamps to keep track of time series and event data.  They can be\n                tricky to work with especially when manipulating data across timezones.\n        \n        \n            `BLOB`\n            Finally, SQL can store binary data in blobs right in the database.  These values are often opaque\n                to the database, so you usually have to store them with the right metadata to requery them.\n        \n        \n            Docs:\n                MySQL,\n                Postgres,\n                SQLite,\n                Microsoft SQL Server\n            \n        \n    \n\n### Table constraints\n\nWe aren't going to dive too deep into table constraints in this lesson, but each column can have \nadditional table constraints on it which limit what values can be inserted into that column.  This\nis not a comprehensive list, but will show a few common constraints that you might find useful.\n\n    \n        \n            Constraint\n            Description\n        \n        \n            `PRIMARY KEY`\n            This means that the values in this column are unique, and each value can be used to \n                identify a single row in this table.\n        \n        \n            `AUTOINCREMENT`\n            For integer values, this means that the value is automatically filled in and incremented \n                with each row insertion.  Not supported in all databases.\n        \n        \n            `UNIQUE`\n            This means that the values in this column have to be unique, so you can't insert another \n                row with the same value in this column as another row in the table.  Differs from the\n                `PRIMARY KEY` in that it doesn't have to be a key for a row in the table.\n        \n        \n            `NOT NULL`\n            This means that the inserted value can not be `NULL`.\n        \n        \n            `CHECK (expression)`\n            This allows you to run a more complex expression to test whether the values inserted \n                are valid. For example, you can check that values are positive, or greater than a specific \n                size, or start with a certain prefix, etc.\n        \n        \n            `FOREIGN KEY`\n            This is a consistency check which ensures that each value in this column corresponds to \n                another value in a column in another table.\n                For example, if there are two tables, one listing all Employees by ID,\n                and another listing their payroll information, the `FOREIGN KEY` can ensure that every \n                row in the payroll table corresponds to a valid employee in the master Employee list.\n        \n    \n\n### An example\n\nHere's an example schema for the *Movies* table that we've been using in the lessons up to now.\n\n> **Movies table schema**\n```sql\nCREATE TABLE movies (\n    id INTEGER PRIMARY KEY,\n    title TEXT,\n    director TEXT,\n    year INTEGER, \n    length_minutes INTEGER\n);\n```\n\n### Exercise\n\nIn this exercise, you'll need to create a new table for us to insert some new rows into."
            }
          },
          "sqlExercise": {
            "schemaSql": "CREATE TABLE movies (\n    id integer primary key autoincrement,\n    title text,\n    director text,\n    year integer, \n    length_minutes integer\n);",
            "seedSql": "INSERT INTO movies (id, title, director, year, length_minutes) VALUES\n(1, 'Toy Story', 'John Lasseter', 1995, 81),\n(2, 'A Bug''s Life', 'John Lasseter', 1998, 95),\n(3, 'Toy Story 2', 'John Lasseter', 1999, 93),\n(4, 'Monsters, Inc.', 'Pete Docter', 2001, 92),\n(5, 'Finding Nemo', 'Andrew Stanton', 2003, 107),\n(6, 'The Incredibles', 'Brad Bird', 2004, 116),\n(7, 'Cars', 'John Lasseter', 2006, 117),\n(8, 'Ratatouille', 'Brad Bird', 2007, 115),\n(9, 'WALL-E', 'Andrew Stanton', 2008, 104),\n(10, 'Up', 'Pete Docter', 2009, 101),\n(11, 'Toy Story 3', 'Lee Unkrich', 2010, 103),\n(12, 'Cars 2', 'John Lasseter', 2011, 120),\n(13, 'Brave', 'Brenda Chapman', 2012, 102),\n(14, 'Monsters University', 'Dan Scanlon', 2013, 110);\n",
            "defaultTable": "movies",
            "defaultQuery": "-- Write your CREATE TABLE statement here\n",
            "tasks": [
              {
                "instruction": "Create a new table named Database with the following columns: &ndash; Name A string (text) describing the name of the database &ndash; Version A number (floating point) of the latest version of this database &ndash; Download_count An integer count of the number of times this database was downloaded This table has no constraints.",
                "expectedQuery": "CREATE TABLE Database (\n    Name TEXT,\n    Version FLOAT,\n    Download_count INTEGER\n);",
                "solution": "CREATE TABLE Database (\n    Name TEXT,\n    Version FLOAT,\n    Download_count INTEGER\n);",
                "orderSensitive": false
              }
            ]
          }
        },
        {
          "topicId": "sql-m4-t5",
          "title": "SQL Lesson 17: Altering tables",
          "description": "Hands-on SQL exercise: SQL Lesson 17: Altering tables. Learn syntax, inspect live SQLite tables, and validate queries in real-time.",
          "mode": "SQL_INTERACTIVE",
          "readingTimeMinutes": 15,
          "lessonMarkdown": "As your data changes over time, SQL provides a way for you to update your corresponding tables and \ndatabase schemas by using the `ALTER TABLE` statement to add, remove, or modify columns and table\nconstraints.\n\n### Adding columns\n\nThe syntax for adding a new column is similar to the syntax when creating new rows in the `CREATE TABLE`\nstatement.  You need to specify the data type of the column along with any potential table constraints \nand default values to be applied to both existing *and* new rows.  In some databases like MySQL, \nyou can even specify where to insert the new column using the `FIRST` or `AFTER` clauses, though this \nis not a standard feature.\n\n> **Altering table to add new column(s)**\n```sql\nALTER TABLE mytable\nADD column *DataType* *OptionalTableConstraint* \n    DEFAULT default_value;\n```\n\n### Removing columns\n\nDropping columns is as easy as specifying the column to drop, however, some databases (including SQLite) \ndon't support this feature.  Instead you may have to create a new table and migrate the data over.\n\n> **Altering table to remove column(s)**\n```sql\nALTER TABLE mytable\nDROP column_to_be_deleted;\n```\n\n### Renaming the table\n\nIf you need to rename the table itself, you can also do that using the `RENAME TO` clause of the statement.\n\n> **Altering table name**\n```sql\nALTER TABLE mytable\nRENAME TO new_table_name;\n```\n\n### Other changes\n\nEach database implementation supports different methods of altering their tables, so it's always best to \nconsult your database docs before proceeding: \nMySQL,\nPostgres,\nSQLite,\nMicrosoft SQL Server.\n\n### Exercise\n\nOur exercises use an implementation that only support adding new columns, so give that a try below.",
          "lessonContent": {
            "textAndVisuals": {
              "content": "As your data changes over time, SQL provides a way for you to update your corresponding tables and \ndatabase schemas by using the `ALTER TABLE` statement to add, remove, or modify columns and table\nconstraints.\n\n### Adding columns\n\nThe syntax for adding a new column is similar to the syntax when creating new rows in the `CREATE TABLE`\nstatement.  You need to specify the data type of the column along with any potential table constraints \nand default values to be applied to both existing *and* new rows.  In some databases like MySQL, \nyou can even specify where to insert the new column using the `FIRST` or `AFTER` clauses, though this \nis not a standard feature.\n\n> **Altering table to add new column(s)**\n```sql\nALTER TABLE mytable\nADD column *DataType* *OptionalTableConstraint* \n    DEFAULT default_value;\n```\n\n### Removing columns\n\nDropping columns is as easy as specifying the column to drop, however, some databases (including SQLite) \ndon't support this feature.  Instead you may have to create a new table and migrate the data over.\n\n> **Altering table to remove column(s)**\n```sql\nALTER TABLE mytable\nDROP column_to_be_deleted;\n```\n\n### Renaming the table\n\nIf you need to rename the table itself, you can also do that using the `RENAME TO` clause of the statement.\n\n> **Altering table name**\n```sql\nALTER TABLE mytable\nRENAME TO new_table_name;\n```\n\n### Other changes\n\nEach database implementation supports different methods of altering their tables, so it's always best to \nconsult your database docs before proceeding: \nMySQL,\nPostgres,\nSQLite,\nMicrosoft SQL Server.\n\n### Exercise\n\nOur exercises use an implementation that only support adding new columns, so give that a try below."
            }
          },
          "sqlExercise": {
            "schemaSql": "CREATE TABLE movies (\n    id integer primary key autoincrement,\n    title text,\n    director text,\n    year integer, \n    length_minutes integer\n);",
            "seedSql": "INSERT INTO movies (id, title, director, year, length_minutes) VALUES\n(1, 'Toy Story', 'John Lasseter', 1995, 81),\n(2, 'A Bug''s Life', 'John Lasseter', 1998, 95),\n(3, 'Toy Story 2', 'John Lasseter', 1999, 93),\n(4, 'Monsters, Inc.', 'Pete Docter', 2001, 92),\n(5, 'Finding Nemo', 'Andrew Stanton', 2003, 107),\n(6, 'The Incredibles', 'Brad Bird', 2004, 116),\n(7, 'Cars', 'John Lasseter', 2006, 117),\n(8, 'Ratatouille', 'Brad Bird', 2007, 115),\n(9, 'WALL-E', 'Andrew Stanton', 2008, 104),\n(10, 'Up', 'Pete Docter', 2009, 101),\n(11, 'Toy Story 3', 'Lee Unkrich', 2010, 103),\n(12, 'Cars 2', 'John Lasseter', 2011, 120),\n(13, 'Brave', 'Brenda Chapman', 2012, 102),\n(14, 'Monsters University', 'Dan Scanlon', 2013, 110);\n",
            "defaultTable": "movies",
            "defaultQuery": "-- Write your ALTER TABLE statement here\n",
            "tasks": [
              {
                "instruction": "Add a column named Aspect_ratio with a FLOAT data type to store the aspect-ratio each movie was released in.",
                "expectedQuery": "ALTER TABLE Movies\n  ADD COLUMN Aspect_ratio FLOAT DEFAULT 2.39;",
                "solution": "ALTER TABLE Movies\n  ADD COLUMN Aspect_ratio FLOAT DEFAULT 2.39;",
                "orderSensitive": false
              },
              {
                "instruction": "Add another column named Language with a TEXT data type to store the language that the movie was released in. Ensure that the default for this language is English .",
                "expectedQuery": "ALTER TABLE Movies\n  ADD COLUMN Language TEXT DEFAULT \"English\";",
                "solution": "ALTER TABLE Movies\n  ADD COLUMN Language TEXT DEFAULT \"English\";",
                "orderSensitive": false
              }
            ]
          }
        },
        {
          "topicId": "sql-m4-t6",
          "title": "SQL Lesson 18: Dropping tables",
          "description": "Hands-on SQL exercise: SQL Lesson 18: Dropping tables. Learn syntax, inspect live SQLite tables, and validate queries in real-time.",
          "mode": "SQL_INTERACTIVE",
          "readingTimeMinutes": 12,
          "lessonMarkdown": "In some rare cases, you may want to remove an entire table including all of its data and metadata, and\nto do so, you can use the `DROP TABLE` statement, which differs from the `DELETE` statement in that it\nalso removes the table schema from the database entirely.\n\n> **Drop table statement**\n```sql\nDROP TABLE IF EXISTS mytable;\n```\n\nLike the `CREATE TABLE` statement, the database may throw an error if the specified table does not exist, \nand to suppress that error, you can use the `IF EXISTS` clause.\nIn addition, if you have another table that is dependent on columns in table you are removing (for example, \nwith a `FOREIGN KEY` dependency) then you will have to either update all dependent tables first to remove \nthe dependent rows or to remove those tables entirely.\n\n### Exercise\n\nWe've reached the end of our exercises, so lets clean up by removing all the tables we've worked with.",
          "lessonContent": {
            "textAndVisuals": {
              "content": "In some rare cases, you may want to remove an entire table including all of its data and metadata, and\nto do so, you can use the `DROP TABLE` statement, which differs from the `DELETE` statement in that it\nalso removes the table schema from the database entirely.\n\n> **Drop table statement**\n```sql\nDROP TABLE IF EXISTS mytable;\n```\n\nLike the `CREATE TABLE` statement, the database may throw an error if the specified table does not exist, \nand to suppress that error, you can use the `IF EXISTS` clause.\nIn addition, if you have another table that is dependent on columns in table you are removing (for example, \nwith a `FOREIGN KEY` dependency) then you will have to either update all dependent tables first to remove \nthe dependent rows or to remove those tables entirely.\n\n### Exercise\n\nWe've reached the end of our exercises, so lets clean up by removing all the tables we've worked with."
            }
          },
          "sqlExercise": {
            "schemaSql": "CREATE TABLE movies (\n    id integer primary key autoincrement,\n    title text,\n    director text,\n    year integer, \n    length_minutes integer\n);",
            "seedSql": "INSERT INTO movies (id, title, director, year, length_minutes) VALUES\n(1, 'Toy Story', 'John Lasseter', 1995, 81),\n(2, 'A Bug''s Life', 'John Lasseter', 1998, 95),\n(3, 'Toy Story 2', 'John Lasseter', 1999, 93),\n(4, 'Monsters, Inc.', 'Pete Docter', 2001, 92),\n(5, 'Finding Nemo', 'Andrew Stanton', 2003, 107),\n(6, 'The Incredibles', 'Brad Bird', 2004, 116),\n(7, 'Cars', 'John Lasseter', 2006, 117),\n(8, 'Ratatouille', 'Brad Bird', 2007, 115),\n(9, 'WALL-E', 'Andrew Stanton', 2008, 104),\n(10, 'Up', 'Pete Docter', 2009, 101),\n(11, 'Toy Story 3', 'Lee Unkrich', 2010, 103),\n(12, 'Cars 2', 'John Lasseter', 2011, 120),\n(13, 'Brave', 'Brenda Chapman', 2012, 102),\n(14, 'Monsters University', 'Dan Scanlon', 2013, 110);\n",
            "defaultTable": "movies",
            "defaultQuery": "-- Write your DROP TABLE statement here\n",
            "tasks": [
              {
                "instruction": "We've sadly reached the end of our lessons, lets clean up by removing the Movies table",
                "expectedQuery": "DROP TABLE Movies;",
                "solution": "DROP TABLE Movies;",
                "orderSensitive": false
              },
              {
                "instruction": "And drop the BoxOffice table as well",
                "expectedQuery": "DROP TABLE BoxOffice;",
                "solution": "DROP TABLE BoxOffice;",
                "orderSensitive": false
              }
            ]
          }
        }
      ],
      "msa": {
        "title": "Module 4 Assessment: DDL & DML Schema Administration",
        "passPercent": 85,
        "durationMinutes": 20,
        "mcqSection": {
          "sectionTitle": "Section 1: Database Mutations & Table Schema MCQs",
          "durationMinutes": 20,
          "passCutoffPercent": 85,
          "questions": [
            {
              "questionId": "m4-msa-q1",
              "question": "What happens if you execute an UPDATE statement without a WHERE clause?",
              "prompt": "What happens if you execute an UPDATE statement without a WHERE clause?",
              "options": [
                "The query will throw a syntax error.",
                "Every single row in the table will be modified with the new values.",
                "Only the first row in the table will be updated.",
                "The table schema will be dropped."
              ],
              "correctAnswer": 1,
              "correctIndex": 1,
              "correctOptionIndex": 1,
              "explanation": "Without a WHERE constraint, UPDATE modifies all records across the entire table."
            },
            {
              "questionId": "m4-msa-q2",
              "question": "Which SQL command is used to add a new column named status to an existing students table?",
              "prompt": "Which SQL command is used to add a new column named status to an existing students table?",
              "options": [
                "INSERT INTO students COLUMN status VARCHAR(20);",
                "ALTER TABLE students ADD COLUMN status VARCHAR(20);",
                "MODIFY TABLE students ADD status VARCHAR(20);",
                "UPDATE students CREATE COLUMN status VARCHAR(20);"
              ],
              "correctAnswer": 1,
              "correctIndex": 1,
              "correctOptionIndex": 1,
              "explanation": "ALTER TABLE ... ADD COLUMN is the standard DDL command to append new attributes."
            },
            {
              "questionId": "m4-msa-q3",
              "question": "What is the primary difference between DELETE FROM table; and DROP TABLE table;?",
              "prompt": "What is the primary difference between DELETE FROM table; and DROP TABLE table;?",
              "options": [
                "DELETE removes all rows but retains the table structure; DROP deletes both the data and the table structure.",
                "DELETE drops the schema, while DROP only deletes rows.",
                "DELETE cannot be used in SQLite.",
                "There is no difference."
              ],
              "correctAnswer": 0,
              "correctIndex": 0,
              "correctOptionIndex": 0,
              "explanation": "DELETE is DML operating on table content; DROP is DDL destroying the table definition entirely."
            },
            {
              "questionId": "m4-msa-q4",
              "question": "Which keyword in a CREATE TABLE statement prevents two rows from having the same value in a specific column and enforces fast indexed lookups?",
              "prompt": "Which keyword in a CREATE TABLE statement prevents two rows from having the same value in a specific column and enforces fast indexed lookups?",
              "options": [
                "CHECK",
                "PRIMARY KEY",
                "CASCADE",
                "FOREIGN KEY"
              ],
              "correctAnswer": 1,
              "correctIndex": 1,
              "correctOptionIndex": 1,
              "explanation": "A PRIMARY KEY uniquely identifies each record in a relational database table and enforces uniqueness."
            },
            {
              "questionId": "m4-msa-q5",
              "question": "What is the primary difference between `DELETE FROM table;` and `TRUNCATE TABLE table;`?",
              "prompt": "What is the primary difference between `DELETE FROM table;` and `TRUNCATE TABLE table;`?",
              "options": [
                "`DELETE` is a DML statement that removes rows one by one and can be filtered with WHERE; `TRUNCATE` is a DDL statement that deallocates data pages rapidly and cannot have a WHERE clause.",
                "`TRUNCATE` deletes the table schema from the database permanently.",
                "`DELETE` cannot be rolled back in a transaction, whereas `TRUNCATE` always can.",
                "There is no difference; they are exact aliases."
              ],
              "correctAnswer": 0,
              "correctIndex": 0,
              "correctOptionIndex": 0,
              "explanation": "DELETE scans rows, logs changes, can take a WHERE clause, and triggers row-level delete triggers. TRUNCATE is DDL that resets the table rapidly by deallocating pages and cannot be filtered with WHERE."
            },
            {
              "questionId": "m4-msa-q6",
              "question": "What constraint ensures that all values in a column are distinct and not null, automatically acting as the entity identifier?",
              "prompt": "What constraint ensures that all values in a column are distinct and not null, automatically acting as the entity identifier?",
              "options": [
                "FOREIGN KEY",
                "PRIMARY KEY",
                "CHECK",
                "DEFAULT"
              ],
              "correctAnswer": 1,
              "correctIndex": 1,
              "correctOptionIndex": 1,
              "explanation": "A PRIMARY KEY uniquely identifies each row in a table. It implicitly enforces both UNIQUE and NOT NULL constraints."
            },
            {
              "questionId": "m4-msa-q7",
              "question": "Which statement adds a new column `email VARCHAR(255)` to an existing `users` table?",
              "prompt": "Which statement adds a new column `email VARCHAR(255)` to an existing `users` table?",
              "options": [
                "UPDATE TABLE users ADD email VARCHAR(255);",
                "ALTER TABLE users ADD COLUMN email VARCHAR(255);",
                "INSERT INTO users COLUMN email VARCHAR(255);",
                "MODIFY TABLE users CREATE email VARCHAR(255);"
              ],
              "correctAnswer": 1,
              "correctIndex": 1,
              "correctOptionIndex": 1,
              "explanation": "The `ALTER TABLE <table_name> ADD COLUMN <col_name> <data_type>` statement modifies an existing schema by appending a new attribute."
            },
            {
              "questionId": "m4-msa-q8",
              "question": "What is the effect of the `ON DELETE CASCADE` option on a FOREIGN KEY reference?",
              "prompt": "What is the effect of the `ON DELETE CASCADE` option on a FOREIGN KEY reference?",
              "options": [
                "Prevents deletion of the parent row if child records exist",
                "Automatically deletes child rows when the referenced parent row is deleted",
                "Sets the foreign key column in child rows to NULL when the parent is deleted",
                "Archives deleted parent rows in a backup audit table"
              ],
              "correctAnswer": 1,
              "correctIndex": 1,
              "correctOptionIndex": 1,
              "explanation": "ON DELETE CASCADE guarantees referential integrity by automatically deleting dependent records in the referencing (child) table when the referenced (parent) record is deleted."
            },
            {
              "questionId": "m4-msa-q9",
              "question": "Which constraint validates that values inserted into a column satisfy a boolean expression (e.g. `age >= 18`)?",
              "prompt": "Which constraint validates that values inserted into a column satisfy a boolean expression (e.g. `age >= 18`)?",
              "options": [
                "CHECK constraint",
                "ASSERT constraint",
                "VALIDATE constraint",
                "FILTER constraint"
              ],
              "correctAnswer": 0,
              "correctIndex": 0,
              "correctOptionIndex": 0,
              "explanation": "A CHECK constraint enforces domain integrity by limiting values to those that satisfy a specified boolean predicate."
            },
            {
              "questionId": "m4-msa-q10",
              "question": "How do you insert multiple records into a table using a single `INSERT` statement?",
              "prompt": "How do you insert multiple records into a table using a single `INSERT` statement?",
              "options": [
                "INSERT INTO table (col1) VALUES (val1) AND (val2) AND (val3);",
                "INSERT INTO table (col1) VALUES (val1), (val2), (val3);",
                "INSERT ALL INTO table (col1) VALUES (val1), (val2);",
                "REPEAT INSERT INTO table (col1) VALUES (val1), (val2);"
              ],
              "correctAnswer": 1,
              "correctIndex": 1,
              "correctOptionIndex": 1,
              "explanation": "Standard multi-row INSERT syntax separates each tuple of values with a comma: `VALUES (v1), (v2), (v3)`."
            },
            {
              "questionId": "m4-msa-q11",
              "question": "What is the consequence of executing `DROP TABLE students;`?",
              "prompt": "What is the consequence of executing `DROP TABLE students;`?",
              "options": [
                "All rows are cleared, but the table schema structure remains available for new inserts.",
                "The entire table structure, indexes, and all stored data are permanently removed from the database.",
                "The table is converted into a temporary read-only view.",
                "The table data is moved into the system recycling bin."
              ],
              "correctAnswer": 1,
              "correctIndex": 1,
              "correctOptionIndex": 1,
              "explanation": "DROP TABLE is a DDL command that completely deletes the table definition and all its data and indexes from the database schema."
            }
          ]
        }
      }
    },
    {
      "moduleId": "sql-m5",
      "title": "Module 5: Advanced SQL Patterns & Subqueries",
      "description": "Explore sophisticated subqueries, correlated queries, nested SELECT evaluations, and self-joins from SQLZoo.",
      "estimatedMinutes": 75,
      "topics": [
        {
          "topicId": "sql-m5-t1",
          "title": "SQL Advanced: Subqueries & Nested SELECT",
          "description": "Use a query within another query to evaluate dynamic criteria and compute multi-level comparisons.",
          "mode": "SQL_INTERACTIVE",
          "readingTimeMinutes": 20,
          "lessonMarkdown": "### Subqueries & Nested SELECT Queries\n\nA subquery (or inner query) is a query nested inside a larger SQL statement (outer query). Subqueries can be placed in `WHERE`, `FROM`, or `SELECT` clauses.\n\n> **Basic Subquery in WHERE**\n```sql\nSELECT name, years_employed FROM employees\nWHERE years_employed > (\n  SELECT AVG(years_employed) FROM employees\n);\n```\n\n#### Why Use Subqueries?\n1. **Dynamic Thresholds**: Compare records against calculated metrics (like the average or maximum) without hardcoding values.\n2. **Filtering with IN / NOT IN**: Select records whose IDs exist in another table or filtered list.\n3. **Derived Tables**: Use a subquery in the `FROM` clause as a virtual table.\n\n### Exercise\nWrite queries using the `employees` table below to solve the subquery objectives.",
          "lessonContent": {
            "textAndVisuals": {
              "content": "### Subqueries & Nested SELECT Queries\n\nA subquery (or inner query) is a query nested inside a larger SQL statement (outer query). Subqueries can be placed in `WHERE`, `FROM`, or `SELECT` clauses.\n\n> **Basic Subquery in WHERE**\n```sql\nSELECT name, years_employed FROM employees\nWHERE years_employed > (\n  SELECT AVG(years_employed) FROM employees\n);\n```\n\n#### Why Use Subqueries?\n1. **Dynamic Thresholds**: Compare records against calculated metrics (like the average or maximum) without hardcoding values.\n2. **Filtering with IN / NOT IN**: Select records whose IDs exist in another table or filtered list.\n3. **Derived Tables**: Use a subquery in the `FROM` clause as a virtual table.\n\n### Exercise\nWrite queries using the `employees` table below to solve the subquery objectives."
            }
          },
          "sqlExercise": {
            "schemaSql": "CREATE TABLE employees (\n    -- id integer primary key autoincrement,\n    role text,\n    name text,\n    building text,\n    years_employed integer\n);",
            "seedSql": "INSERT INTO employees (role, name, building, years_employed) VALUES\n('Engineer', 'Becky A.', '1e', 4),\n('Engineer', 'Dan B.', '1e', 2),\n('Engineer', 'Sharon F.', '1e', 6),\n('Engineer', 'Dan M.', '1e', 4),\n('Engineer', 'Malcom S.', '1e', 1),\n('Artist', 'Tylar S.', '2w', 2),\n('Artist', 'Sherman D.', '2w', 8),\n('Artist', 'Jakob J.', '2w', 6),\n('Artist', 'Lillia A.', '2w', 7),\n('Artist', 'Brandon J.', '2w', 7),\n('Manager', 'Scott K.', '1e', 9),\n('Manager', 'Shirlee M.', '1e', 3),\n('Manager', 'Daria O.', '2w', 6);\n",
            "defaultTable": "employees",
            "defaultQuery": "-- Write a query using a subquery\nSELECT name, years_employed FROM employees;",
            "tasks": [
              {
                "instruction": "Find the name and role of employees who have been employed for longer than the average years employed across all employees.",
                "expectedQuery": "SELECT name, role FROM employees WHERE years_employed > (SELECT AVG(years_employed) FROM employees);",
                "solution": "SELECT name, role FROM employees WHERE years_employed > (SELECT AVG(years_employed) FROM employees);"
              },
              {
                "instruction": "Find the employees who work in the same building as Malcolm S. (using a subquery).",
                "expectedQuery": "SELECT name, building FROM employees WHERE building = (SELECT building FROM employees WHERE name = 'Malcolm S.');",
                "solution": "SELECT name, building FROM employees WHERE building = (SELECT building FROM employees WHERE name = 'Malcolm S.');"
              }
            ]
          }
        },
        {
          "topicId": "sql-m5-t2",
          "title": "SQL Advanced: Self-Joins & Hierarchical Data",
          "description": "Join a table to itself to analyze peer-to-peer relationships, hierarchies, and multi-hop connections.",
          "mode": "SQL_INTERACTIVE",
          "readingTimeMinutes": 20,
          "lessonMarkdown": "### Self-Joins in Relational Databases\n\nA self-join is a regular join, but the table is joined with itself. Self-joins are essential when rows within the same table reference each other (e.g. employees and their managers, or bus routes sharing a stop).\n\n> **Self-Join Syntax**\n```sql\nSELECT A.name AS Employee, B.name AS Colleague\nFROM employees A\nJOIN employees B ON A.building = B.building AND A.name != B.name;\n```\n\n#### Key Rules for Self-Joins:\n- You must use **table aliases** (`A`, `B`) to distinguish between the two copies of the same table.\n- Ensure you avoid matching rows to themselves by adding conditions like `A.name != B.name` or `A.id < B.id`.\n\n### Exercise\nExplore peer relationships in the `employees` dataset using self-joins.",
          "lessonContent": {
            "textAndVisuals": {
              "content": "### Self-Joins in Relational Databases\n\nA self-join is a regular join, but the table is joined with itself. Self-joins are essential when rows within the same table reference each other (e.g. employees and their managers, or bus routes sharing a stop).\n\n> **Self-Join Syntax**\n```sql\nSELECT A.name AS Employee, B.name AS Colleague\nFROM employees A\nJOIN employees B ON A.building = B.building AND A.name != B.name;\n```\n\n#### Key Rules for Self-Joins:\n- You must use **table aliases** (`A`, `B`) to distinguish between the two copies of the same table.\n- Ensure you avoid matching rows to themselves by adding conditions like `A.name != B.name` or `A.id < B.id`.\n\n### Exercise\nExplore peer relationships in the `employees` dataset using self-joins."
            }
          },
          "sqlExercise": {
            "schemaSql": "CREATE TABLE employees (\n    -- id integer primary key autoincrement,\n    role text,\n    name text,\n    building text,\n    years_employed integer\n);",
            "seedSql": "INSERT INTO employees (role, name, building, years_employed) VALUES\n('Engineer', 'Becky A.', '1e', 4),\n('Engineer', 'Dan B.', '1e', 2),\n('Engineer', 'Sharon F.', '1e', 6),\n('Engineer', 'Dan M.', '1e', 4),\n('Engineer', 'Malcom S.', '1e', 1),\n('Artist', 'Tylar S.', '2w', 2),\n('Artist', 'Sherman D.', '2w', 8),\n('Artist', 'Jakob J.', '2w', 6),\n('Artist', 'Lillia A.', '2w', 7),\n('Artist', 'Brandon J.', '2w', 7),\n('Manager', 'Scott K.', '1e', 9),\n('Manager', 'Shirlee M.', '1e', 3),\n('Manager', 'Daria O.', '2w', 6);\n",
            "defaultTable": "employees",
            "defaultQuery": "-- Write a self-join query\nSELECT A.name, B.name FROM employees A JOIN employees B ON A.building = B.building;",
            "tasks": [
              {
                "instruction": "Find all pairs of employees who work in the same building (show employee name and colleague name, avoiding duplicate reversed pairs).",
                "expectedQuery": "SELECT A.name, B.name FROM employees A JOIN employees B ON A.building = B.building AND A.name < B.name;",
                "solution": "SELECT A.name, B.name FROM employees A JOIN employees B ON A.building = B.building AND A.name < B.name;"
              },
              {
                "instruction": "Find all pairs of employees who have the exact same role and years employed.",
                "expectedQuery": "SELECT A.name, B.name, A.role FROM employees A JOIN employees B ON A.role = B.role AND A.years_employed = B.years_employed AND A.name < B.name;",
                "solution": "SELECT A.name, B.name, A.role FROM employees A JOIN employees B ON A.role = B.role AND A.years_employed = B.years_employed AND A.name < B.name;"
              }
            ]
          }
        }
      ],
      "msa": {
        "title": "Module 5 Assessment: Subqueries & Advanced SQL Patterns (SQLZoo)",
        "passPercent": 85,
        "durationMinutes": 25,
        "mcqSection": {
          "sectionTitle": "Section 1: SQLZoo Nested Subqueries & Self-Joins MCQs",
          "durationMinutes": 25,
          "passCutoffPercent": 85,
          "questions": [
            {
              "questionId": "nested_select_quiz-q1",
              "question": "Select the code that shows the name, region and population of the smallest country in each region",
              "prompt": "Select the code that shows the name, region and population of the smallest country in each region",
              "options": [
                "SELECT region , name , FROM bbc x WHERE population <= ALL ( SELECT population FROM bbc y WHERE y . region = x . region AND population > 0 )",
                "SELECT region , name , population FROM bbc WHERE population <= ALL ( SELECT population FROM bbc WHERE population > 0 )",
                "SELECT region , name , population FROM bbc x WHERE population <= ALL ( SELECT population FROM bbc y WHERE y . region = x . region AND population > 0 )",
                "SELECT region , name , population FROM bbc x WHERE population = ALL ( SELECT population FROM bbc y WHERE y . region = x . region AND population > 0 )"
              ],
              "correctAnswer": 2,
              "correctIndex": 2,
              "correctOptionIndex": 2,
              "explanation": "Correct SQL Syntax/Output: SELECT region , name , population FROM bbc x WHERE population <= ALL ( SELECT population FROM bbc y WHERE y . region = x . region AND population > 0 )"
            },
            {
              "questionId": "nested_select_quiz-q2",
              "question": "Select the code that shows the countries belonging to regions with all populations over 50000",
              "prompt": "Select the code that shows the countries belonging to regions with all populations over 50000",
              "options": [
                "SELECT name , region , population FROM bbc x WHERE 50000 < ALL ( SELECT population FROM bbc y WHERE population > 0 )",
                "SELECT name , region , population FROM bbc x WHERE 50000 < ALL ( SELECT population FROM bbc y WHERE x . region = y . region AND y . population > 0 )",
                "SELECT name , region , population FROM bbc x WHERE 50000 = ALL ( SELECT population FROM bbc y WHERE x . region = y . region AND y . population > 0 )",
                "SELECT name , region , population FROM bbc x WHERE 50000 > ALL ( SELECT population FROM bbc y WHERE x . region = y . region AND y . population > 0 )"
              ],
              "correctAnswer": 1,
              "correctIndex": 1,
              "correctOptionIndex": 1,
              "explanation": "Correct SQL Syntax/Output: SELECT name , region , population FROM bbc x WHERE 50000 < ALL ( SELECT population FROM bbc y WHERE x . region = y . region AND y . population > 0 )"
            },
            {
              "questionId": "nested_select_quiz-q3",
              "question": "Select the code that shows the countries with a less than a third of the population of the countries around it",
              "prompt": "Select the code that shows the countries with a less than a third of the population of the countries around it",
              "options": [
                "SELECT name , region FROM bbc x WHERE population < ALL ( SELECT population / 3 FROM bbc y WHERE y . region = x . region AND y . name != x . name )",
                "SELECT name , region FROM bbc x WHERE population = ALL ( SELECT population / 3 FROM bbc y WHERE y . region = x . region AND y . name != x . name )",
                "SELECT name , region FROM bbc x WHERE population > ALL ( SELECT population / 3 FROM bbc y WHERE y . region = x . region AND y . name != x . name )",
                "SELECT name , region FROM bbc x WHERE population < ALL ( SELECT population * 3 FROM bbc y WHERE y . region = x . region AND y . name != x . name )"
              ],
              "correctAnswer": 0,
              "correctIndex": 0,
              "correctOptionIndex": 0,
              "explanation": "Correct SQL Syntax/Output: SELECT name , region FROM bbc x WHERE population < ALL ( SELECT population / 3 FROM bbc y WHERE y . region = x . region AND y . name != x . name )"
            },
            {
              "questionId": "nested_select_quiz-q5",
              "question": "Select the code that would show the countries with a greater GDP than any country in Africa (some countries may have NULL gdp values).",
              "prompt": "Select the code that would show the countries with a greater GDP than any country in Africa (some countries may have NULL gdp values).",
              "options": [
                "SELECT name FROM bbc WHERE gdp > ALL ( SELECT MAX ( gdp ) FROM bbc WHERE region = 'Africa' AND gdp = 0 )",
                "SELECT name FROM bbc WHERE gdp > ( SELECT MAX ( gdp ) FROM bbc WHERE region = 'Africa' )",
                "SELECT name FROM bbc WHERE gdp > ALL ( SELECT MIN ( gdp ) FROM bbc WHERE region = 'Africa' )",
                "SELECT name FROM bbc WHERE gdp > ALL ( SELECT gdp FROM bbc WHERE region = 'Africa' )"
              ],
              "correctAnswer": 1,
              "correctIndex": 1,
              "correctOptionIndex": 1,
              "explanation": "Correct SQL Syntax/Output: SELECT name FROM bbc WHERE gdp > ( SELECT MAX ( gdp ) FROM bbc WHERE region = 'Africa' )"
            },
            {
              "questionId": "nested_select_quiz-q6",
              "question": "Select the code that shows the countries with population smaller than Russia but bigger than Denmark",
              "prompt": "Select the code that shows the countries with population smaller than Russia but bigger than Denmark",
              "options": [
                "SELECT name FROM bbc WHERE population < ( SELECT population FROM bbc WHERE name = 'Denmark' ) AND population > ( SELECT population FROM bbc WHERE name = 'Russia' )",
                "SELECT name FROM bbc WHERE population < ( SELECT population FROM bbc WHERE name = 'Russia' ) AND population > ( SELECT population FROM bbc WHERE name = 'Denmark' )",
                "SELECT name FROM bbc WHERE population = (SELECT population FROM bbc WHERE name='Russia') AND population > (SELECT population FROM bbc WHERE name='Denmark')",
                "SELECT name FROM bbc WHERE population > (SELECT population FROM bbc WHERE name='Russia') AND population > (SELECT population FROM bbc WHERE name='Denmark')"
              ],
              "correctAnswer": 1,
              "correctIndex": 1,
              "correctOptionIndex": 1,
              "explanation": "Correct SQL Syntax/Output: SELECT name FROM bbc WHERE population < ( SELECT population FROM bbc WHERE name = 'Russia' ) AND population > ( SELECT population FROM bbc WHERE name = 'Denmark' )"
            },
            {
              "questionId": "self_join_quiz-q1",
              "question": "Select the code that would show it is possible to get from Craiglockhart to Haymarket",
              "prompt": "Select the code that would show it is possible to get from Craiglockhart to Haymarket",
              "options": [
                "SELECT DISTINCT a . name , b . name FROM stops a JOIN route z IN a . id = z . stop JOIN route y ON y . num = z . num JOIN stops b IN y . stop = b . id WHERE a . name = 'Craiglockhart' AND b . name = 'Haymarket'",
                "SELECT DISTINCT a . name , b . name FROM stops a JOIN route z ON a . id = z . stop JOIN route y JOIN stops b ON y . stop = b . id WHERE a . name = 'Craiglockhart' AND b . name = 'Haymarket'",
                "SELECT DISTINCT a . name , b . name FROM stops a JOIN route z ON a . id = z . stop JOIN route y ON y . num = z . num JOIN stops b ON y . stop = b . id WHERE a . name = 'Craiglockhart' AND b . name = 'Haymarket'",
                "SELECT DISTINCT a . name , b . name from stops a JOIN route z ON a . id = z . stop JOIN route y ON y . num = z . num JOIN stops b ON y . stop = b . id WHERE a . name = 'Craiglockhart' AND b . name = 'Sighthill'"
              ],
              "correctAnswer": 2,
              "correctIndex": 2,
              "correctOptionIndex": 2,
              "explanation": "Correct SQL Syntax/Output: SELECT DISTINCT a . name , b . name FROM stops a JOIN route z ON a . id = z . stop JOIN route y ON y . num = z . num JOIN stops b ON y . stop = b . id WHERE a . name = 'Craiglockhart' AND b . name = 'Haymarket'"
            },
            {
              "questionId": "self_join_quiz-q2",
              "question": "Select the code that shows the stops that are on route.num '2A' which can be reached with one bus from Haymarket?",
              "prompt": "Select the code that shows the stops that are on route.num '2A' which can be reached with one bus from Haymarket?",
              "options": [
                "SELECT S2 . id , S2 . name , R2 . company , R2 . num FROM stops S1 , stops S2 , route R1 , route R2 WHERE S1 . name = 'Haymarket' AND S1 . id = R1 . stop AND S1 . company = S2 . company AND S1 . num = S2 . num AND R2 . stop = S2 . id AND R1 . num = '2A'",
                "SELECT S2 . id , S2 . name , R2 . company , R2 . num FROM stops S1 , stops S2 , route R1 , route R2 WHERE S1 . name = 'Haymarket' AND S1 . id = R1 . num AND R1 . company = R2 . company AND R1 . num = R2 . num AND R2 . stop = S2 . id AND R2 . num = '2A'",
                "SELECT S2 . id , S2 . name , R2 . company , R2 . num FROM stops S1 , stops S2 , route R1 , route R2 WHERE S1 . name = 'Haymarket' AND S1 . id = R1 . stop AND R1 . company = R2 . company AND R1 . num = R2 . num AND R2 . stop = S2 . id",
                "SELECT S2 . id , S2 . name , R2 . company , R2 . num FROM stops S1 , stops S2 , route R1 , route R2 WHERE S1 . name = 'Haymarket' AND S1 . id = R1 . stop AND R1 . company = R2 . company AND R1 . num = R2 . num AND R2 . stop = S2 . id AND R2 . num = '2'"
              ],
              "correctAnswer": 0,
              "correctIndex": 0,
              "correctOptionIndex": 0,
              "explanation": "Correct SQL Syntax/Output: SELECT S2 . id , S2 . name , R2 . company , R2 . num FROM stops S1 , stops S2 , route R1 , route R2 WHERE S1 . name = 'Haymarket' AND S1 . id = R1 . stop AND S1 . company = S2 . company AND S1 . num = S2 . num AND R2 . stop = S2 . id AND R1 . num = '2A'"
            },
            {
              "questionId": "self_join_quiz-q3",
              "question": "Select the code that shows the services available from Tollcross?",
              "prompt": "Select the code that shows the services available from Tollcross?",
              "options": [
                "SELECT a . company , a . num , stopa . name , stopb . name FROM route a JOIN route b ON ( a . company = b . company AND a . num = b . num ) JOIN stops stopa ON ( a . stop = stopa . id ) JOIN stops stopb ON ( b . stop = stopb . id )",
                "SELECT a . company , a . num , stopa . name , stopb . name FROM route a JOIN route b ON ( a . company = b . company AND a . num = b . num ) JOIN stops stopa ON ( a . stop = stopa . id ) JOIN stops stopb ON ( b . stop = stopb . id ) WHERE stopa . name = 'Sighthill'",
                "SELECT a . company , a . num , stopa . name , stopb . name FROM route a JOIN route b IN ( a . company = b . company AND a . num = b . num ) JOIN stops stopa IN ( a . stop = stopa . id ) JOIN stops stopb ON ( b . stop = stopb . id ) WHERE stopa . name = 'Tollcross'",
                "SELECT a . company , a . num , stopa . name , stopb . name FROM route a JOIN route b ON ( a . company = b . company AND a . num = b . num ) JOIN stops stopa ON ( a . stop = stopa . id ) JOIN stops stopb ON ( b . stop = stopb . id ) WHERE stopa . name = 'Tollcross'"
              ],
              "correctAnswer": 3,
              "correctIndex": 3,
              "correctOptionIndex": 3,
              "explanation": "Correct SQL Syntax/Output: SELECT a . company , a . num , stopa . name , stopb . name FROM route a JOIN route b ON ( a . company = b . company AND a . num = b . num ) JOIN stops stopa ON ( a . stop = stopa . id ) JOIN stops stopb ON ( b . stop = stopb . id ) WHERE stopa . name = 'Tollcross'"
            },
            {
              "questionId": "m5-msa-q9",
              "question": "What distinguishes a correlated subquery from a standard (independent) subquery?",
              "prompt": "What distinguishes a correlated subquery from a standard (independent) subquery?",
              "options": [
                "A correlated subquery executes once before the outer query runs.",
                "A correlated subquery references columns from the outer query and must be evaluated once for each candidate row of the outer query.",
                "A correlated subquery can only return numerical values.",
                "A correlated subquery cannot contain a WHERE clause."
              ],
              "correctAnswer": 1,
              "correctIndex": 1,
              "correctOptionIndex": 1,
              "explanation": "A correlated subquery depends on values from the outer query row (e.g. `WHERE y.department_id = x.department_id`). It re-evaluates for each row considered by the outer query."
            },
            {
              "questionId": "m5-msa-q10",
              "question": "What is the primary difference between `UNION` and `UNION ALL`?",
              "prompt": "What is the primary difference between `UNION` and `UNION ALL`?",
              "options": [
                "`UNION` combines results and removes duplicate rows; `UNION ALL` retains all rows including duplicates and executes faster.",
                "`UNION ALL` removes duplicates, while `UNION` retains them.",
                "`UNION` works across different databases, while `UNION ALL` works only on single tables.",
                "`UNION` merges columns horizontally, while `UNION ALL` stacks rows vertically."
              ],
              "correctAnswer": 0,
              "correctIndex": 0,
              "correctOptionIndex": 0,
              "explanation": "UNION performs a distinct sort operation to remove duplicate rows from the combined result sets. UNION ALL simply concatenates the result sets without deduplication, making it significantly faster."
            },
            {
              "questionId": "m5-msa-q11",
              "question": "How does the `EXISTS` operator evaluate a subquery?",
              "prompt": "How does the `EXISTS` operator evaluate a subquery?",
              "options": [
                "It checks if the subquery returns at least one row, stopping evaluation as soon as a match is found (short-circuiting).",
                "It verifies that all rows in the subquery match the outer column value.",
                "It counts the number of rows returned and compares it to zero.",
                "It requires the subquery to return a boolean TRUE or FALSE value."
              ],
              "correctAnswer": 0,
              "correctIndex": 0,
              "correctOptionIndex": 0,
              "explanation": "EXISTS tests for the existence of any rows in its subquery. Because it short-circuits as soon as the first matching row is discovered, it is often more efficient than `IN` for large datasets."
            },
            {
              "questionId": "m5-msa-q12",
              "question": "What is the output of the following `CASE` expression if `score = 85`? \n```sql\nCASE \n  WHEN score >= 90 THEN 'A'\n  WHEN score >= 80 THEN 'B'\n  WHEN score >= 70 THEN 'C'\n  ELSE 'F'\nEND\n```",
              "prompt": "What is the output of the following `CASE` expression if `score = 85`? \n```sql\nCASE \n  WHEN score >= 90 THEN 'A'\n  WHEN score >= 80 THEN 'B'\n  WHEN score >= 70 THEN 'C'\n  ELSE 'F'\nEND\n```",
              "options": [
                "'A'",
                "'B'",
                "'C'",
                "'B' and 'C'"
              ],
              "correctAnswer": 1,
              "correctIndex": 1,
              "correctOptionIndex": 1,
              "explanation": "SQL CASE expressions evaluate branches sequentially and return the result for the first condition that evaluates to TRUE. For 85, `score >= 80` is the first match, returning 'B'."
            },
            {
              "questionId": "m5-msa-q13",
              "question": "What does the expression `> ALL (SELECT salary FROM employees WHERE department = 'Sales')` mean?",
              "prompt": "What does the expression `> ALL (SELECT salary FROM employees WHERE department = 'Sales')` mean?",
              "options": [
                "The value must be greater than at least one salary in Sales (i.e. greater than the minimum).",
                "The value must be greater than every single salary in Sales (i.e. greater than the maximum).",
                "The value must equal the sum of all salaries in Sales.",
                "The value must equal the average salary in Sales."
              ],
              "correctAnswer": 1,
              "correctIndex": 1,
              "correctOptionIndex": 1,
              "explanation": "'> ALL (...)' means the comparison must hold true against every value returned by the subquery, meaning it must exceed the maximum value in that set."
            },
            {
              "questionId": "m5-msa-q14",
              "question": "In SQL window functions, what does `ROW_NUMBER() OVER (PARTITION BY department_id ORDER BY salary DESC)` calculate?",
              "prompt": "In SQL window functions, what does `ROW_NUMBER() OVER (PARTITION BY department_id ORDER BY salary DESC)` calculate?",
              "options": [
                "A sequential row count over the entire table regardless of department",
                "A sequential integer ranking (1, 2, 3...) restarting at 1 for each department, ordered by highest salary first",
                "The total count of employees in each department",
                "The sum of salaries within each department"
              ],
              "correctAnswer": 1,
              "correctIndex": 1,
              "correctOptionIndex": 1,
              "explanation": "PARTITION BY divides the result set into distinct partitions (groups) where window calculations occur independently, and ORDER BY specifies the sorting order within each partition."
            },
            {
              "questionId": "m5-msa-q15",
              "question": "What requirements must be met to combine two queries with `UNION`?",
              "prompt": "What requirements must be met to combine two queries with `UNION`?",
              "options": [
                "Both queries must select from the same table.",
                "Both queries must select the same number of columns with compatible data types in corresponding positions.",
                "Both queries must have identical WHERE clauses.",
                "The tables must have matching primary keys."
              ],
              "correctAnswer": 1,
              "correctIndex": 1,
              "correctOptionIndex": 1,
              "explanation": "Set operations (UNION, INTERSECT, EXCEPT) require that each SELECT list contains the same number of columns in corresponding order with compatible data types."
            }
          ]
        }
      }
    },
    {
      "moduleId": "sql-m6",
      "title": "Module 6: SQL & Database Fundamentals",
      "description": "Build the foundation behind SQL by learning what data, databases, DBMS, and RDBMS are, how database models differ, how relational keys connect tables, how SQL command families work, and how constraints, transactions, ACID, and normalization support reliable data systems.",
      "estimatedMinutes": 180,
      "topics": [
        {
          "topicId": "sql-m6-t1",
          "title": "Database, Data and DBMS Basics",
          "description": "Foundation lesson: Database, Data and DBMS Basics. Build clear conceptual understanding and reinforce it with practical SQL exercises.",
          "mode": "SQL_INTERACTIVE",
          "readingTimeMinutes": 30,
          "lessonMarkdown": "## 1. What is data?\n\n**Data** is information stored in a form that a computer can record, process, and retrieve. Examples include customer names, product prices, student marks, order dates, and account balances.\n\n## 2. What is a database?\n\nA **database** is an organized collection of related data. The goal is not simply to store information, but to make it possible to retrieve, update, protect, and manage that information reliably.\n\nA database may contain tables, indexes, views, constraints, metadata, and other objects depending on the database system.\n\n## 3. What is a DBMS?\n\nA **Database Management System (DBMS)** is software that allows applications and users to create, store, retrieve, update, secure, and manage data in databases.\n\nA DBMS commonly provides:\n- Data storage and retrieval\n- Query processing\n- Transactions and recovery\n- Access control and security\n- Constraints and integrity checks\n- Backup and administration facilities\n- Concurrency management\n\n### Database vs DBMS\n\nA **database** is the data being managed. A **DBMS** is the software that manages that data.\n\n### Examples\n\nCommon DBMS products include PostgreSQL, MySQL, MariaDB, Oracle Database, Microsoft SQL Server, and SQLite. They differ in capabilities and architecture, but all provide mechanisms for working with stored data.\n\n### Key idea\n\nThink of a database as the organized information and a DBMS as the system that manages that information. SQL is one of the main ways to communicate with relational databases.",
          "lessonContent": {
            "textAndVisuals": {
              "content": "## 1. What is data?\n\n**Data** is information stored in a form that a computer can record, process, and retrieve. Examples include customer names, product prices, student marks, order dates, and account balances.\n\n## 2. What is a database?\n\nA **database** is an organized collection of related data. The goal is not simply to store information, but to make it possible to retrieve, update, protect, and manage that information reliably.\n\nA database may contain tables, indexes, views, constraints, metadata, and other objects depending on the database system.\n\n## 3. What is a DBMS?\n\nA **Database Management System (DBMS)** is software that allows applications and users to create, store, retrieve, update, secure, and manage data in databases.\n\nA DBMS commonly provides:\n- Data storage and retrieval\n- Query processing\n- Transactions and recovery\n- Access control and security\n- Constraints and integrity checks\n- Backup and administration facilities\n- Concurrency management\n\n### Database vs DBMS\n\nA **database** is the data being managed. A **DBMS** is the software that manages that data.\n\n### Examples\n\nCommon DBMS products include PostgreSQL, MySQL, MariaDB, Oracle Database, Microsoft SQL Server, and SQLite. They differ in capabilities and architecture, but all provide mechanisms for working with stored data.\n\n### Key idea\n\nThink of a database as the organized information and a DBMS as the system that manages that information. SQL is one of the main ways to communicate with relational databases."
            }
          },
          "sqlExercise": {
            "schemaSql": "CREATE TABLE departments (\n    department_id INTEGER PRIMARY KEY,\n    department_name TEXT NOT NULL\n);\nCREATE TABLE employees (\n    employee_id INTEGER PRIMARY KEY,\n    employee_name TEXT NOT NULL,\n    department_id INTEGER\n);",
            "seedSql": "INSERT INTO departments (department_id, department_name) VALUES\n(1, 'Engineering'),\n(2, 'Finance'),\n(3, 'Human Resources');\nINSERT INTO employees (employee_id, employee_name, department_id) VALUES\n(101, 'Anita', 1),\n(102, 'Bala', 1),\n(103, 'Chen', 2),\n(104, 'Divya', NULL);",
            "defaultTable": "departments",
            "defaultQuery": "SELECT * FROM departments;",
            "tasks": [
              {
                "instruction": "List all departments and their IDs.",
                "expectedQuery": "SELECT department_id, department_name FROM departments ORDER BY department_id ASC;",
                "solution": "SELECT department_id, department_name FROM departments ORDER BY department_id ASC;",
                "orderSensitive": false
              },
              {
                "instruction": "List all employees who belong to the Engineering department.",
                "expectedQuery": "SELECT employee_name FROM employees WHERE department_id = 1 ORDER BY employee_id ASC;",
                "solution": "SELECT employee_name FROM employees WHERE department_id = 1 ORDER BY employee_id ASC;",
                "orderSensitive": false
              },
              {
                "instruction": "Count the number of employees stored in the employees table.",
                "expectedQuery": "SELECT COUNT(*) AS employee_count FROM employees;",
                "solution": "SELECT COUNT(*) AS employee_count FROM employees;",
                "orderSensitive": false
              }
            ]
          }
        },
        {
          "topicId": "sql-m6-t2",
          "title": "DBMS Types and RDBMS",
          "description": "Foundation lesson: DBMS Types and RDBMS. Build clear conceptual understanding and reinforce it with practical SQL exercises.",
          "mode": "SQL_INTERACTIVE",
          "readingTimeMinutes": 35,
          "lessonMarkdown": "## Why different database models exist\n\nDifferent applications have different data shapes and access patterns. Database systems are therefore built using different models.\n\n### Common database models\n\n**Hierarchical databases** organize records in a tree-like parent/child structure. They are useful when relationships naturally form a hierarchy.\n\n**Network databases** extend hierarchical ideas to support richer links between records, including many-to-many-style relationships.\n\n**Relational databases** store data in tables made of rows and columns. Relationships are represented using keys, and SQL is the dominant query language.\n\n**Object-oriented databases** represent persistent data using objects and object-oriented concepts.\n\n**Document databases** store semi-structured documents, commonly in JSON-like formats.\n\n**Key-value databases** store values associated with unique keys and are often used for fast lookups.\n\n**Graph databases** represent entities as nodes and relationships as edges, making relationship-heavy queries a core use case.\n\n## DBMS vs RDBMS\n\nA **DBMS** is the broad category of software used to manage databases. An **RDBMS (Relational Database Management System)** specifically implements the relational model, where information is organized into related relations (tables).\n\nImportant relational ideas include:\n- Tables (relations)\n- Rows (tuples/records)\n- Columns (attributes)\n- Primary keys\n- Foreign keys\n- Integrity constraints\n- Set-based queries\n\n### Important distinction\n\nNot every database system is relational, and not every data store uses SQL. SQL is strongly associated with relational systems, while many non-relational systems use different query APIs or languages.",
          "lessonContent": {
            "textAndVisuals": {
              "content": "## Why different database models exist\n\nDifferent applications have different data shapes and access patterns. Database systems are therefore built using different models.\n\n### Common database models\n\n**Hierarchical databases** organize records in a tree-like parent/child structure. They are useful when relationships naturally form a hierarchy.\n\n**Network databases** extend hierarchical ideas to support richer links between records, including many-to-many-style relationships.\n\n**Relational databases** store data in tables made of rows and columns. Relationships are represented using keys, and SQL is the dominant query language.\n\n**Object-oriented databases** represent persistent data using objects and object-oriented concepts.\n\n**Document databases** store semi-structured documents, commonly in JSON-like formats.\n\n**Key-value databases** store values associated with unique keys and are often used for fast lookups.\n\n**Graph databases** represent entities as nodes and relationships as edges, making relationship-heavy queries a core use case.\n\n## DBMS vs RDBMS\n\nA **DBMS** is the broad category of software used to manage databases. An **RDBMS (Relational Database Management System)** specifically implements the relational model, where information is organized into related relations (tables).\n\nImportant relational ideas include:\n- Tables (relations)\n- Rows (tuples/records)\n- Columns (attributes)\n- Primary keys\n- Foreign keys\n- Integrity constraints\n- Set-based queries\n\n### Important distinction\n\nNot every database system is relational, and not every data store uses SQL. SQL is strongly associated with relational systems, while many non-relational systems use different query APIs or languages."
            }
          },
          "sqlExercise": {
            "schemaSql": "CREATE TABLE customers (\n    customer_id INTEGER PRIMARY KEY,\n    customer_name TEXT NOT NULL\n);\nCREATE TABLE orders (\n    order_id INTEGER PRIMARY KEY,\n    customer_id INTEGER,\n    order_total REAL\n);",
            "seedSql": "INSERT INTO customers (customer_id, customer_name) VALUES\n(1, 'Asha'),\n(2, 'Ben'),\n(3, 'Cara');\nINSERT INTO orders (order_id, customer_id, order_total) VALUES\n(501, 1, 1250.00),\n(502, 1, 500.00),\n(503, 3, 900.00);",
            "defaultTable": "customers",
            "defaultQuery": "SELECT * FROM customers;",
            "tasks": [
              {
                "instruction": "Show each customer and the total value of that customer's orders.",
                "expectedQuery": "SELECT c.customer_name, SUM(o.order_total) AS total_order_value FROM customers AS c JOIN orders AS o ON c.customer_id = o.customer_id GROUP BY c.customer_id, c.customer_name ORDER BY c.customer_id ASC;",
                "solution": "SELECT c.customer_name, SUM(o.order_total) AS total_order_value FROM customers AS c JOIN orders AS o ON c.customer_id = o.customer_id GROUP BY c.customer_id, c.customer_name ORDER BY c.customer_id ASC;",
                "orderSensitive": false
              },
              {
                "instruction": "List customers who currently have no orders.",
                "expectedQuery": "SELECT c.customer_name FROM customers AS c LEFT JOIN orders AS o ON c.customer_id = o.customer_id WHERE o.order_id IS NULL ORDER BY c.customer_id ASC;",
                "solution": "SELECT c.customer_name FROM customers AS c LEFT JOIN orders AS o ON c.customer_id = o.customer_id WHERE o.order_id IS NULL ORDER BY c.customer_id ASC;",
                "orderSensitive": false
              },
              {
                "instruction": "List all orders together with the customer name that owns each order.",
                "expectedQuery": "SELECT o.order_id, c.customer_name, o.order_total FROM orders AS o JOIN customers AS c ON o.customer_id = c.customer_id ORDER BY o.order_id ASC;",
                "solution": "SELECT o.order_id, c.customer_name, o.order_total FROM orders AS o JOIN customers AS c ON o.customer_id = c.customer_id ORDER BY o.order_id ASC;",
                "orderSensitive": false
              }
            ]
          }
        },
        {
          "topicId": "sql-m6-t3",
          "title": "Tables, Rows, Columns, Keys and Relationships",
          "description": "Foundation lesson: Tables, Rows, Columns, Keys and Relationships. Build clear conceptual understanding and reinforce it with practical SQL exercises.",
          "mode": "SQL_INTERACTIVE",
          "readingTimeMinutes": 35,
          "lessonMarkdown": "## The relational model\n\nA relational database represents data as **relations (tables)**. A table contains **rows** and **columns**.\n\n- **Table:** a set of related records\n- **Row:** one record/tuple\n- **Column:** one attribute/property shared by records\n- **Schema:** the defined structure of database objects and their relationships\n\n## Keys\n\n### Primary key\n\nA **primary key (PK)** uniquely identifies each row in a table. A primary key cannot contain NULL values, and a table has one primary-key constraint (which may contain multiple columns).\n\n### Foreign key\n\nA **foreign key (FK)** stores values that refer to a key in another table. It helps represent relationships such as customer → orders or department → employees.\n\n### Composite key\n\nA **composite key** uses more than one column together to uniquely identify a row. This is common in relationship tables, such as `student_id + course_id`.\n\n### UNIQUE constraint\n\nA `UNIQUE` constraint prevents duplicate values in the constrained column or column combination. It is different from the primary key because it is not the table's primary identifier.\n\n## Relationships\n\n- **One-to-one:** one row corresponds to at most one row in the other table.\n- **One-to-many:** one parent row can relate to many child rows.\n- **Many-to-many:** many rows on both sides relate to many rows on the other side, usually implemented with a junction/bridge table.\n\n### Example\n\nA student can enroll in many courses, and a course can have many students. A junction table such as `enrollments(student_id, course_id)` represents this many-to-many relationship.",
          "lessonContent": {
            "textAndVisuals": {
              "content": "## The relational model\n\nA relational database represents data as **relations (tables)**. A table contains **rows** and **columns**.\n\n- **Table:** a set of related records\n- **Row:** one record/tuple\n- **Column:** one attribute/property shared by records\n- **Schema:** the defined structure of database objects and their relationships\n\n## Keys\n\n### Primary key\n\nA **primary key (PK)** uniquely identifies each row in a table. A primary key cannot contain NULL values, and a table has one primary-key constraint (which may contain multiple columns).\n\n### Foreign key\n\nA **foreign key (FK)** stores values that refer to a key in another table. It helps represent relationships such as customer → orders or department → employees.\n\n### Composite key\n\nA **composite key** uses more than one column together to uniquely identify a row. This is common in relationship tables, such as `student_id + course_id`.\n\n### UNIQUE constraint\n\nA `UNIQUE` constraint prevents duplicate values in the constrained column or column combination. It is different from the primary key because it is not the table's primary identifier.\n\n## Relationships\n\n- **One-to-one:** one row corresponds to at most one row in the other table.\n- **One-to-many:** one parent row can relate to many child rows.\n- **Many-to-many:** many rows on both sides relate to many rows on the other side, usually implemented with a junction/bridge table.\n\n### Example\n\nA student can enroll in many courses, and a course can have many students. A junction table such as `enrollments(student_id, course_id)` represents this many-to-many relationship."
            }
          },
          "sqlExercise": {
            "schemaSql": "CREATE TABLE students (\n    student_id INTEGER PRIMARY KEY,\n    student_name TEXT NOT NULL\n);\nCREATE TABLE courses (\n    course_id INTEGER PRIMARY KEY,\n    course_name TEXT NOT NULL\n);\nCREATE TABLE enrollments (\n    student_id INTEGER,\n    course_id INTEGER,\n    PRIMARY KEY (student_id, course_id)\n);",
            "seedSql": "INSERT INTO students (student_id, student_name) VALUES\n(1, 'Kavin'),\n(2, 'Meera'),\n(3, 'Rohan');\nINSERT INTO courses (course_id, course_name) VALUES\n(10, 'SQL'),\n(20, 'Java'),\n(30, 'Python');\nINSERT INTO enrollments (student_id, course_id) VALUES\n(1, 10),\n(1, 20),\n(2, 10),\n(3, 30);",
            "defaultTable": "students",
            "defaultQuery": "SELECT * FROM enrollments;",
            "tasks": [
              {
                "instruction": "List each student with the course they are enrolled in.",
                "expectedQuery": "SELECT s.student_name, c.course_name FROM enrollments AS e JOIN students AS s ON e.student_id = s.student_id JOIN courses AS c ON e.course_id = c.course_id ORDER BY s.student_id ASC, c.course_id ASC;",
                "solution": "SELECT s.student_name, c.course_name FROM enrollments AS e JOIN students AS s ON e.student_id = s.student_id JOIN courses AS c ON e.course_id = c.course_id ORDER BY s.student_id ASC, c.course_id ASC;",
                "orderSensitive": false
              },
              {
                "instruction": "Find all students enrolled in SQL.",
                "expectedQuery": "SELECT s.student_name FROM enrollments AS e JOIN students AS s ON e.student_id = s.student_id WHERE e.course_id = 10 ORDER BY s.student_id ASC;",
                "solution": "SELECT s.student_name FROM enrollments AS e JOIN students AS s ON e.student_id = s.student_id WHERE e.course_id = 10 ORDER BY s.student_id ASC;",
                "orderSensitive": false
              },
              {
                "instruction": "Count how many courses each student is enrolled in.",
                "expectedQuery": "SELECT s.student_name, COUNT(e.course_id) AS course_count FROM students AS s LEFT JOIN enrollments AS e ON s.student_id = e.student_id GROUP BY s.student_id, s.student_name ORDER BY s.student_id ASC;",
                "solution": "SELECT s.student_name, COUNT(e.course_id) AS course_count FROM students AS s LEFT JOIN enrollments AS e ON s.student_id = e.student_id GROUP BY s.student_id, s.student_name ORDER BY s.student_id ASC;",
                "orderSensitive": false
              }
            ]
          }
        },
        {
          "topicId": "sql-m6-t4",
          "title": "SQL Command Types and Data Types",
          "description": "Foundation lesson: SQL Command Types and Data Types. Build clear conceptual understanding and reinforce it with practical SQL exercises.",
          "mode": "SQL_INTERACTIVE",
          "readingTimeMinutes": 40,
          "lessonMarkdown": "## SQL command families\n\nSQL statements are commonly grouped by purpose. Exact classifications can vary slightly between references, but the following categories are useful for learning.\n\n### DQL — Data Query Language\n\n`SELECT` retrieves data.\n\n### DML — Data Manipulation Language\n\nCommands such as `INSERT`, `UPDATE`, and `DELETE` change row data.\n\n### DDL — Data Definition Language\n\nCommands such as `CREATE`, `ALTER`, and `DROP` define or change database objects such as tables.\n\n### DCL — Data Control Language\n\nCommands such as `GRANT` and `REVOKE` manage access permissions in systems that support them.\n\n### TCL — Transaction Control Language\n\nCommands such as `COMMIT`, `ROLLBACK`, and `SAVEPOINT` control transactions in database systems that support transactional commands.\n\n## Common SQL data types\n\nTypical relational systems provide categories such as:\n- Integer and other exact numeric types\n- Decimal/fixed-point numeric types\n- Floating-point types\n- Character/text types\n- Date/time types\n- Boolean types\n- Binary/large-object types\n\nThe exact names and behavior vary between DBMS products. For example, SQLite uses storage classes including `NULL`, `INTEGER`, `REAL`, `TEXT`, and `BLOB`, and applies dynamic typing rules.\n\n### NULL is not a data type synonym for empty\n\n`NULL` represents a missing/unknown value. It is not the same as zero, an empty string, or the word `'NULL'`. Use `IS NULL` and `IS NOT NULL` to test for NULL.",
          "lessonContent": {
            "textAndVisuals": {
              "content": "## SQL command families\n\nSQL statements are commonly grouped by purpose. Exact classifications can vary slightly between references, but the following categories are useful for learning.\n\n### DQL — Data Query Language\n\n`SELECT` retrieves data.\n\n### DML — Data Manipulation Language\n\nCommands such as `INSERT`, `UPDATE`, and `DELETE` change row data.\n\n### DDL — Data Definition Language\n\nCommands such as `CREATE`, `ALTER`, and `DROP` define or change database objects such as tables.\n\n### DCL — Data Control Language\n\nCommands such as `GRANT` and `REVOKE` manage access permissions in systems that support them.\n\n### TCL — Transaction Control Language\n\nCommands such as `COMMIT`, `ROLLBACK`, and `SAVEPOINT` control transactions in database systems that support transactional commands.\n\n## Common SQL data types\n\nTypical relational systems provide categories such as:\n- Integer and other exact numeric types\n- Decimal/fixed-point numeric types\n- Floating-point types\n- Character/text types\n- Date/time types\n- Boolean types\n- Binary/large-object types\n\nThe exact names and behavior vary between DBMS products. For example, SQLite uses storage classes including `NULL`, `INTEGER`, `REAL`, `TEXT`, and `BLOB`, and applies dynamic typing rules.\n\n### NULL is not a data type synonym for empty\n\n`NULL` represents a missing/unknown value. It is not the same as zero, an empty string, or the word `'NULL'`. Use `IS NULL` and `IS NOT NULL` to test for NULL."
            }
          },
          "sqlExercise": {
            "schemaSql": "CREATE TABLE products (\n    product_id INTEGER PRIMARY KEY,\n    product_name TEXT NOT NULL,\n    price REAL,\n    in_stock INTEGER,\n    sku TEXT UNIQUE\n);",
            "seedSql": "INSERT INTO products (product_id, product_name, price, in_stock, sku) VALUES\n(1, 'Keyboard', 1500.00, 1, 'KB100'),\n(2, 'Mouse', 800.00, 1, 'MS200'),\n(3, 'Monitor', 12500.00, 0, 'MN300'),\n(4, 'Webcam', 3200.00, 1, NULL);",
            "defaultTable": "products",
            "defaultQuery": "SELECT product_id, product_name, price FROM products;",
            "tasks": [
              {
                "instruction": "List all products with their prices.",
                "expectedQuery": "SELECT product_name, price FROM products ORDER BY product_id ASC;",
                "solution": "SELECT product_name, price FROM products ORDER BY product_id ASC;",
                "orderSensitive": false
              },
              {
                "instruction": "List products that are currently in stock.",
                "expectedQuery": "SELECT product_name FROM products WHERE in_stock = 1 ORDER BY product_id ASC;",
                "solution": "SELECT product_name FROM products WHERE in_stock = 1 ORDER BY product_id ASC;",
                "orderSensitive": false
              },
              {
                "instruction": "Find products whose SKU has not been assigned yet.",
                "expectedQuery": "SELECT product_name FROM products WHERE sku IS NULL ORDER BY product_id ASC;",
                "solution": "SELECT product_name FROM products WHERE sku IS NULL ORDER BY product_id ASC;",
                "orderSensitive": false
              }
            ]
          }
        },
        {
          "topicId": "sql-m6-t5",
          "title": "Constraints, Transactions, ACID and Normalization Basics",
          "description": "Foundation lesson: Constraints, Transactions, ACID and Normalization Basics. Build clear conceptual understanding and reinforce it with practical SQL exercises.",
          "mode": "SQL_INTERACTIVE",
          "readingTimeMinutes": 40,
          "lessonMarkdown": "## Data integrity with constraints\n\nConstraints help the database reject invalid data rather than relying entirely on application code. Common constraints include:\n\n- `PRIMARY KEY` — uniquely identifies rows\n- `FOREIGN KEY` — enforces valid references between tables\n- `NOT NULL` — requires a value\n- `UNIQUE` — prevents duplicate constrained values\n- `CHECK` — enforces a condition\n- `DEFAULT` — supplies a value when one is not explicitly provided\n\n## Transactions\n\nA **transaction** groups related database operations into a logical unit of work. A typical transaction may either complete successfully or be rolled back when a failure occurs.\n\nFor example, transferring money from account A to account B conceptually requires both a debit and a credit. Committing only one side would leave the data inconsistent.\n\n## ACID properties\n\n**Atomicity:** all operations in a transaction happen as one unit, or the transaction is rolled back.\n\n**Consistency:** a successful transaction takes the database from one valid state to another valid state, respecting defined integrity rules.\n\n**Isolation:** concurrent transactions are controlled so that intermediate work does not produce unacceptable interference.\n\n**Durability:** once a transaction is committed, its changes are intended to survive failures according to the database system's durability guarantees.\n\n## Normalization basics\n\nNormalization is a design technique used to reduce unnecessary duplication and update anomalies.\n\n**1NF:** values are atomic and repeating groups are removed.\n\n**2NF:** the table is in 1NF and non-key attributes depend on the whole key, which is especially important for composite keys.\n\n**3NF:** the table is in 2NF and non-key attributes do not depend transitively on another non-key attribute.\n\nNormalization is a design choice, not a rule that every database must maximize at all costs. Some systems intentionally denormalize selected data for performance or reporting, while maintaining correctness carefully.",
          "lessonContent": {
            "textAndVisuals": {
              "content": "## Data integrity with constraints\n\nConstraints help the database reject invalid data rather than relying entirely on application code. Common constraints include:\n\n- `PRIMARY KEY` — uniquely identifies rows\n- `FOREIGN KEY` — enforces valid references between tables\n- `NOT NULL` — requires a value\n- `UNIQUE` — prevents duplicate constrained values\n- `CHECK` — enforces a condition\n- `DEFAULT` — supplies a value when one is not explicitly provided\n\n## Transactions\n\nA **transaction** groups related database operations into a logical unit of work. A typical transaction may either complete successfully or be rolled back when a failure occurs.\n\nFor example, transferring money from account A to account B conceptually requires both a debit and a credit. Committing only one side would leave the data inconsistent.\n\n## ACID properties\n\n**Atomicity:** all operations in a transaction happen as one unit, or the transaction is rolled back.\n\n**Consistency:** a successful transaction takes the database from one valid state to another valid state, respecting defined integrity rules.\n\n**Isolation:** concurrent transactions are controlled so that intermediate work does not produce unacceptable interference.\n\n**Durability:** once a transaction is committed, its changes are intended to survive failures according to the database system's durability guarantees.\n\n## Normalization basics\n\nNormalization is a design technique used to reduce unnecessary duplication and update anomalies.\n\n**1NF:** values are atomic and repeating groups are removed.\n\n**2NF:** the table is in 1NF and non-key attributes depend on the whole key, which is especially important for composite keys.\n\n**3NF:** the table is in 2NF and non-key attributes do not depend transitively on another non-key attribute.\n\nNormalization is a design choice, not a rule that every database must maximize at all costs. Some systems intentionally denormalize selected data for performance or reporting, while maintaining correctness carefully."
            }
          },
          "sqlExercise": {
            "schemaSql": "CREATE TABLE accounts (\n    account_id INTEGER PRIMARY KEY,\n    account_holder TEXT NOT NULL,\n    balance REAL NOT NULL CHECK (balance >= 0)\n);\nCREATE TABLE payments (\n    payment_id INTEGER PRIMARY KEY,\n    account_id INTEGER NOT NULL,\n    amount REAL NOT NULL CHECK (amount > 0),\n    status TEXT NOT NULL DEFAULT 'PENDING'\n);",
            "seedSql": "INSERT INTO accounts (account_id, account_holder, balance) VALUES\n(1, 'Arun', 25000.00),\n(2, 'Bhavya', 18000.00),\n(3, 'Charan', 32000.00);\nINSERT INTO payments (payment_id, account_id, amount, status) VALUES\n(9001, 1, 1500.00, 'PAID'),\n(9002, 1, 700.00, 'PENDING'),\n(9003, 2, 2500.00, 'PAID');",
            "defaultTable": "accounts",
            "defaultQuery": "SELECT * FROM accounts;",
            "tasks": [
              {
                "instruction": "List all account holders with their balances.",
                "expectedQuery": "SELECT account_holder, balance FROM accounts ORDER BY account_id ASC;",
                "solution": "SELECT account_holder, balance FROM accounts ORDER BY account_id ASC;",
                "orderSensitive": false
              },
              {
                "instruction": "Find all pending payments and their amounts.",
                "expectedQuery": "SELECT payment_id, amount FROM payments WHERE status = 'PENDING' ORDER BY payment_id ASC;",
                "solution": "SELECT payment_id, amount FROM payments WHERE status = 'PENDING' ORDER BY payment_id ASC;",
                "orderSensitive": false
              },
              {
                "instruction": "Calculate the total value of paid payments for each account.",
                "expectedQuery": "SELECT account_id, SUM(amount) AS paid_total FROM payments WHERE status = 'PAID' GROUP BY account_id ORDER BY account_id ASC;",
                "solution": "SELECT account_id, SUM(amount) AS paid_total FROM payments WHERE status = 'PAID' GROUP BY account_id ORDER BY account_id ASC;",
                "orderSensitive": false
              }
            ]
          }
        }
      ],
      "msa": {
        "title": "Module 6 Assessment: SQL & Database Fundamentals",
        "passPercent": 80,
        "durationMinutes": 30,
        "mcqSection": {
          "sectionTitle": "Section 1: DBMS, RDBMS, SQL Basics & Data Management MCQs",
          "durationMinutes": 30,
          "passCutoffPercent": 80,
          "questions": [
            {
              "questionId": "sql-m6-msa-q01",
              "question": "What is a database?",
              "prompt": "What is a database?",
              "options": [
                "A programing language used only to format web pages",
                "An organized collection of related data",
                "A hardware device used to connect computers",
                "A single SQL statement"
              ],
              "correctAnswer": 1,
              "correctIndex": 1,
              "correctOptionIndex": 1,
              "explanation": "A database stores organized information so it can be managed and retrieved."
            },
            {
              "questionId": "sql-m6-msa-q02",
              "question": "What is the primary role of a DBMS?",
              "prompt": "What is the primary role of a DBMS?",
              "options": [
                "To design website layouts",
                "To manage storage, retrieval, updates, security, and other database operations",
                "To replace every programming language",
                "To act only as a spreadsheet viewer"
              ],
              "correctAnswer": 1,
              "correctIndex": 1,
              "correctOptionIndex": 1,
              "explanation": "A DBMS is software that manages data stored in databases."
            },
            {
              "questionId": "sql-m6-msa-q03",
              "question": "What does RDBMS mean?",
              "prompt": "What does RDBMS mean?",
              "options": [
                "Random Database Management System",
                "Relational Database Management System",
                "Remote Data Backup Management Service",
                "Rapid Data Build Management System"
              ],
              "correctAnswer": 1,
              "correctIndex": 1,
              "correctOptionIndex": 1,
              "explanation": "RDBMS stands for Relational Database Management System."
            },
            {
              "questionId": "sql-m6-msa-q04",
              "question": "Which database model organizes records primarily in a tree-like parent-child structure?",
              "prompt": "Which database model organizes records primarily in a tree-like parent-child structure?",
              "options": [
                "Hierarchical",
                "Relational",
                "Document",
                "Graph"
              ],
              "correctAnswer": 0,
              "correctIndex": 0,
              "correctOptionIndex": 0,
              "explanation": "The hierarchical model uses a tree-like structure of parent and child records."
            },
            {
              "questionId": "sql-m6-msa-q05",
              "question": "Which model represents data primarily as rows and columns in related tables?",
              "prompt": "Which model represents data primarily as rows and columns in related tables?",
              "options": [
                "Network",
                "Relational",
                "Key-value",
                "Hierarchical"
              ],
              "correctAnswer": 1,
              "correctIndex": 1,
              "correctOptionIndex": 1,
              "explanation": "The relational model represents data as relations (tables) consisting of rows and columns."
            },
            {
              "questionId": "sql-m6-msa-q06",
              "question": "Which database type commonly stores semi-structured JSON-like documents?",
              "prompt": "Which database type commonly stores semi-structured JSON-like documents?",
              "options": [
                "Document database",
                "Hierarchical database",
                "Spreadsheet database",
                "Network database"
              ],
              "correctAnswer": 0,
              "correctIndex": 0,
              "correctOptionIndex": 0,
              "explanation": "Document databases commonly store self-contained document structures such as JSON-like objects."
            },
            {
              "questionId": "sql-m6-msa-q07",
              "question": "What is the main purpose of a primary key?",
              "prompt": "What is the main purpose of a primary key?",
              "options": [
                "To sort rows alphabetically",
                "To uniquely identify each row",
                "To store only encrypted passwords",
                "To allow duplicate identifiers"
              ],
              "correctAnswer": 1,
              "correctIndex": 1,
              "correctOptionIndex": 1,
              "explanation": "A primary key identifies rows uniquely."
            },
            {
              "questionId": "sql-m6-msa-q08",
              "question": "What is the role of a foreign key?",
              "prompt": "What is the role of a foreign key?",
              "options": [
                "It references a key in another table to represent a relationship",
                "It always stores a decimal value",
                "It automatically creates an index in every DBMS",
                "It replaces the primary key"
              ],
              "correctAnswer": 0,
              "correctIndex": 0,
              "correctOptionIndex": 0,
              "explanation": "A foreign key stores values that refer to a key in another table."
            },
            {
              "questionId": "sql-m6-msa-q09",
              "question": "What is a composite key?",
              "prompt": "What is a composite key?",
              "options": [
                "A key made from two or more columns",
                "A key stored only as text",
                "A key that can contain duplicate rows",
                "A key that exists only in NoSQL databases"
              ],
              "correctAnswer": 0,
              "correctIndex": 0,
              "correctOptionIndex": 0,
              "explanation": "A composite key uses multiple columns together to identify a row uniquely."
            },
            {
              "questionId": "sql-m6-msa-q10",
              "question": "Which constraint prevents a column from storing NULL values?",
              "prompt": "Which constraint prevents a column from storing NULL values?",
              "options": [
                "CHECK",
                "NOT NULL",
                "DEFAULT",
                "ORDER BY"
              ],
              "correctAnswer": 1,
              "correctIndex": 1,
              "correctOptionIndex": 1,
              "explanation": "NOT NULL requires a value to be present."
            },
            {
              "questionId": "sql-m6-msa-q11",
              "question": "Which group is normally classified as DDL?",
              "prompt": "Which group is normally classified as DDL?",
              "options": [
                "SELECT, WHERE, ORDER BY",
                "CREATE, ALTER, DROP",
                "INSERT, UPDATE, DELETE",
                "GRANT, REVOKE only"
              ],
              "correctAnswer": 1,
              "correctIndex": 1,
              "correctOptionIndex": 1,
              "explanation": "DDL defines or changes database objects; CREATE, ALTER, and DROP are common DDL commands."
            },
            {
              "questionId": "sql-m6-msa-q12",
              "question": "Which group is normally classified as DML?",
              "prompt": "Which group is normally classified as DML?",
              "options": [
                "INSERT, UPDATE, DELETE",
                "CREATE, ALTER, DROP",
                "COMMIT, ROLLBACK, SAVEPOINT",
                "GRANT, REVOKE"
              ],
              "correctAnswer": 0,
              "correctIndex": 0,
              "correctOptionIndex": 0,
              "explanation": "DML changes row data using commands such as INSERT, UPDATE, and DELETE."
            },
            {
              "questionId": "sql-m6-msa-q13",
              "question": "Which SQL statement retrieves rows from a table?",
              "prompt": "Which SQL statement retrieves rows from a table?",
              "options": [
                "SELECT",
                "CREATE",
                "COMMIT",
                "GRANT"
              ],
              "correctAnswer": 0,
              "correctIndex": 0,
              "correctOptionIndex": 0,
              "explanation": "SELECT is used to retrieve query results."
            },
            {
              "questionId": "sql-m6-msa-q14",
              "question": "Which commands are commonly associated with transaction control?",
              "prompt": "Which commands are commonly associated with transaction control?",
              "options": [
                "COMMIT and ROLLBACK",
                "SELECT and WHERE",
                "CREATE and DROP",
                "GRANT and REVOKE"
              ],
              "correctAnswer": 0,
              "correctIndex": 0,
              "correctOptionIndex": 0,
              "explanation": "COMMIT and ROLLBACK control the outcome of a transaction in systems that support transactional commands."
            },
            {
              "questionId": "sql-m6-msa-q15",
              "question": "Which SQL value represents missing or unknown information?",
              "prompt": "Which SQL value represents missing or unknown information?",
              "options": [
                "0",
                "An empty string",
                "NULL",
                "FALSE"
              ],
              "correctAnswer": 2,
              "correctIndex": 2,
              "correctOptionIndex": 2,
              "explanation": "NULL represents missing or unknown information and is not the same as zero or an empty string."
            },
            {
              "questionId": "sql-m6-msa-q16",
              "question": "Which property of ACID means a transaction is all-or-nothing?",
              "prompt": "Which property of ACID means a transaction is all-or-nothing?",
              "options": [
                "Consistency",
                "Isolation",
                "Atomicity",
                "Durability"
              ],
              "correctAnswer": 2,
              "correctIndex": 2,
              "correctOptionIndex": 2,
              "explanation": "Atomicity means the transaction is treated as one unit of work."
            },
            {
              "questionId": "sql-m6-msa-q17",
              "question": "Which ACID property concerns the persistence of committed changes?",
              "prompt": "Which ACID property concerns the persistence of committed changes?",
              "options": [
                "Durability",
                "Atomicity",
                "Isolation",
                "Normalization"
              ],
              "correctAnswer": 0,
              "correctIndex": 0,
              "correctOptionIndex": 0,
              "explanation": "Durability concerns retaining committed changes across failures according to the DBMS guarantees."
            },
            {
              "questionId": "sql-m6-msa-q18",
              "question": "What is the main goal of normalization?",
              "prompt": "What is the main goal of normalization?",
              "options": [
                "Increase unnecessary duplication",
                "Reduce redundancy and update anomalies",
                "Remove every table from a database",
                "Guarantee zero query execution time"
              ],
              "correctAnswer": 1,
              "correctIndex": 1,
              "correctOptionIndex": 1,
              "explanation": "Normalization is used to reduce redundancy and improve data integrity."
            },
            {
              "questionId": "sql-m6-msa-q19",
              "question": "What is a key idea of First Normal Form (1NF)?",
              "prompt": "What is a key idea of First Normal Form (1NF)?",
              "options": [
                "Every table must have exactly one column",
                "Values should be atomic and repeating groups removed",
                "Every table must have three candidate keys",
                "All data must be stored as JSON"
              ],
              "correctAnswer": 1,
              "correctIndex": 1,
              "correctOptionIndex": 1,
              "explanation": "1NF requires atomic values and removes repeating groups."
            },
            {
              "questionId": "sql-m6-msa-q20",
              "question": "What does a many-to-many relationship usually require in a relational design?",
              "prompt": "What does a many-to-many relationship usually require in a relational design?",
              "options": [
                "A single text column containing all related IDs",
                "A junction/bridge table",
                "No keys at all",
                "Only a view"
              ],
              "correctAnswer": 1,
              "correctIndex": 1,
              "correctOptionIndex": 1,
              "explanation": "A junction table records the pairings between rows on both sides."
            },
            {
              "questionId": "sql-m6-msa-q21",
              "question": "Which constraint is designed to prevent duplicate values in a constrained column or column combination?",
              "prompt": "Which constraint is designed to prevent duplicate values in a constrained column or column combination?",
              "options": [
                "UNIQUE",
                "LIMIT",
                "OFFSET",
                "LIKE"
              ],
              "correctAnswer": 0,
              "correctIndex": 0,
              "correctOptionIndex": 0,
              "explanation": "UNIQUE prevents duplicate constrained values."
            },
            {
              "questionId": "sql-m6-msa-q22",
              "question": "Which command grants privileges in database systems that support SQL access control?",
              "prompt": "Which command grants privileges in database systems that support SQL access control?",
              "options": [
                "GRANT",
                "GROUP BY",
                "MERGE",
                "VACUUM"
              ],
              "correctAnswer": 0,
              "correctIndex": 0,
              "correctOptionIndex": 0,
              "explanation": "GRANT is commonly used to assign privileges."
            },
            {
              "questionId": "sql-m6-msa-q23",
              "question": "Which of the following is a SQLite storage class?",
              "prompt": "Which of the following is a SQLite storage class?",
              "options": [
                "INTEGER",
                "VARCHAR2 only",
                "NUMBERIC",
                "DATETIME2"
              ],
              "correctAnswer": 0,
              "correctIndex": 0,
              "correctOptionIndex": 0,
              "explanation": "SQLite uses storage classes including NULL, INTEGER, REAL, TEXT, and BLOB."
            },
            {
              "questionId": "sql-m6-msa-q24",
              "question": "Which statement best describes a foreign key relationship?",
              "prompt": "Which statement best describes a foreign key relationship?",
              "options": [
                "It guarantees two columns have the same name",
                "It helps enforce valid references between related tables",
                "It always makes a query faster",
                "It stores passwords securely by default"
              ],
              "correctAnswer": 1,
              "correctIndex": 1,
              "correctOptionIndex": 1,
              "explanation": "Foreign keys represent references between rows in related tables and can enforce referential integrity."
            },
            {
              "questionId": "sql-m6-msa-q25",
              "question": "Why are constraints useful?",
              "prompt": "Why are constraints useful?",
              "options": [
                "They make invalid data easier to insert",
                "They move all data into memory",
                "They let the database enforce data-integrity rules",
                "They replace transactions"
              ],
              "correctAnswer": 2,
              "correctIndex": 2,
              "correctOptionIndex": 2,
              "explanation": "Constraints allow the database to reject or restrict invalid data according to defined rules."
            }
          ]
        }
      }
    }
  ]
}